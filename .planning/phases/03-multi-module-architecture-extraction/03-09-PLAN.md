---
phase: 03-multi-module-architecture-extraction
plan: 09
type: execute
wave: 9
depends_on: ["03-08"]
files_modified:
  - shared/build.gradle.kts
  - androidApp/build.gradle.kts
  - buildSrc/.gitignore
  - buildSrc/build.gradle.kts
  - buildSrc/settings.gradle.kts
  - buildSrc/src/main/java/Dependencies.kt
autonomous: true
requirements: [ARCH-01, ARCH-02, ARCH-03, ARCH-04]

estimate:
  tokens: 90000
  raw_tokens: 90000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - ":shared is a thin umbrella (D-10): zero Kotlin sources, Gradle path :shared, framework baseName \"shared\", isStatic = true, api + export() of exactly :core:model, :core:data, :core:analytics for all three iOS targets (D-11), no export of :core:network, and the Xcode build phase ./gradlew :shared:embedAndSignAppleFrameworkForXcode unchanged"
    - "A :shared module with no own sources still produces shared.framework whose Swift-referenced type names are all present and whose collision-disambiguated name count equals the pre-phase baseline (ARCH-04 empty + adjacency edges)"
    - { statement: "The Swift-visible name set is compared as a sorted set, so the declaration order of export() calls has no effect on the generated names (ARCH-04 ordering edge)", verification: backstop }
    - ":androidApp no longer depends on :shared; it depends directly on the core and feature modules it uses"
    - "buildSrc is deleted and no build file references the old AndroidSdk object; SDK levels (min 26 / compile 37 / target 36) come only from build-logic (D-13, D-18)"
    - "No build file other than build-logic contains compileSdk, minSdk, targetSdk, compileOptions, jvmTarget or a detekt block (ARCH-01)"
    - "Gradle reports exactly 14 projects (:androidApp, :shared, :core:model, :core:network, :core:data, :core:analytics, :core:ui, :core:testing, :feature:agenda, :feature:speakers, :feature:venue, :feature:session-detail, :feature:about, :feature:settings); no :core:* module depends on a :feature:* module and no :feature:* module depends on another (ARCH-02)"
    - "Dagger Hilt is the only DI framework (D-01); per D-04 the absence of per-module Koin modules is expected in Phase 3 and is not a gap"
    - "settings.gradle.dcl remains the DCL settings file with includeBuild(\"build-logic\") and all 14 includes; every new module build file is Kotlin DSL (D-20)"
    - "The final pushed HEAD is green on both CI workflows (ARCH-02 concurrency edge, D-18)"
  artifacts:
    - path: shared/build.gradle.kts
      provides: "thin umbrella: devfest.kmp.library + framework baseName shared + 3 export() per iOS target"
  key_links:
    - from: shared/build.gradle.kts
      to: core/model, core/data, core/analytics
      via: "api(project(...)) + export(project(...)) inside binaries.framework for iosX64/iosArm64/iosSimulatorArm64"
      pattern: "export\\(project\\(\":core:(model|data|analytics)\"\\)\\)"
    - from: iosApp/iosApp.xcodeproj/project.pbxproj
      to: shared
      via: "./gradlew :shared:embedAndSignAppleFrameworkForXcode build phase (unchanged)"
      pattern: "embedAndSignAppleFrameworkForXcode"
  prohibitions:
    - requirement_id: ARCH-04
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT change the iOS integration surface — no framework rename, no Xcode project edit, no Swift source edit — so iOS users get an identical app"
---

<objective>
Final D-18 step: thin `:shared` to a pure umbrella (D-10/D-11), cut `:androidApp`'s dependency on it, delete `buildSrc` (D-13), and run the phase-level graph/duplication/regression gates before verification.

Purpose: close ARCH-01 (no duplicated build config, no buildSrc), ARCH-02 (graph direction proven for every module), ARCH-04 (umbrella with zero own sources).
Output: thin `shared/build.gradle.kts`, no `buildSrc/`, green full suite + CI on the phase PR.
</objective>

<execution_context>
@/Users/robin/Development/Android/DevFestNantes/.claude/gsd-core/workflows/execute-plan.md
@/Users/robin/Development/Android/DevFestNantes/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-multi-module-architecture-extraction/03-CONTEXT.md
@.planning/phases/03-multi-module-architecture-extraction/03-RESEARCH.md
@.planning/phases/03-multi-module-architecture-extraction/03-08-SUMMARY.md
@shared/build.gradle.kts
@androidApp/build.gradle.kts
@buildSrc/src/main/java/Dependencies.kt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Thin :shared to a pure umbrella, drop :androidApp -> :shared, delete buildSrc</name>
  <files>shared/build.gradle.kts, androidApp/build.gradle.kts, buildSrc/.gitignore, buildSrc/build.gradle.kts, buildSrc/settings.gradle.kts, buildSrc/src/main/java/Dependencies.kt</files>
  <read_first>
    - shared/build.gradle.kts (current full state after 03-01..03-03 edits)
    - androidApp/build.gradle.kts (project dependencies block)
    - build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/KmpLibraryConventionPlugin.kt and AndroidSdk.kt
    - buildSrc/src/main/java/Dependencies.kt
    - .planning/phases/03-multi-module-architecture-extraction/03-RESEARCH.md "Pattern 3" (umbrella end-state shape)
    - iosApp/iosApp.xcodeproj/project.pbxproj (confirm the build phase string, read-only)
  </read_first>
  <action>
Step 0: capture `./gradlew --no-daemon :androidApp:dependencies --configuration releaseRuntimeClasspath` to `${TMPDIR:-/tmp}/p3-09-deps-before.txt`.
Step 1 (commit "refactor: thin :shared to umbrella", D-10/D-11): confirm `git ls-files shared/src | grep '\.kt$'` is empty. Rewrite `shared/build.gradle.kts` to: `plugins { id("devfest.kmp.library") }` (targets, detekt, ObjC-name opt-in and SDK levels now come from the convention); a `kotlin { }` block that, for each Kotlin/Native iOS target declared by the convention (`targets.withType<KotlinNativeTarget>()`), declares `binaries.framework { baseName = "shared"; isStatic = true; export(project(":core:model")); export(project(":core:data")); export(project(":core:analytics")) }`; `sourceSets.commonMain.dependencies { api(project(":core:model")); api(project(":core:data")); api(project(":core:analytics")) }`; keep the existing native `binaryOptions["memoryModel"] = "experimental"` block unchanged (no behavior change). Remove the explicit android block, the detekt block, `detektPlugins`, `jvm()` duplicate, and the ksp + native-coroutines plugins (the annotations live in :core:data, 03-03); if iOS compilation of the Swift app then fails for missing async wrappers, restore those two plugins on :shared and record it in the SUMMARY. The network module is not declared here (it reaches the framework only as core:data's implementation dependency and is never exported, D-11). Do not enable transitive export. Do not touch `iosApp/` or `shared/shared.podspec`.
In `androidApp/build.gradle.kts` remove `implementation(project(":shared"))`; ensure explicit `implementation(project(...))` for `:core:model`, `:core:data`, `:core:analytics`, `:core:ui` and all six features. Diff the new release runtime classpath report against Step 0 as a normalized sorted set (tree prefixes and `(*)`/`(c)`/`(n)` markers stripped): the only removed entry may be `project :shared`; nothing else may change.
Step 2 (commit "build: remove buildSrc", D-13, same step per D-18): `git rm -r buildSrc`. `git grep -n 'AndroidSdk\.' -- '*.gradle.kts'` must print nothing (all consumers use build-logic conventions). Verify both commits green.
  </action>
  <verify>
    <automated>test -z "$(git ls-files buildSrc)" && ./gradlew --no-daemon :androidApp:dependencies --configuration releaseRuntimeClasspath > "${TMPDIR:-/tmp}/p3-09-deps-after.txt" && N() { sed -E 's/^[|+\\ -]+//; s/ \([*cn]\)$//' "$1" | grep -E '^(project |[A-Za-z0-9_.-]+:)' | grep -v '^project :shared$' | sort -u; } && diff <(N "${TMPDIR:-/tmp}/p3-09-deps-before.txt") <(N "${TMPDIR:-/tmp}/p3-09-deps-after.txt") > "${TMPDIR:-/tmp}/p3-09-deps-extra.txt"; test ! -s "${TMPDIR:-/tmp}/p3-09-deps-extra.txt" && ./gradlew --no-daemon :shared:linkDebugFrameworkIosSimulatorArm64 :androidApp:assembleDebug :androidApp:assembleRelease detekt && bash .planning/phases/03-multi-module-architecture-extraction/swift-names-gate.sh check</automated>
    <fails_when>non-zero exit, buildSrc still tracked, any release-classpath change other than the removed :shared entries (p3-09-deps-extra.txt non-empty), or last line not SWIFT-NAMES-OK</fails_when>
  </verify>
  <acceptance_criteria>
    - `git ls-files buildSrc` prints nothing; `git grep -n 'AndroidSdk\.' -- '*.gradle.kts'` prints nothing.
    - `shared/build.gradle.kts` contains `id("devfest.kmp.library")`, `baseName = "shared"`, `isStatic = true`, `export(project(":core:model"))`, `export(project(":core:data"))`, `export(project(":core:analytics"))`, and contains no `core:network`, no `transitiveExport`, no `compileSdk`, no `detekt {`.
    - `git ls-files shared/src | grep -c '\.kt$'` prints 0.
    - `androidApp/build.gradle.kts` contains no `project(":shared")`.
    - `git diff origin/main --stat -- iosApp/` prints nothing (iOS project and Swift untouched).
    - `SWIFT-NAMES-OK` printed.
  </acceptance_criteria>
  <done>:shared is a source-less umbrella producing the same Swift-facing framework; the Android app no longer routes through it; buildSrc is gone; two commits.</done>
</task>

<task type="auto">
  <name>Task 2: Phase gates — graph direction, no duplicated config, full suite, final CI</name>
  <files>none (verification only; fixes, if any, go back to the owning plan's files)</files>
  <read_first>
    - settings.gradle.dcl
    - .planning/phases/03-multi-module-architecture-extraction/03-VALIDATION.md (full-suite command and sampling rules)
    - .planning/phases/03-multi-module-architecture-extraction/03-CONTEXT.md (D-01, D-04, D-07, D-20)
  </read_first>
  <action>Run the three gates below. Gate A (graph, ARCH-02): for every core module, `./gradlew :core:<m>:dependencies` must list no `project :feature:`; for every feature module, its releaseRuntimeClasspath must list no `project :feature:`; `./gradlew projects` must list exactly the 14 projects. Gate B (source/config hygiene, ARCH-01/D-01/D-07/D-20): no leaf build file (all `core/*`, `feature/*`, `shared`, `androidApp` build.gradle.kts) contains compileSdk, minSdk, targetSdk, compileOptions, jvmTarget or a detekt block; no Kotlin/Gradle/TOML file mentions the DI library D-01 defers to Phase 4 (D-04 makes its absence expected); no feature Kotlin file references the Navigation Compose controller type (D-07); `settings.gradle.dcl` exists with the build-logic includeBuild, no root `settings.gradle.kts` exists, and no `*.gradle.dcl` module build file was introduced (D-20). Gate C (full suite, 03-VALIDATION.md): clean build + all module tests + iOS framework link + detekt/lint, then push and require both workflows green. If any gate fails, fix in a new commit (never amend) and re-run all three. Record gate outputs in the SUMMARY.</action>
  <verify>
    <automated>./gradlew --no-daemon projects > "${TMPDIR:-/tmp}/p3-projects.txt" && test "$(grep -oE "Project ':[A-Za-z:-]+'" "${TMPDIR:-/tmp}/p3-projects.txt" | sort -u | wc -l | tr -d ' ')" -eq 14 && grep -q "Project ':feature:session-detail'" "${TMPDIR:-/tmp}/p3-projects.txt" && grep -q "Project ':core:testing'" "${TMPDIR:-/tmp}/p3-projects.txt" && for M in core:model core:network core:data core:analytics core:testing; do ./gradlew --no-daemon -q :$M:dependencies > "${TMPDIR:-/tmp}/p3-dep-$M.txt" || exit 1; ! grep -q 'project :feature:' "${TMPDIR:-/tmp}/p3-dep-$M.txt" || { echo "CORE-TO-FEATURE $M"; exit 1; }; done && for M in core:ui feature:agenda feature:speakers feature:venue feature:session-detail feature:about feature:settings; do ./gradlew --no-daemon -q :$M:dependencies --configuration releaseRuntimeClasspath > "${TMPDIR:-/tmp}/p3-dep-$M.txt" || exit 1; ! grep -q 'project :feature:' "${TMPDIR:-/tmp}/p3-dep-$M.txt" || { echo "BAD-EDGE $M"; exit 1; }; done && echo "GRAPH-OK"</automated>
    <fails_when>non-zero exit, a CORE-TO-FEATURE or BAD-EDGE line, project count not 14, or output lacks GRAPH-OK</fails_when>
    <automated>LEAF=$(git ls-files 'core/*/build.gradle.kts' 'feature/*/build.gradle.kts' shared/build.gradle.kts androidApp/build.gradle.kts) || exit 1; test -n "$LEAF" && ! grep -nE 'compileSdk|minSdk|targetSdk|compileOptions|jvmTarget|detekt \{' $LEAF && KOIN=$(git grep -n -i 'koin' -- '*.kt' '*.kts' '*.toml'); test -z "$KOIN" && NAV=$(git grep -n 'NavController' -- 'feature/*.kt'); test -z "$NAV" && test -f settings.gradle.dcl && test ! -f settings.gradle.kts && grep -q 'includeBuild("build-logic")' settings.gradle.dcl && test -z "$(git ls-files '*/build.gradle.dcl')" && echo "HYGIENE-OK"</automated>
    <fails_when>output lacks HYGIENE-OK — a leaf build file repeats shared config, a Koin reference exists, a feature references the nav controller, or the DCL settings/includeBuild setup changed</fails_when>
    <automated>./gradlew --no-daemon clean detekt lint assembleDebug assembleRelease testDebugUnitTest :core:model:jvmTest :core:network:jvmTest :core:analytics:jvmTest :core:data:jvmTest :core:testing:jvmTest :shared:linkDebugFrameworkIosSimulatorArm64 && bash .planning/phases/03-multi-module-architecture-extraction/swift-names-gate.sh check && bash .planning/phases/03-multi-module-architecture-extraction/resources-gate.sh</automated>
    <fails_when>non-zero exit, last swift gate line not SWIFT-NAMES-OK, or RESOURCES-DRIFT</fails_when>
    <automated>SHA=$(git rev-parse HEAD) || exit 1; BR=$(git branch --show-current) || exit 1; test -n "$SHA" -a -n "$BR" || { echo "NO-GIT-CONTEXT"; exit 1; }; for WF in android.yml ios.yml; do RID=$(gh run list --workflow "$WF" --branch "$BR" --limit 20 --json databaseId,headSha --jq "[.[]|select(.headSha==\"$SHA\")][0].databaseId"); test -n "$RID" || { echo "NO-RUN $WF $SHA"; exit 1; }; gh run watch "$RID" --exit-status || { echo "RED $WF $RID"; exit 1; }; done && echo "CI-GREEN-BOTH $SHA"</automated>
    <fails_when>non-zero exit, or output contains NO-RUN, RED or NO-GIT-CONTEXT instead of CI-GREEN-BOTH</fails_when>
  </verify>
  <acceptance_criteria>
    - `GRAPH-OK`, `HYGIENE-OK`, `SWIFT-NAMES-OK`, `RESOURCES-OK`, `CI-GREEN-BOTH <sha>` all printed for the final HEAD.
    - `core/data/build/test-results/jvmTest` contains results for `GraphQLStoreJvmTest` and `DevFestNantesStoreContractTest`; `core/model/build/test-results/jvmTest` contains `ScheduleSlotDateParsingTest`; `core/testing/build/test-results/jvmTest` contains `FakeStoresTest` — all 0 failures.
  </acceptance_criteria>
  <done>Every phase-level invariant is proven on the final commit and both CI workflows are green; the phase PR is ready for /gsd-verify-work.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kotlin umbrella framework -> Swift | Final exported API surface of the iOS binary |
| Build tooling -> shipped Android app | Removing buildSrc and the :shared edge must not change the release classpath |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-23 | Information Disclosure | shared/build.gradle.kts export list | medium | mitigate | Acceptance: exactly three export() lines, no core:network, no transitive export; swift-names gate diff classified in SUMMARY |
| T-03-24 | Tampering | :androidApp release runtime classpath | medium | mitigate | Before/after dependency report diff may only drop `project :shared` lines |
| T-03-25 | Repudiation | Module graph erosion (core -> feature, feature -> feature) | low | mitigate | GRAPH-OK gate over every module's dependency report (interim control until CICD-V2-01 lint) |
</threat_model>

<verification>
- `SWIFT-NAMES-OK`, `GRAPH-OK`, `HYGIENE-OK`, `RESOURCES-OK`, `CI-GREEN-BOTH`.
Edge coverage authored here: ARCH-04 empty (source-less umbrella), ARCH-04 adjacency, ARCH-04 ordering (backstop), ARCH-02 concurrency. FA-1 (ARCH-01 unclassified edge) remains a flagged assumption: duplication beyond the grepped keys is judged at code review.
</verification>

<success_criteria>
- ARCH-01: convention plugins + catalog, no duplicated config in leaf build files, buildSrc gone.
- ARCH-02: 6 core modules, strictly unidirectional graph (GRAPH-OK).
- ARCH-03: 6 feature modules (verified in 03-06..03-08, re-checked by GRAPH-OK).
- ARCH-04: one umbrella framework `shared`, Swift names unchanged, iOS CI green.
</success_criteria>

## Artifacts this phase produces (this plan)

- Rewritten `shared/build.gradle.kts` (thin umbrella); deleted `buildSrc/` (object `AndroidSdk` now only `com.gdgnantes.devfest.buildlogic.AndroidSdk`)
- No new classes/functions; final module set: 14 Gradle projects + `build-logic` included build

<output>
Create `.planning/phases/03-multi-module-architecture-extraction/03-09-SUMMARY.md` when done (gate outputs, final swift-names diff classification, CI URLs).
</output>
