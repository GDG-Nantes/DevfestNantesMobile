---
phase: 03-multi-module-architecture-extraction
plan: 08
type: execute
wave: 9
depends_on: ["03-07"]
files_modified:
  - settings.gradle.dcl
  - feature/agenda/build.gradle.kts
  - feature/agenda/src/main/AndroidManifest.xml
  - feature/agenda/src/main/java/** (moved agenda screens, AgendaViewModel, PagerTab, SessionFilter, SessionTypeUtils, SessionFiltersService; new AgendaRoute.kt)
  - feature/agenda/src/main/res/**
  - feature/session-detail/build.gradle.kts
  - feature/session-detail/src/main/AndroidManifest.xml
  - feature/session-detail/src/main/java/** (moved session screens, SessionViewModel, FeedbackForm*, SessionComplexity, SessionType, ScheduleSlotUtils; new SessionDetailRoute.kt, OpenFeedbackConfig.kt)
  - feature/session-detail/src/main/res/**
  - core/ui/src/main/res/**
  - androidApp/build.gradle.kts
  - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/MainActivity.kt
  - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/Home.kt
  - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/home/HomeViewModel.kt
  - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/core/injection/AppModule.kt
  - androidApp/src/main/res/**
autonomous: false
requirements: [ARCH-02, ARCH-03]

estimate:
  tokens: 150000
  raw_tokens: 150000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - ":feature:agenda exists (D-05) with AgendaViewModel, Agenda/AgendaPager/AgendaColumn/AgendaRow/EmptyLayout/SessionFiltersDrawer and SessionFiltersService/Impl (D-03 consumer-owned), package com.gdgnantes.devfest.feature.agenda; the row bookmark toggle and favorites filter use :core:ui's BookmarksViewModel and :core:data's BookmarksStore (D-06)"
    - ":feature:session-detail exists at feature/session-detail (D-14) with SessionViewModel (+ assisted factory), SessionLayout/SessionDetails/SessionSpeaker, FeedbackForm/FallbackFeedbackForm/FeedbackFormViewModel, SessionComplexity/SessionType chips, package com.gdgnantes.devfest.feature.sessiondetail"
    - "OpenFeedback build-config values reach the feature through OpenFeedbackConfig provided by the single Hilt AppModule in :androidApp (D-02); computed values are identical to today"
    - "Entry points AgendaRoute, SessionDetailRoute take only callbacks/state/ViewModel (D-07); :androidApp keeps both NavHosts, Screen routes, HomeViewModel and the assisted-factory EntryPoint; route/argument formats unchanged"
    - "After this plan :androidApp's Kotlin sources are exactly the app-shell files of the module map (Application, MainActivity, Home shell, HomeViewModel, Screen, BottomAppBar, AppModule/AppScope, initializers, logging, ExternalContentService, AssistedViewModelUtils, CoroutinesDispatcherProvider, SuspendUseCase) — D-03 thin shell"
    - "No feature references any com.gdgnantes.devfest.androidapp symbol or a Navigation Compose controller type; no feature depends on another feature (ARCH-02, D-07)"
    - "SessionFiltersServiceImpl's SharedPreferences key literals are byte-identical to origin/main; resources-gate.sh prints RESOURCES-OK"
    - { statement: "Agenda room/time ordering and speaker list ordering on device are identical to the pre-phase app (sorting code moved byte-identical; confirmed at Android smoke checkpoint 3)", verification: backstop }
    - "Android smoke checkpoint 3 (D-18, after the last feature) approved; each module commit green; both CI workflows green on pushed HEAD (ARCH-02 concurrency edge)"
  artifacts:
    - path: feature/agenda/src/main/java/com/gdgnantes/devfest/feature/agenda/AgendaRoute.kt
      provides: "@Composable fun AgendaRoute(...)"
    - path: feature/session-detail/src/main/java/com/gdgnantes/devfest/feature/sessiondetail/SessionDetailRoute.kt
      provides: "@Composable fun SessionDetailRoute(viewModel: SessionViewModel, ...)"
    - path: feature/session-detail/src/main/java/com/gdgnantes/devfest/feature/sessiondetail/OpenFeedbackConfig.kt
      provides: "data class OpenFeedbackConfig(enabled: Boolean, projectId: String)"
  key_links:
    - from: androidApp/src/main/java/com/gdgnantes/devfest/androidapp/core/injection/AppModule.kt
      to: feature/session-detail OpenFeedbackConfig
      via: "@Provides fun openFeedbackConfig(): OpenFeedbackConfig from app BuildConfig"
      pattern: "OpenFeedbackConfig"
    - from: androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/home/HomeViewModel.kt
      to: feature/agenda SessionFiltersService
      via: "import com.gdgnantes.devfest.feature.agenda.services.SessionFiltersService"
      pattern: "feature\\.agenda\\.services"
  prohibitions:
    - requirement_id: ARCH-03
      category: privacy
      status: resolved
      verification: test
      resolution: null
      reason: null
      statement: "MUST NOT change any persisted SharedPreferences key or file name used by SessionFiltersServiceImpl — existing users' agenda filters must survive the update unchanged"
    - requirement_id: ARCH-03
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT ship any user-visible behavior change in agenda or session detail (ordering, bookmark behavior, feedback form visibility, texts)"
---

<objective>
Extract the two most-coupled features last (D-18 order: agenda -> session-detail last): `:feature:agenda` (with the consumer-owned `SessionFiltersService`, D-03) and `:feature:session-detail` (with the OpenFeedback config lifted out of the app's build config via the single `AppModule`, D-02). Close with Android smoke checkpoint 3 (D-18).

Purpose: complete ARCH-03 — every feature in its own module, the app reduced to a thin shell.
Output: two feature modules, thin `:androidApp`, green CI, approved smoke.
</objective>

<execution_context>
@/Users/robin/Development/Android/DevFestNantes/.claude/gsd-core/workflows/execute-plan.md
@/Users/robin/Development/Android/DevFestNantes/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-multi-module-architecture-extraction/03-CONTEXT.md
@.planning/phases/03-multi-module-architecture-extraction/03-05-PLAN.md
@.planning/phases/03-multi-module-architecture-extraction/03-06-PLAN.md
@.planning/phases/03-multi-module-architecture-extraction/03-07-SUMMARY.md
@androidApp/src/main/java/com/gdgnantes/devfest/androidapp/MainActivity.kt
@androidApp/src/main/java/com/gdgnantes/devfest/androidapp/core/injection/AppModule.kt

Use the module map (03-05-PLAN.md) and the "Feature extraction recipe" (03-06-PLAN.md context) verbatim.

## Placement decisions (D-03 discretion)

- `SessionFiltersService` (+ Impl, one file): consumers `AgendaViewModel` (agenda) and `HomeViewModel` + `AppModule` (app) -> `:feature:agenda`, package `com.gdgnantes.devfest.feature.agenda.services` (app -> feature allowed; RESEARCH Open Question 1).
- `SessionComplexity`, `SessionType` chips are consumed only by `SessionDetails` -> stay single-consumer in `:feature:session-detail` (D-08).
- OpenFeedback: `FeedbackFormViewModel` and `FeedbackForm` read two app build-config fields (enabled flag, project id). A `data class OpenFeedbackConfig(val enabled: Boolean, val projectId: String)` in the feature root package is provided by `AppModule` (`@Provides`, computing `enabled` with the same `toBoolean()` conversion used today) and injected into `FeedbackFormViewModel`; `FeedbackForm` obtains the project id through the ViewModel it already uses (or a parameter if it has none). `OpenFeedbackInitializer` stays in the app unchanged.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract :feature:agenda (with SessionFiltersService)</name>
  <files>settings.gradle.dcl, feature/agenda/build.gradle.kts, feature/agenda/src/main/AndroidManifest.xml, feature/agenda/src/main/java/com/gdgnantes/devfest/feature/agenda/*.kt, feature/agenda/src/main/java/com/gdgnantes/devfest/feature/agenda/AgendaRoute.kt, feature/agenda/src/main/java/com/gdgnantes/devfest/feature/agenda/utils/*.kt, feature/agenda/src/main/java/com/gdgnantes/devfest/feature/agenda/services/SessionFiltersService.kt, feature/agenda/src/main/res/**, core/ui/src/main/res/**, androidApp/build.gradle.kts, androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/Home.kt, androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/home/HomeViewModel.kt, androidApp/src/main/java/com/gdgnantes/devfest/androidapp/core/injection/AppModule.kt, androidApp/src/main/res/**</files>
  <read_first>
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/agenda/Agenda.kt, AgendaPager.kt, AgendaRow.kt, AgendaViewModel.kt, SessionFiltersDrawer.kt
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/services/SessionFiltersService.kt (SharedPreferences key constants)
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/utils/PagerTab.kt, SessionFilter.kt, SessionTypeUtils.kt
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/Home.kt (Agenda call site with agendaFilterDrawerState)
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/home/HomeViewModel.kt
  </read_first>
  <action>Apply the feature extraction recipe with `<name>` = `agenda` and the :feature:agenda row of the module map (11 files). Module-specific deps as compilation requires (e.g. `libs.bundles.accompanist` for the pager, `libs.kotlinx.datetime`). Commit 2 package targets: `ui.screens.agenda` -> `com.gdgnantes.devfest.feature.agenda`, `utils` (PagerTab, SessionFilter, SessionTypeUtils) -> `com.gdgnantes.devfest.feature.agenda.utils`, `services.SessionFiltersService` -> `com.gdgnantes.devfest.feature.agenda.services`. Add `AgendaRoute.kt` with public `AgendaRoute(...)` whose parameters are exactly what Home.kt passes to `Agenda(...)` (drawer state + `onSessionClick`). HomeViewModel and AppModule change imports only. Two commits.</action>
  <verify>
    <automated>APP=$(git grep -n -E 'com\.gdgnantes\.devfest\.androidapp|NavController|BuildConfig' -- 'feature/agenda/*.kt') ; test -z "$APP" && ./gradlew --no-daemon :feature:agenda:assembleDebug :androidApp:assembleDebug :androidApp:assembleRelease :androidApp:testDebugUnitTest detekt lint && bash .planning/phases/03-multi-module-architecture-extraction/resources-gate.sh</automated>
    <fails_when>non-zero exit (feature references an app symbol/navigation controller, build/lint failure), or RESOURCES-DRIFT</fails_when>
    <automated>diff <(git show origin/main:androidApp/src/main/java/com/gdgnantes/devfest/androidapp/services/SessionFiltersService.kt | grep -oE '"[^"]*"') <(grep -oE '"[^"]*"' feature/agenda/src/main/java/com/gdgnantes/devfest/feature/agenda/services/SessionFiltersService.kt) && echo "PREFS-KEYS-OK"</automated>
    <fails_when>diff prints any line or output lacks PREFS-KEYS-OK (agenda-filter SharedPreferences key literal changed or file missing at new path)</fails_when>
  </verify>
  <acceptance_criteria>
    - `feature/agenda/build.gradle.kts` contains `id("devfest.android.feature")` and none of `compileSdk`, `minSdk`, `detekt {`, `:feature:`.
    - `git grep -n 'fun AgendaRoute(' -- feature/agenda` returns one match; Home.kt calls `AgendaRoute(`.
    - `git ls-files androidApp/src/main/java | grep -E '/screens/agenda/|/utils/(PagerTab|SessionFilter|SessionTypeUtils)\.kt|/services/SessionFiltersService\.kt'` prints nothing; HomeViewModel.kt still under androidApp.
    - `PREFS-KEYS-OK` and `RESOURCES-OK` printed.
  </acceptance_criteria>
  <done>:feature:agenda builds as a leaf; the Agenda tab (filters drawer, bookmarks) renders through AgendaRoute; two commits.</done>
</task>

<task type="auto">
  <name>Task 2: Extract :feature:session-detail (OpenFeedbackConfig via AppModule), push, CI green, launch sanity</name>
  <files>settings.gradle.dcl, feature/session-detail/build.gradle.kts, feature/session-detail/src/main/AndroidManifest.xml, feature/session-detail/src/main/java/com/gdgnantes/devfest/feature/sessiondetail/*.kt, feature/session-detail/src/main/java/com/gdgnantes/devfest/feature/sessiondetail/SessionDetailRoute.kt, feature/session-detail/src/main/java/com/gdgnantes/devfest/feature/sessiondetail/OpenFeedbackConfig.kt, feature/session-detail/src/main/java/com/gdgnantes/devfest/feature/sessiondetail/components/*.kt, feature/session-detail/src/main/java/com/gdgnantes/devfest/feature/sessiondetail/utils/ScheduleSlotUtils.kt, feature/session-detail/src/main/res/**, core/ui/src/main/res/**, androidApp/build.gradle.kts, androidApp/src/main/java/com/gdgnantes/devfest/androidapp/MainActivity.kt, androidApp/src/main/java/com/gdgnantes/devfest/androidapp/core/injection/AppModule.kt, androidApp/src/main/res/**</files>
  <read_first>
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/session/SessionViewModel.kt (@AssistedFactory SessionViewModelFactory)
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/session/FeedbackForm.kt and FeedbackFormViewModel.kt (build-config fields and remote-config usage)
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/session/SessionLayout.kt, SessionDetails.kt, SessionSpeaker.kt, FallbackFeedbackForm.kt
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/components/SessionComplexity.kt, SessionType.kt; utils/ScheduleSlotUtils.kt
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/core/injection/AppModule.kt (where @Provides functions live)
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/MainActivity.kt (session route composable + EntryPoint)
    - androidApp/build.gradle.kts (openfeedback + firebase config aliases)
  </read_first>
  <action>Apply the feature extraction recipe with `<name>` = `session-detail` (directory `feature/session-detail`, Gradle path `:feature:session-detail`, namespace/package `com.gdgnantes.devfest.feature.sessiondetail`) and the :feature:session-detail row of the module map (10 files). Commit 1 compile-enabling edit: create `OpenFeedbackConfig.kt` (in the feature, same package as the moved session files for this commit) and add an `@Provides` for it to `AppModule` built from the app's two OpenFeedback build-config fields (enabled via the same `toBoolean()` call, project id verbatim); `FeedbackFormViewModel` injects it instead of reading the app build config; `FeedbackForm` takes the project id from that ViewModel (or a parameter). Move module-specific deps: `libs.openfeedback.m3`, `libs.openfeedback.viewmodel`, and Firebase config (with the Firebase BOM platform) if the ViewModel uses remote config — keep them on `:androidApp` too only if app-shell code (e.g. OpenFeedbackInitializer) still imports them. Commit 2 package targets: `ui.screens.session` -> `com.gdgnantes.devfest.feature.sessiondetail`, `ui.components` (SessionComplexity, SessionType) -> `com.gdgnantes.devfest.feature.sessiondetail.components`, `utils.ScheduleSlotUtils` -> `com.gdgnantes.devfest.feature.sessiondetail.utils`. Add `SessionDetailRoute.kt` with public `SessionDetailRoute(viewModel: SessionViewModel, ...)` taking exactly what MainActivity passes to `SessionLayout(...)`; MainActivity keeps `ViewModelFactoryProvider`, `assistedViewModel { SessionViewModel.provideFactory(...) }` and navigation. Two commits, then push, CI gate, launch sanity.</action>
  <verify>
    <automated>APP=$(git grep -n -E 'com\.gdgnantes\.devfest\.androidapp|NavController|BuildConfig' -- 'feature/session-detail/*.kt') ; KT=$(git ls-files androidApp/src/main/java | grep -c '\.kt$') ; test -z "$APP" && test "$KT" -le 19 && ./gradlew --no-daemon :feature:session-detail:assembleDebug :feature:agenda:assembleDebug :androidApp:assembleDebug :androidApp:assembleRelease :androidApp:testDebugUnitTest detekt lint && bash .planning/phases/03-multi-module-architecture-extraction/resources-gate.sh</automated>
    <fails_when>non-zero exit (feature references an app symbol/build config/navigation controller, more than 19 Kotlin files left in the app shell, build/lint failure), or RESOURCES-DRIFT</fails_when>
    <automated>SHA=$(git rev-parse HEAD) || exit 1; BR=$(git branch --show-current) || exit 1; test -n "$SHA" -a -n "$BR" || { echo "NO-GIT-CONTEXT"; exit 1; }; for WF in android.yml ios.yml; do RID=$(gh run list --workflow "$WF" --branch "$BR" --limit 20 --json databaseId,headSha --jq "[.[]|select(.headSha==\"$SHA\")][0].databaseId"); test -n "$RID" || { echo "NO-RUN $WF $SHA"; exit 1; }; gh run watch "$RID" --exit-status || { echo "RED $WF $RID"; exit 1; }; done && echo "CI-GREEN-BOTH $SHA"</automated>
    <fails_when>non-zero exit, or output contains NO-RUN, RED or NO-GIT-CONTEXT instead of CI-GREEN-BOTH</fails_when>
    <automated>android run && android layout | grep -q 'com.gdgnantes.devfest.mobile.androidapp.dev' && android screen capture && echo "COMPOSE-SANITY-RUN-OK"</automated>
    <fails_when>non-zero exit, or output lacks COMPOSE-SANITY-RUN-OK</fails_when>
  </verify>
  <acceptance_criteria>
    - `settings.gradle.dcl` contains `include(":feature:session-detail")`; `feature/session-detail/build.gradle.kts` contains `id("devfest.android.feature")` and none of `compileSdk`, `minSdk`, `detekt {`, `:feature:`.
    - `git grep -n 'fun SessionDetailRoute(' -- feature/session-detail` returns one match; MainActivity calls `SessionDetailRoute(` and still contains `sessionViewModelFactory()`.
    - `AppModule.kt` (still in androidApp) contains `OpenFeedbackConfig`; `git grep -n 'OPEN_FEEDBACK' -- 'feature/*'` prints nothing.
    - `git ls-files androidApp/src/main/java` lists only the :androidApp rows of the module map (18 files, 19 if StringExtensions stayed).
    - For every feature F: `./gradlew :feature:F:dependencies --configuration releaseRuntimeClasspath` contains no `project :feature:`.
    - `RESOURCES-OK`, `CI-GREEN-BOTH`, `COMPOSE-SANITY-RUN-OK` printed.
  </acceptance_criteria>
  <done>All six features are modules; the app is a thin shell; CI green; app launches.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: D-18 Android smoke checkpoint 3 (after the last feature)</name>
  <files>none (verification only)</files>
  <read_first>
    - .planning/phases/03-multi-module-architecture-extraction/03-05-SUMMARY.md (checkpoint 2 walkthrough)
  </read_first>
  <action>Executor presents the screenshot, `android layout` excerpt and CI URLs, then pauses.</action>
  <what-built>All six feature modules extracted; :androidApp is the navigation/DI shell only.</what-built>
  <how-to-verify>
    On the debug app: Agenda — both days, room/time ordering same as before, open the filter drawer and apply a filter (persists after relaunch), toggle a bookmark (persists after relaunch), favorites filter. Session detail — chips, speakers, bookmark FAB, feedback section (fallback form while OpenFeedback is disabled, as today). Speakers — list order as before, speaker detail, tap a speaker's session. Venue — details, floor plan, navigation button. About — links, partners. Settings — version text, Legal, Data collection screen (toggle state preserved). On a fresh install, the data-collection agreement dialog appears exactly when it did before.
  </how-to-verify>
  <verify>
    <automated>bash .planning/phases/03-multi-module-architecture-extraction/resources-gate.sh</automated>
    <fails_when>output lacks RESOURCES-OK</fails_when>
  </verify>
  <acceptance_criteria>
    - Human replies "approved" (confirms the backstop ordering truth and consent/bookmark/filter persistence); otherwise stop before 03-09.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe what differs</resume-signal>
  <done>User approved Android smoke checkpoint 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App BuildConfig (secrets-plugin values) -> feature | OpenFeedback project id/flag cross into a library module |
| Device storage -> SessionFiltersServiceImpl | Persisted filters read after update |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-20 | Information Disclosure | OpenFeedback secrets (project id, Firebase keys) | medium | mitigate | Only `enabled` + `projectId` cross via OpenFeedbackConfig from AppModule; other OPEN_FEEDBACK_* fields stay in the app (acceptance: no OPEN_FEEDBACK reference under feature/) |
| T-03-21 | Tampering | SessionFiltersServiceImpl SharedPreferences keys | medium | mitigate | PREFS-KEYS-OK literal diff vs origin/main |
| T-03-22 | Denial of Service | Assisted ViewModel factories after move | low | mitigate | EntryPoint stays in MainActivity; release build compiles the Hilt graph; smoke opens session and speaker details |
</threat_model>

<verification>
- Leaf builds + app build/test/lint green; `PREFS-KEYS-OK`; `RESOURCES-OK`; `CI-GREEN-BOTH`; `COMPOSE-SANITY-RUN-OK`; human approval.
Edge coverage: ARCH-03 ordering (backstop truth, confirmed at checkpoint), ARCH-03 adjacency, ARCH-02 concurrency.
</verification>

<success_criteria>
- All six ARCH-03 feature modules exist, each with its ViewModel(s) and Compose screens, callbacks-only entry points, no feature->feature edge (ARCH-02, ARCH-03).
</success_criteria>

## Artifacts this phase produces (this plan)

- Gradle modules `:feature:agenda` (packages `com.gdgnantes.devfest.feature.agenda`, `.utils`, `.services`) and `:feature:session-detail` (packages `com.gdgnantes.devfest.feature.sessiondetail`, `.components`, `.utils`)
- Functions: `com.gdgnantes.devfest.feature.agenda.AgendaRoute`, `com.gdgnantes.devfest.feature.sessiondetail.SessionDetailRoute`
- Class: `com.gdgnantes.devfest.feature.sessiondetail.OpenFeedbackConfig`; new `@Provides` for it in `AppModule`
- Files: `feature/agenda/build.gradle.kts`, `feature/session-detail/build.gradle.kts`

<output>
Create `.planning/phases/03-multi-module-architecture-extraction/03-08-SUMMARY.md` when done.
</output>
