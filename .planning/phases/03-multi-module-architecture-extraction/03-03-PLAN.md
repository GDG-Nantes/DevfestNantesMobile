---
phase: 03-multi-module-architecture-extraction
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - settings.gradle.dcl
  - core/data/build.gradle.kts
  - core/data/src/commonMain/kotlin/** (moved store/*, store/graphql/GraphQLStore.kt, Mappers.kt, domain/RoomSortIndex.kt)
  - core/data/src/commonTest/kotlin/** (moved DevFestNantesStoreContractTest.kt)
  - core/data/src/jvmTest/kotlin/** (moved GraphQLStoreJvmTest.kt)
  - core/data/src/androidMain/kotlin/** (moved BookmarksStoreImpl.kt)
  - shared/build.gradle.kts
  - androidApp/build.gradle.kts
  - androidApp/src/main/java/** (import updates only)
autonomous: false
requirements: [ARCH-02, ARCH-04]

estimate:
  tokens: 130000
  raw_tokens: 130000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - ":core:data exists (D-14) with DevFestNantesStore, DevFestNantesStoreBuilder, BookmarksStore, DevFestNantesStoreMocked, GraphQLStore, Mappers and RoomSortIndex in commonMain and BookmarksStoreImpl in androidMain (D-03, D-06), all in package com.gdgnantes.devfest.core.data (+ .graphql / .domain) after the repackage commit (D-15)"
    - ":core:data applies the KMP-NativeCoroutines plugin + KSP so @NativeCoroutines on DevFestNantesStore/BookmarksStore keeps generating the Swift-facing async wrappers, and :shared declares api(project(\":core:data\")) + export(project(\":core:data\")) for all three iOS targets (D-11)"
    - ":core:data depends on :core:network with implementation (never exported) and on :core:model with api; it depends on no :feature:* module (ARCH-02)"
    - "GraphQLStoreJvmTest runs via :core:data:jvmTest and DevFestNantesStoreContractTest via :core:data commonTest (jvmTest) — neither test goes dark (tests follow code)"
    - "DevFestNantesStoreMocked and model stubs stay in production source sets because production DevFestNantesStoreBuilder (Kotlin, and Swift BaseViewModel via it) and Compose @Preview functions use them (D-19 consumer verification recorded)"
    - "The SharedPreferences key literal selected_sessions in BookmarksStoreImpl is byte-identical to origin/main"
    - "Every Kotlin type name referenced from iosApp Swift sources is still present unprefixed in shared.h and the collision count equals baseline (ARCH-04 adjacency edge); both CI workflows green on pushed HEAD; each module commit builds green (ARCH-02 concurrency edge, D-16, D-18)"
    - "iOS simulator smoke (D-12 checkpoint 2) and Android smoke (D-18 checkpoint 1: all tabs, session detail, speaker detail, settings) are approved by the user"
    - "The Hilt AppModule stays in :androidApp and still provides DevFestNantesStore via DevFestNantesStoreBuilder and binds BookmarksStoreImpl (D-02); no DI framework other than Hilt appears (D-01)"
  artifacts:
    - path: core/data/build.gradle.kts
      provides: "devfest.kmp.library + ksp + kmp.native.coroutines leaf"
    - path: core/data/src/commonMain/kotlin/com/gdgnantes/devfest/core/data/DevFestNantesStore.kt
      provides: "store contract exported to Swift"
    - path: core/data/src/androidMain/kotlin/com/gdgnantes/devfest/core/data/BookmarksStoreImpl.kt
      provides: "SharedPreferences-backed BookmarksStore"
    - path: core/data/src/jvmTest/kotlin/com/gdgnantes/devfest/core/data/graphql/GraphQLStoreJvmTest.kt
      provides: "GraphQL store parity test"
  key_links:
    - from: shared/build.gradle.kts
      to: core/data
      via: "api(project(\":core:data\")) + export(project(\":core:data\"))"
      pattern: "export\\(project\\(\":core:data\"\\)\\)"
    - from: core/data/build.gradle.kts
      to: core/network
      via: "implementation(project(\":core:network\"))"
      pattern: "implementation\\(project\\(\":core:network\"\\)\\)"
    - from: androidApp/src/main/java/com/gdgnantes/devfest/androidapp/core/injection/AppModule.kt
      to: core/data
      via: "DevFestNantesStoreBuilder().setUseMockServer(false).build() + @Binds BookmarksStoreImpl"
      pattern: "DevFestNantesStoreBuilder"
  prohibitions:
    - requirement_id: ARCH-02
      category: privacy
      status: resolved
      verification: test
      resolution: null
      reason: null
      statement: "MUST NOT change any persisted SharedPreferences key or file name used by BookmarksStoreImpl — existing users' bookmarks must survive the update unchanged"
    - requirement_id: ARCH-04
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT ship any user-visible behavior change on Android or iOS while moving code (no drive-by fixes; StoreStubs' unseeded RNG stays as-is per D-19 — that is TEST-01, Phase 5)"
---

<objective>
Extract `:core:data` — the store layer both platforms read through (D-18 order: after network/analytics). Moves the store contract, GraphQL implementation, mappers, mock store and `BookmarksStoreImpl` (androidMain, D-03/D-06), exports the module to Swift (D-11), and lands the two mid-phase smoke checkpoints: iOS simulator (D-12 #2) and Android (D-18 #1).

Purpose: after this plan, `:shared` holds no Kotlin sources; the riskiest remaining export (KMP-NativeCoroutines wrappers on DevFestNantesStore/BookmarksStore) is proven on device.
Output: `:core:data`, updated umbrella + app wiring, green CI, two approved smoke runs.
</objective>

<execution_context>
@/Users/robin/Development/Android/DevFestNantes/.claude/gsd-core/workflows/execute-plan.md
@/Users/robin/Development/Android/DevFestNantes/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-multi-module-architecture-extraction/03-CONTEXT.md
@.planning/phases/03-multi-module-architecture-extraction/03-RESEARCH.md
@.planning/phases/03-multi-module-architecture-extraction/03-PATTERNS.md
@.planning/phases/03-multi-module-architecture-extraction/03-02-SUMMARY.md
@shared/build.gradle.kts

## Decisions (planner discretion)

- `jvm()` + `jvmTest` ownership (RESEARCH A4 / Pitfall 6): `GraphQLStoreJvmTest` moves to `core/data/src/jvmTest` (its subject `GraphQLStore` lives in core:data); `jvm()` is already declared uniformly by `devfest.kmp.library` (03-01), so no per-module target edits.
- `RoomSortIndex.kt` (extension on the Apollo-generated `RoomDetails` fragment, consumed only by `Mappers.kt`) -> `:core:data` package `com.gdgnantes.devfest.core.data.domain`.
- D-19 verification result: `DevFestNantesStoreMocked` is instantiated by production `DevFestNantesStoreBuilder` (called by `AppModule` and Swift `BaseViewModel`), so it stays in `:core:data` commonMain; it is therefore not moved to `:core:testing`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract :core:data — pure move, then repackage; export to Swift</name>
  <files>settings.gradle.dcl, core/data/build.gradle.kts, core/data/src/commonMain/kotlin/com/gdgnantes/devfest/core/data/BookmarksStore.kt, core/data/src/commonMain/kotlin/com/gdgnantes/devfest/core/data/DevFestNantesStore.kt, core/data/src/commonMain/kotlin/com/gdgnantes/devfest/core/data/DevFestNantesStoreBuilder.kt, core/data/src/commonMain/kotlin/com/gdgnantes/devfest/core/data/DevFestNantesStoreMocked.kt, core/data/src/commonMain/kotlin/com/gdgnantes/devfest/core/data/graphql/GraphQLStore.kt, core/data/src/commonMain/kotlin/com/gdgnantes/devfest/core/data/graphql/Mappers.kt, core/data/src/commonMain/kotlin/com/gdgnantes/devfest/core/data/domain/RoomSortIndex.kt, core/data/src/commonTest/kotlin/com/gdgnantes/devfest/core/data/DevFestNantesStoreContractTest.kt, core/data/src/jvmTest/kotlin/com/gdgnantes/devfest/core/data/graphql/GraphQLStoreJvmTest.kt, core/data/src/androidMain/kotlin/com/gdgnantes/devfest/core/data/BookmarksStoreImpl.kt, shared/build.gradle.kts, androidApp/build.gradle.kts, androidApp/src/main/java/** (import updates only)</files>
  <read_first>
    - shared/build.gradle.kts (plugins ksp + kmp.native.coroutines, commonMain/commonTest deps)
    - shared/src/commonMain/kotlin/com/gdgnantes/devfest/store/DevFestNantesStore.kt and BookmarksStore.kt (@NativeCoroutines annotations)
    - shared/src/commonMain/kotlin/com/gdgnantes/devfest/store/DevFestNantesStoreBuilder.kt
    - shared/src/commonMain/kotlin/com/gdgnantes/devfest/store/graphql/GraphQLStore.kt, Mappers.kt
    - shared/src/jvmTest/kotlin/com/gdgnantes/devfest/store/graphql/GraphQLStoreJvmTest.kt (test-only dependencies it needs)
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/services/BookmarksStoreImpl.kt (PREF_SELECTED_SESSIONS literal, androidx.core edit import)
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/core/injection/AppModule.kt (store provider + BookmarksStore binding)
    - core/analytics/build.gradle.kts (androidMain dependency shape from 03-02)
  </read_first>
  <action>
Commit 1 (pure move, packages unchanged, D-16): add `include(":core:data")` to `settings.gradle.dcl`. `git mv`: `shared/src/commonMain/kotlin/com/gdgnantes/devfest/store/` (BookmarksStore, DevFestNantesStore, DevFestNantesStoreBuilder, DevFestNantesStoreMocked, graphql/GraphQLStore, graphql/Mappers) and `.../devfest/domain/RoomSortIndex.kt` to the same relative paths under `core/data/src/commonMain/kotlin/`; `shared/src/commonTest/kotlin/com/gdgnantes/devfest/store/DevFestNantesStoreContractTest.kt` -> `core/data/src/commonTest/kotlin/com/gdgnantes/devfest/store/`; `shared/src/jvmTest/kotlin/com/gdgnantes/devfest/store/graphql/GraphQLStoreJvmTest.kt` -> `core/data/src/jvmTest/kotlin/com/gdgnantes/devfest/store/graphql/`; `androidApp/.../services/BookmarksStoreImpl.kt` -> `core/data/src/androidMain/kotlin/com/gdgnantes/devfest/androidapp/services/` (D-03, D-06). Create `core/data/build.gradle.kts`: `plugins { id("devfest.kmp.library"); alias(libs.plugins.ksp); alias(libs.plugins.kmp.native.coroutines) }`; `commonMain.dependencies { api(project(":core:model")); implementation(project(":core:network")); api(libs.kotlinx.coroutines) }` plus what compilation needs (kotlinx-datetime etc.); `jvmTest.dependencies` = whatever GraphQLStoreJvmTest imports today (read it); `androidMain.dependencies { implementation(libs.dagger.hilt.android) }` + the androidx artifact providing `androidx.core.content.edit` (existing alias, or a new alias pinned to the version `:androidApp:releaseRuntimeClasspath` already resolves). Keep `ksp` + `kmp.native.coroutines` applied on `:shared` too (removed only in 03-09 if unused). In `shared/build.gradle.kts` add `api(project(":core:data"))` and `export(project(":core:data"))` inside the framework lambda (D-11); `shared/src` now contains no `.kt` files — confirm with `git ls-files shared/src | grep '\.kt$'` (expect empty). In `androidApp/build.gradle.kts` add `implementation(project(":core:data"))`. `AppModule` stays in the app (D-02). Verify, commit.
Commit 2 (repackage, D-15): `git mv` into package directories and rename: `com.gdgnantes.devfest.store` -> `com.gdgnantes.devfest.core.data`; `com.gdgnantes.devfest.store.graphql` -> `com.gdgnantes.devfest.core.data.graphql`; `com.gdgnantes.devfest.domain` -> `com.gdgnantes.devfest.core.data.domain`; `BookmarksStoreImpl` (`com.gdgnantes.devfest.androidapp.services`) -> `com.gdgnantes.devfest.core.data`; tests follow their subjects' packages. Update imports everywhere (`AppModule`, ViewModels, `BookmarksViewModel`, `HomeViewModel`, etc.). Only package/import lines change; the `PREF_SELECTED_SESSIONS` literal and every other string literal stay byte-identical (RESEARCH Pitfall 4). Verify, commit.
  </action>
  <verify>
    <automated>OLD=$(git grep -n -E 'com\.gdgnantes\.devfest\.(store|domain)([.;[:space:]]|$)|androidapp\.services\.BookmarksStoreImpl' -- '*.kt') ; test -z "$OLD" && ./gradlew --no-daemon :core:data:jvmTest :core:data:compileKotlinIosSimulatorArm64 :core:model:jvmTest :androidApp:assembleDebug :androidApp:assembleRelease detekt lint && bash .planning/phases/03-multi-module-architecture-extraction/swift-names-gate.sh check</automated>
    <fails_when>non-zero exit (old store/domain package still referenced, GraphQLStoreJvmTest or DevFestNantesStoreContractTest failing, Hilt graph not compiling), or last line not SWIFT-NAMES-OK</fails_when>
    <automated>diff <(git show origin/main:androidApp/src/main/java/com/gdgnantes/devfest/androidapp/services/BookmarksStoreImpl.kt | grep -oE '"[^"]*"') <(grep -oE '"[^"]*"' core/data/src/androidMain/kotlin/com/gdgnantes/devfest/core/data/BookmarksStoreImpl.kt) && echo "PREFS-KEYS-OK"</automated>
    <fails_when>diff prints any line or output lacks PREFS-KEYS-OK (a string literal — including the selected_sessions key — changed, or the file is missing at the new path)</fails_when>
  </verify>
  <acceptance_criteria>
    - `git ls-files shared/src | grep -c '\.kt$'` prints 0.
    - `core/data/build.gradle.kts` contains `id("devfest.kmp.library")`, `implementation(project(":core:network"))`, `api(project(":core:model"))`, the kmp native coroutines plugin alias, and none of `compileSdk`, `minSdk`, `detekt {`.
    - `shared/build.gradle.kts` contains `export(project(":core:data"))`, `export(project(":core:analytics"))`, `export(project(":core:model"))` and exactly one `core:network` reference.
    - `./gradlew :core:data:jvmTest` report lists `GraphQLStoreJvmTest` and `DevFestNantesStoreContractTest` executed with 0 failures (check `core/data/build/test-results/jvmTest/*.xml`).
    - `PREFS-KEYS-OK` printed.
    - Repackage commit Kotlin diff contains only `package`/`import` lines.
    - `swift-names-gate.sh check` prints `SWIFT-NAMES-OK` (DevFestNantesStore, DevFestNantesStoreBuilder unprefixed).
  </acceptance_criteria>
  <done>:core:data owns the store layer on both platforms, exported under unchanged Swift names; all moved tests run green from their new home; two commits.</done>
</task>

<task type="auto">
  <name>Task 2: Push, both CI workflows green, automated Android launch sanity</name>
  <files>none (push + verification only)</files>
  <read_first>
    - .planning/phases/02-dependency-build-tooling-upgrade/02-03-PLAN.md (Phase 2 local smoke command shape)
    - AGENTS.md (android CLI usage)
  </read_first>
  <action>Push the branch (PR from 03-01 is open) and run the CI gate. Then run `android info` to confirm a device/emulator (start one with `android emulator start` if none), and run the local launch sanity command below, keeping the `android screen capture` output path for the checkpoint. Do not use raw adb for install/launch (AGENTS.md).</action>
  <verify>
    <automated>SHA=$(git rev-parse HEAD) || exit 1; BR=$(git branch --show-current) || exit 1; test -n "$SHA" -a -n "$BR" || { echo "NO-GIT-CONTEXT"; exit 1; }; for WF in android.yml ios.yml; do RID=$(gh run list --workflow "$WF" --branch "$BR" --limit 20 --json databaseId,headSha --jq "[.[]|select(.headSha==\"$SHA\")][0].databaseId"); test -n "$RID" || { echo "NO-RUN $WF $SHA"; exit 1; }; gh run watch "$RID" --exit-status || { echo "RED $WF $RID"; exit 1; }; done && echo "CI-GREEN-BOTH $SHA"</automated>
    <fails_when>non-zero exit, or output contains NO-RUN, RED or NO-GIT-CONTEXT instead of CI-GREEN-BOTH</fails_when>
    <automated>android run && android layout | grep -q 'com.gdgnantes.devfest.mobile.androidapp.dev' && android screen capture && echo "COMPOSE-SANITY-RUN-OK"</automated>
    <fails_when>non-zero exit, or output lacks COMPOSE-SANITY-RUN-OK (app not installed/launched or not in foreground)</fails_when>
  </verify>
  <acceptance_criteria>
    - `CI-GREEN-BOTH <sha>` printed for the pushed HEAD.
    - `COMPOSE-SANITY-RUN-OK` printed; the screenshot path is recorded for Task 3.
  </acceptance_criteria>
  <done>Both workflows green on HEAD; debug app launches on a device/emulator.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: D-12 checkpoint 2 (iOS simulator) + D-18 checkpoint 1 (Android) smoke</name>
  <files>none (verification only)</files>
  <read_first>
    - .planning/phases/03-multi-module-architecture-extraction/03-01-SUMMARY.md (iOS smoke steps used at checkpoint 1)
  </read_first>
  <action>Executor presents: CI run URLs, the swift-names-gate informational diff (classify each changed name), the Android screenshot from Task 2. Then pause for the human.</action>
  <what-built>:core:data extracted and exported; :shared now contains no Kotlin sources and only re-exports core modules.</what-built>
  <how-to-verify>
    iOS (D-12 #2): run `iosApp` from Xcode on an iPhone simulator — Agenda (both days, sessions load), Speakers (list + one detail), Venue, About all render with data; bookmark toggle on iOS behaves as before if present.
    Android (D-18 #1): on the launched debug app — Agenda tab (sessions load; toggle a bookmark, relaunch the app, bookmark is still set), Speakers tab, Venue tab, About tab, open a session detail, open a speaker detail, open Settings.
  </how-to-verify>
  <verify>
    <automated>bash .planning/phases/03-multi-module-architecture-extraction/swift-names-gate.sh check</automated>
    <fails_when>last line is not SWIFT-NAMES-OK</fails_when>
  </verify>
  <acceptance_criteria>
    - Human replies "approved" after both platform walkthroughs; any failure stops the phase for diagnosis before 03-04.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe what failed</resume-signal>
  <done>User approved both smoke runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Device storage (SharedPreferences) -> BookmarksStoreImpl | Persisted user state read after app update |
| Kotlin umbrella framework -> Swift | Exported store API incl. native-coroutines wrappers |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-07 | Tampering | BookmarksStoreImpl SharedPreferences key | medium | mitigate | PREFS-KEYS-OK diff of all string literals vs origin/main; repackage commit restricted to package/import lines |
| T-03-08 | Information Disclosure | :core:network reachable from exported :core:data | medium | mitigate | core:data -> core:network is implementation; :shared has exactly one core:network reference and no export of it (D-11) |
| T-03-09 | Denial of Service | Swift async wrappers (KMP-NativeCoroutines) after module move | medium | mitigate | Plugin applied on :core:data; iOS CI xcodebuild + D-12 simulator smoke prove the wrappers still compile and run |
</threat_model>

<verification>
- Local: `./gradlew --no-daemon :core:data:jvmTest :core:model:jvmTest :androidApp:assembleDebug :androidApp:assembleRelease detekt lint` green; `SWIFT-NAMES-OK`; `PREFS-KEYS-OK`.
- CI: `CI-GREEN-BOTH`.
- Human: iOS + Android smoke approved.
Edge coverage: ARCH-02 concurrency, ARCH-04 adjacency (explicit truths).
</verification>

<success_criteria>
- :core:data exists, exported, KMP graph complete (model, network, analytics, data) with no feature dependency (ARCH-02, ARCH-04).
- Bookmarks persistence key unchanged; both smoke runs approved.
</success_criteria>

## Artifacts this phase produces (this plan)

- Gradle module `:core:data` (packages `com.gdgnantes.devfest.core.data`, `.graphql`, `.domain`)
- File: `core/data/build.gradle.kts`
- Moved symbols (new FQNs): `com.gdgnantes.devfest.core.data.DevFestNantesStore`, `.DevFestNantesStoreBuilder`, `.DevFestNantesStoreMocked`, `.BookmarksStore`, `.BookmarksStoreImpl`, `com.gdgnantes.devfest.core.data.graphql.GraphQLStore`, `com.gdgnantes.devfest.core.data.domain.sortIndex`

<output>
Create `.planning/phases/03-multi-module-architecture-extraction/03-03-SUMMARY.md` when done (include test report counts, swift-names diff classification, CI URLs, smoke results).
</output>
