No external API integration: structural refactor of the existing GraphQL backend usage (modules, iOS umbrella export, Apollo class renames); no new API, SDK or endpoint.

The detector's hit is the phrase "Public Kotlin API surface exported to the iOS binary" in the plans' threat models, not an external integration.
