---
phase: 03-multi-module-architecture-extraction
plan: 07
type: execute
wave: 8
depends_on: ["03-06"]
files_modified:
  - settings.gradle.dcl
  - feature/settings/build.gradle.kts
  - feature/settings/src/main/AndroidManifest.xml
  - feature/settings/src/main/java/** (moved settings/*, datacollection/*, legal/LegalScreen.kt, services/DataCollectionSettingsService.kt; new SettingsRoute.kt)
  - feature/settings/src/main/res/**
  - feature/speakers/build.gradle.kts
  - feature/speakers/src/main/AndroidManifest.xml
  - feature/speakers/src/main/java/** (moved speakers list/details + ViewModels; new SpeakersRoute.kt)
  - feature/speakers/src/main/res/**
  - core/ui/src/main/res/**
  - androidApp/build.gradle.kts
  - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/MainActivity.kt
  - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/Home.kt
  - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/Screen.kt
  - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/core/DataSharingInitializer.kt
  - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/core/injection/AppModule.kt
  - androidApp/src/main/res/**
autonomous: true
requirements: [ARCH-02, ARCH-03]

estimate:
  tokens: 140000
  raw_tokens: 140000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - ":feature:settings exists (D-05) with Settings (+ SettingsItem/Tile*), DataCollection (DataCollectionSettingsScreen, DataCollectionAgreementDialog, DataCollectionViewModel), LegalScreen and DataCollectionSettingsService/Impl (D-03 consumer-owned placement), package com.gdgnantes.devfest.feature.settings"
    - ":feature:speakers exists (D-05) with SpeakersViewModel, SpeakerViewModel (+ its assisted factory), Speakers list and SpeakerLayout/SpeakerDetails/SpeakerSession, package com.gdgnantes.devfest.feature.speakers"
    - "Feature entry points take only callbacks/values (D-07): SettingsRoute, DataCollectionSettingsRoute, LegalRoute, SpeakersRoute, SpeakerDetailRoute; :androidApp's MainActivity/Home keep every NavHost, Screen route string and the assisted ViewModel factory EntryPoint; route/argument formats unchanged"
    - "No feature module references any com.gdgnantes.devfest.androidapp symbol (Screen, BuildConfig, app R) and no Navigation Compose controller type appears in feature code (D-07)"
    - "The Hilt AppModule stays in :androidApp and still binds DataCollectionSettingsServiceImpl (D-02); DataSharingInitializer stays in :androidApp with unchanged logic"
    - "DataCollectionSettingsServiceImpl's SharedPreferences key/file literals are byte-identical to origin/main"
    - "Resources follow the ownership rule (D-17) and resources-gate.sh prints RESOURCES-OK (ARCH-03 adjacency edge)"
    - "Each feature lands as two green commits; no feature->feature or core->feature edge; both CI workflows green on pushed HEAD (ARCH-02, D-16, D-18)"
  artifacts:
    - path: feature/settings/build.gradle.kts
      provides: "devfest.android.feature leaf"
    - path: feature/settings/src/main/java/com/gdgnantes/devfest/feature/settings/SettingsRoute.kt
      provides: "SettingsRoute, DataCollectionSettingsRoute, LegalRoute"
    - path: feature/speakers/build.gradle.kts
      provides: "devfest.android.feature leaf"
    - path: feature/speakers/src/main/java/com/gdgnantes/devfest/feature/speakers/SpeakersRoute.kt
      provides: "SpeakersRoute, SpeakerDetailRoute"
  key_links:
    - from: androidApp/src/main/java/com/gdgnantes/devfest/androidapp/MainActivity.kt
      to: feature/speakers SpeakerViewModel.SpeakerViewModelFactory
      via: "ViewModelFactoryProvider EntryPoint + assistedViewModel { SpeakerViewModel.provideFactory(...) } then SpeakerDetailRoute(viewModel = ...)"
      pattern: "SpeakerDetailRoute\\("
    - from: androidApp/src/main/java/com/gdgnantes/devfest/androidapp/core/DataSharingInitializer.kt
      to: feature/settings DataCollectionSettingsService
      via: "import com.gdgnantes.devfest.feature.settings.services.DataCollectionSettingsService"
      pattern: "feature\\.settings\\.services"
  prohibitions:
    - requirement_id: ARCH-03
      category: privacy
      status: resolved
      verification: test
      resolution: null
      reason: null
      statement: "MUST NOT change any persisted SharedPreferences key or file name used by DataCollectionSettingsServiceImpl — existing users' data-collection consent choices must survive the update unchanged"
    - requirement_id: ARCH-03
      category: privacy
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT alter data-collection consent gating while moving the consent screen/dialog and service — Firebase Analytics/Crashlytics/Performance collection stays exactly as gated today (agreement dialog shown when it is today, DataSharingInitializer logic untouched)"
---

<objective>
Extract the next two features in D-18 order (settings -> speakers): `:feature:settings` (Settings + DataCollection + Legal, D-05, with the consumer-owned `DataCollectionSettingsService`, D-03) and `:feature:speakers` (list + detail). Cross-feature navigation (speaker -> session) stays wired by callbacks in `:androidApp` (D-07).

Purpose: move the two features that touch the root NavHost and the assisted-injection EntryPoint, proving D-07's "app owns navigation" boundary for detail screens.
Output: two feature modules, Route entry points, green CI.
</objective>

<execution_context>
@/Users/robin/Development/Android/DevFestNantes/.claude/gsd-core/workflows/execute-plan.md
@/Users/robin/Development/Android/DevFestNantes/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-multi-module-architecture-extraction/03-CONTEXT.md
@.planning/phases/03-multi-module-architecture-extraction/03-05-PLAN.md
@.planning/phases/03-multi-module-architecture-extraction/03-06-PLAN.md
@.planning/phases/03-multi-module-architecture-extraction/03-06-SUMMARY.md
@androidApp/src/main/java/com/gdgnantes/devfest/androidapp/MainActivity.kt

Use the module map (03-05-PLAN.md) and the "Feature extraction recipe" (03-06-PLAN.md context) verbatim.

## Placement decisions (D-03 discretion)

- `DataCollectionSettingsService` (+ `DataCollectionService` enum + `DataCollectionSettingsServiceImpl`, one file): consumers are `DataCollectionViewModel`/`DataCollectionSettingsScreen` (settings) and `DataSharingInitializer` + `AppModule` (app). Lowest module both can reach = `:feature:settings` (app -> feature is allowed). Package `com.gdgnantes.devfest.feature.settings.services`.
- `ExternalContentService` stays in `:androidApp` (Activity-scoped, consumed only by MainActivity/Home — RESEARCH Pattern 1).
- `Screen.Settings/DataCollection/Legal.title` references inside feature composables are replaced by the string resource ids they resolve to; `Screen.kt` keeps referencing the same ids (so those strings become app + feature consumed -> `:core:ui` per the ownership rule).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract :feature:settings (Settings + DataCollection + Legal + consent service)</name>
  <files>settings.gradle.dcl, feature/settings/build.gradle.kts, feature/settings/src/main/AndroidManifest.xml, feature/settings/src/main/java/com/gdgnantes/devfest/feature/settings/*.kt, feature/settings/src/main/java/com/gdgnantes/devfest/feature/settings/SettingsRoute.kt, feature/settings/src/main/java/com/gdgnantes/devfest/feature/settings/datacollection/*.kt, feature/settings/src/main/java/com/gdgnantes/devfest/feature/settings/legal/LegalScreen.kt, feature/settings/src/main/java/com/gdgnantes/devfest/feature/settings/services/DataCollectionSettingsService.kt, feature/settings/src/main/res/**, core/ui/src/main/res/**, androidApp/build.gradle.kts, androidApp/src/main/java/com/gdgnantes/devfest/androidapp/MainActivity.kt, androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/Screen.kt, androidApp/src/main/java/com/gdgnantes/devfest/androidapp/core/DataSharingInitializer.kt, androidApp/src/main/java/com/gdgnantes/devfest/androidapp/core/injection/AppModule.kt, androidApp/src/main/res/**</files>
  <read_first>
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/settings/Settings.kt (Screen.Settings.title + BuildConfig.VERSION_NAME/VERSION_CODE)
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/datacollection/DataCollectionSettingsScreen.kt, DataCollectionAgreementDialog.kt, DataCollectionViewModel.kt
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/legal/LegalScreen.kt
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/services/DataCollectionSettingsService.kt (SharedPreferences key constants)
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/core/DataSharingInitializer.kt
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/Screen.kt
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/MainActivity.kt (Settings/DataCollection/Legal composable() call sites and the agreement dialog)
  </read_first>
  <action>Apply the feature extraction recipe with `<name>` = `settings` and the :feature:settings row of the module map (11 files). Commit 1 compile-enabling edits: (b) Settings' version display takes `versionName: String` and `versionCode: Int` parameters supplied by MainActivity from the app's build-config values (same displayed text); (c) each `Screen.<X>.title` reference in the three screens is replaced by the string resource id it resolves to in `Screen.kt`, and those strings move to `:core:ui` (app + feature consumers) with Screen.kt importing `com.gdgnantes.devfest.core.ui.R` (alias if needed). Commit 2 package targets: `ui.screens.settings` -> `com.gdgnantes.devfest.feature.settings`, `ui.screens.datacollection` -> `com.gdgnantes.devfest.feature.settings.datacollection`, `ui.screens.legal` -> `com.gdgnantes.devfest.feature.settings.legal`, `services.DataCollectionSettingsService` -> `com.gdgnantes.devfest.feature.settings.services`. Add `SettingsRoute.kt` with public `SettingsRoute(...)`, `DataCollectionSettingsRoute(...)`, `LegalRoute(...)` whose parameters are exactly what MainActivity passes today (plus the version values); `DataCollectionAgreementDialog` stays a public composable called by MainActivity. Update MainActivity, DataSharingInitializer and AppModule imports only. Consent logic (DataSharingInitializer, dialog trigger conditions in MainActivity) is not modified. Two commits.</action>
  <verify>
    <automated>APP=$(git grep -n -E 'com\.gdgnantes\.devfest\.androidapp|NavController|BuildConfig|(^|[^A-Za-z])Screen\.' -- 'feature/settings/*.kt') ; test -z "$APP" && ./gradlew --no-daemon :feature:settings:assembleDebug :androidApp:assembleDebug :androidApp:assembleRelease :androidApp:testDebugUnitTest detekt lint && bash .planning/phases/03-multi-module-architecture-extraction/resources-gate.sh</automated>
    <fails_when>non-zero exit (feature still references an app symbol/Screen/build config/navigation controller, or build/lint fails), or RESOURCES-DRIFT</fails_when>
    <automated>diff <(git show origin/main:androidApp/src/main/java/com/gdgnantes/devfest/androidapp/services/DataCollectionSettingsService.kt | grep -oE '"[^"]*"') <(grep -oE '"[^"]*"' feature/settings/src/main/java/com/gdgnantes/devfest/feature/settings/services/DataCollectionSettingsService.kt) && echo "PREFS-KEYS-OK"</automated>
    <fails_when>diff prints any line or output lacks PREFS-KEYS-OK (a string literal such as the consent SharedPreferences key changed, or the file is missing at the new path)</fails_when>
  </verify>
  <acceptance_criteria>
    - `feature/settings/build.gradle.kts` contains `id("devfest.android.feature")` and none of `compileSdk`, `minSdk`, `detekt {`, `:feature:`.
    - `git grep -n -E 'fun (SettingsRoute|DataCollectionSettingsRoute|LegalRoute)\(' -- feature/settings` returns 3 matches; MainActivity calls all three.
    - `git ls-files androidApp/src/main/java | grep -E '/screens/(settings|datacollection|legal)/|/services/DataCollectionSettingsService\.kt'` prints nothing; `DataSharingInitializer.kt` and `AppModule.kt` are still under androidApp.
    - `git diff origin/main -- androidApp/src/main/java/com/gdgnantes/devfest/androidapp/core/DataSharingInitializer.kt | grep -E '^[-+]' | grep -vE '^(\+\+\+|---)' | grep -vE '^[-+]import '` prints nothing (consent initializer logic untouched).
    - `PREFS-KEYS-OK` and `RESOURCES-OK` printed.
  </acceptance_criteria>
  <done>:feature:settings builds as a leaf; settings, data-collection consent and legal screens are reached from MainActivity through Route entry points; consent storage and gating unchanged; two commits.</done>
</task>

<task type="auto">
  <name>Task 2: Extract :feature:speakers (list + detail; assisted factory stays wired in app), push, CI green</name>
  <files>settings.gradle.dcl, feature/speakers/build.gradle.kts, feature/speakers/src/main/AndroidManifest.xml, feature/speakers/src/main/java/com/gdgnantes/devfest/feature/speakers/SpeakerViewModel.kt, feature/speakers/src/main/java/com/gdgnantes/devfest/feature/speakers/SpeakersRoute.kt, feature/speakers/src/main/java/com/gdgnantes/devfest/feature/speakers/details/*.kt, feature/speakers/src/main/java/com/gdgnantes/devfest/feature/speakers/list/*.kt, feature/speakers/src/main/res/**, core/ui/src/main/res/**, androidApp/build.gradle.kts, androidApp/src/main/java/com/gdgnantes/devfest/androidapp/MainActivity.kt, androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/Home.kt, androidApp/src/main/res/**</files>
  <read_first>
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/speakers/SpeakerViewModel.kt (@AssistedFactory SpeakerViewModelFactory, provideFactory)
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/speakers/details/SpeakerLayout.kt, SpeakerDetails.kt, SpeakerSession.kt
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/speakers/list/Speakers.kt, SpeakersViewModel.kt
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/MainActivity.kt (ViewModelFactoryProvider EntryPoint, speaker route composable)
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/utils/AssistedViewModelUtils.kt
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/Home.kt (Speakers tab call site)
  </read_first>
  <action>Apply the feature extraction recipe with `<name>` = `speakers` and the :feature:speakers row of the module map (6 files). Commit 2 package targets: `ui.screens.speakers` (SpeakerViewModel) -> `com.gdgnantes.devfest.feature.speakers`, `ui.screens.speakers.details` -> `com.gdgnantes.devfest.feature.speakers.details`, `ui.screens.speakers.list` -> `com.gdgnantes.devfest.feature.speakers.list`. Add `SpeakersRoute.kt` with public `SpeakersRoute(onSpeakerClick)` (parameters exactly what Home.kt passes to `Speakers(...)`) and `SpeakerDetailRoute(viewModel: SpeakerViewModel, ...)` (parameters exactly what MainActivity passes to `SpeakerLayout(...)`, including the session-click and social-link callbacks). MainActivity keeps `ViewModelFactoryProvider`, `assistedViewModel { SpeakerViewModel.provideFactory(speakerViewModelFactory(), speakerId) }` and all navigation; it only switches to `SpeakerDetailRoute` and updates imports. Speaker -> session navigation remains a callback implemented in MainActivity (no dependency on session-detail). Two commits, then push and the CI gate.</action>
  <verify>
    <automated>APP=$(git grep -n -E 'com\.gdgnantes\.devfest\.androidapp|NavController|BuildConfig' -- 'feature/speakers/*.kt') ; test -z "$APP" && ./gradlew --no-daemon :feature:speakers:assembleDebug :feature:settings:assembleDebug :androidApp:assembleDebug :androidApp:assembleRelease :androidApp:testDebugUnitTest detekt lint && bash .planning/phases/03-multi-module-architecture-extraction/resources-gate.sh</automated>
    <fails_when>non-zero exit (feature references an app symbol or navigation controller, Hilt assisted-injection graph fails to compile), or RESOURCES-DRIFT</fails_when>
    <automated>SHA=$(git rev-parse HEAD) || exit 1; BR=$(git branch --show-current) || exit 1; test -n "$SHA" -a -n "$BR" || { echo "NO-GIT-CONTEXT"; exit 1; }; for WF in android.yml ios.yml; do RID=$(gh run list --workflow "$WF" --branch "$BR" --limit 20 --json databaseId,headSha --jq "[.[]|select(.headSha==\"$SHA\")][0].databaseId"); test -n "$RID" || { echo "NO-RUN $WF $SHA"; exit 1; }; gh run watch "$RID" --exit-status || { echo "RED $WF $RID"; exit 1; }; done && echo "CI-GREEN-BOTH $SHA"</automated>
    <fails_when>non-zero exit, or output contains NO-RUN, RED or NO-GIT-CONTEXT instead of CI-GREEN-BOTH</fails_when>
  </verify>
  <acceptance_criteria>
    - `feature/speakers/build.gradle.kts` contains `id("devfest.android.feature")` and none of `compileSdk`, `minSdk`, `detekt {`, `:feature:`.
    - `git grep -n -E 'fun (SpeakersRoute|SpeakerDetailRoute)\(' -- feature/speakers` returns 2 matches; Home.kt calls `SpeakersRoute(` and MainActivity calls `SpeakerDetailRoute(`.
    - MainActivity still contains `speakerViewModelFactory()` and the route string template for the speaker destination is unchanged vs origin/main (`git diff origin/main -- androidApp/src/main/java/com/gdgnantes/devfest/androidapp/MainActivity.kt | grep -E '^[-+].*Screen\.[A-Za-z]+\.route'` prints nothing).
    - `git ls-files androidApp/src/main/java | grep '/screens/speakers/'` prints nothing (SpeakerPicture already in :core:ui).
    - `./gradlew :feature:speakers:dependencies --configuration releaseRuntimeClasspath` contains no `project :feature:`.
    - `RESOURCES-OK` and `CI-GREEN-BOTH` printed.
  </acceptance_criteria>
  <done>:feature:speakers builds as a leaf; speaker list/detail reached via Route entry points with the app owning the assisted factory and all navigation; CI green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User consent choice -> Firebase data collection | DataCollection screen/dialog + service decide whether analytics/crash/perf collection runs |
| Device storage (SharedPreferences) -> DataCollectionSettingsServiceImpl | Persisted consent read after update |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-17 | Tampering | DataCollectionSettingsServiceImpl SharedPreferences keys | high | mitigate | PREFS-KEYS-OK literal diff vs origin/main; repackage commit limited to package/import lines |
| T-03-18 | Information Disclosure | Consent gating (DataSharingInitializer / agreement dialog trigger) | high | mitigate | DataSharingInitializer diff vs origin/main limited to import lines (acceptance); MainActivity keeps the dialog trigger, only switching call targets; Android smoke checkpoint 3 (03-08) re-checks consent UI |
| T-03-19 | Spoofing | Speaker social links opened via callbacks | low | accept | Link opening stays in :androidApp ExternalContentService, unchanged |
</threat_model>

<verification>
- Leaf builds, app debug/release/unit/lint green; `PREFS-KEYS-OK`; `RESOURCES-OK`; `CI-GREEN-BOTH`.
Edge coverage: ARCH-03 adjacency, ARCH-02 concurrency.
</verification>

<success_criteria>
- :feature:settings and :feature:speakers exist with their ViewModels and screens (ARCH-03), callback-only entry points (D-07), consent persistence/gating unchanged.
</success_criteria>

## Artifacts this phase produces (this plan)

- Gradle modules `:feature:settings` (packages `com.gdgnantes.devfest.feature.settings`, `.datacollection`, `.legal`, `.services`) and `:feature:speakers` (packages `com.gdgnantes.devfest.feature.speakers`, `.details`, `.list`)
- Functions: `SettingsRoute`, `DataCollectionSettingsRoute`, `LegalRoute` (package `com.gdgnantes.devfest.feature.settings`), `SpeakersRoute`, `SpeakerDetailRoute` (package `com.gdgnantes.devfest.feature.speakers`)
- Files: `feature/settings/build.gradle.kts`, `feature/speakers/build.gradle.kts`

<output>
Create `.planning/phases/03-multi-module-architecture-extraction/03-07-SUMMARY.md` when done.
</output>
