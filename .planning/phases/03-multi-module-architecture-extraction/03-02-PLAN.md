---
phase: 03-multi-module-architecture-extraction
plan: 02
type: execute
wave: 3
depends_on: ["03-01", "03-10", "03-11"]
files_modified:
  - settings.gradle.dcl
  - core/network/build.gradle.kts
  - core/network/src/commonMain/kotlin/** (moved Apollo.kt, ApolloCache.kt)
  - core/network/src/commonMain/graphql/operations.graphql
  - core/network/src/commonMain/graphql/schema.graphqls
  - core/network/src/commonMain/graphql/extra.graphqls
  - core/analytics/build.gradle.kts
  - core/analytics/src/commonMain/kotlin/** (moved AnalyticsEvent/Page/Param/Service)
  - core/analytics/src/androidMain/kotlin/** (moved FirebaseAnalyticsService, PerformanceMonitoring, PerformanceExtensions)
  - shared/build.gradle.kts
  - shared/src/commonMain/kotlin/** (import updates only)
  - shared/src/jvmTest/kotlin/** (import updates only)
  - androidApp/build.gradle.kts
  - androidApp/src/main/java/** (import updates only)
autonomous: true
requirements: [ARCH-02, ARCH-04]

estimate:
  tokens: 110000
  raw_tokens: 110000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - ":core:network exists (D-14), owns the Apollo Gradle plugin configuration and the GraphQL schema/operations, generates types in package com.gdgnantes.devfest.core.network.graphql (D-15), and is an implementation-only dependency of :shared — never export()-ed to Swift (D-11)"
    - ":core:analytics exists with AnalyticsService/AnalyticsEvent/AnalyticsPage/AnalyticsParam in commonMain and FirebaseAnalyticsService + PerformanceMonitoring + trace/traceDataLoading in androidMain (D-03), and is api + export()-ed by :shared for all three iOS targets (D-11)"
    - "The Hilt AppModule stays in :androidApp and keeps binding FirebaseAnalyticsService to AnalyticsService; only its imports change (D-02)"
    - "Every Kotlin type name referenced from iosApp Swift sources (AnalyticsService, AnalyticsEvent, AnalyticsPage, AnalyticsParam, model types, DevFestNantesStore, DevFestNantesStoreBuilder) is still present unprefixed in shared.h, every referenced type's member set equals the 03-10 re-baseline, no type-level swift_name is underscore-suffixed, and the collision-disambiguated name count equals the baseline — i.e. the strengthened swift-names-gate.sh check prints SWIFT-NAMES-OK (ARCH-04 adjacency edge, D-16)"
    - "The Apollo @targetName schema extension (extra.graphqls, 03-10) moves with the schema unchanged, so the generated schema-type holders stay GraphQLVenue/GraphQLSession/GraphQLSpeaker/GraphQLRoom/GraphQLPartner in the new package and the Kotlin/Native name collision with the core:model domain classes cannot reappear"
    - "Each module step is two commits — pure git mv with packages unchanged, then package rename + imports — and every commit builds green before the next; both CI workflows are green on the pushed HEAD (ARCH-02 concurrency edge, D-16, D-18)"
    - ":core:network and :core:analytics depend on no :feature:* module (ARCH-02)"
  artifacts:
    - path: core/network/build.gradle.kts
      provides: "devfest.kmp.library + Apollo plugin, service(\"service\") with packageName com.gdgnantes.devfest.core.network.graphql"
    - path: core/analytics/build.gradle.kts
      provides: "devfest.kmp.library leaf with androidMain Firebase dependencies"
    - path: core/analytics/src/androidMain/kotlin/com/gdgnantes/devfest/core/analytics/FirebaseAnalyticsService.kt
      provides: "Android AnalyticsService implementation"
  key_links:
    - from: shared/build.gradle.kts
      to: core/analytics
      via: "api(project(\":core:analytics\")) + export(project(\":core:analytics\"))"
      pattern: "export\\(project\\(\":core:analytics\"\\)\\)"
    - from: shared/build.gradle.kts
      to: core/network
      via: "implementation(project(\":core:network\"))"
      pattern: "implementation\\(project\\(\":core:network\"\\)\\)"
    - from: androidApp/src/main/java/com/gdgnantes/devfest/androidapp/core/injection/AppModule.kt
      to: core/analytics androidMain
      via: "@Binds analyticsService(FirebaseAnalyticsService): AnalyticsService"
      pattern: "FirebaseAnalyticsService"
  prohibitions:
    - requirement_id: ARCH-04
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT ship any user-visible behavior change on Android or iOS while moving code (no drive-by fixes, no analytics event name/parameter change) — the project's core value is zero regression for users"
---

<objective>
Extract the two KMP modules that sit directly on `:core:model` (D-18 order: core/network / core/analytics): `:core:network` (Apollo client, cache, schema, codegen — internal, not exported, D-11) and `:core:analytics` (analytics contract in commonMain, Firebase-backed Android implementations in androidMain per D-03, exported to Swift per D-11). Each is two commits (D-16): pure move, then repackage.

Purpose: build the lower half of the KMP graph so `:core:data` (next plan) can depend on real modules; prove non-exported (`core:network`) vs exported (`core:analytics`) umbrella wiring behaves as RESEARCH Pattern 3 predicts.
Output: `:core:network`, `:core:analytics`, updated `:shared` umbrella wiring, green CI.
</objective>

<execution_context>
@/Users/robin/Development/Android/DevFestNantes/.claude/gsd-core/workflows/execute-plan.md
@/Users/robin/Development/Android/DevFestNantes/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-multi-module-architecture-extraction/03-CONTEXT.md
@.planning/phases/03-multi-module-architecture-extraction/03-RESEARCH.md
@.planning/phases/03-multi-module-architecture-extraction/03-PATTERNS.md
@.planning/phases/03-multi-module-architecture-extraction/03-01-SUMMARY.md
@.planning/phases/03-multi-module-architecture-extraction/03-10-SUMMARY.md
@shared/build.gradle.kts

## Placement decisions (D-03 "lowest module that needs it", planner discretion)

- `PerformanceMonitoring` + `PerformanceExtensions` (`trace`, `traceDataLoading`) wrap Firebase Performance and are consumed by 4 ViewModels across 3 future features (speakers, agenda, session-detail) plus `AppModule` -> `:core:analytics` androidMain (Firebase-backed monitoring next to the Firebase analytics impl), package `com.gdgnantes.devfest.core.analytics.performance`.
- `PerformanceInitializer`, `ApplicationInitializer`, `DataSharingInitializer`, `OpenFeedbackInitializer`, `CoroutinesDispatcherProvider`, `SuspendUseCase`, `AppScope`, logging trees stay in `:androidApp` (app-only consumers).
- `@Inject` constructors in moved androidMain classes keep working without KSP in the core module: Dagger generates any missing factory in `:androidApp`'s component processing. javax.inject annotations come from the existing `libs.dagger.hilt.android` alias (no new coordinate).
- Apollo-generated package changes to `com.gdgnantes.devfest.core.network.graphql` (D-15 explicitly includes the Apollo `packageName` config).
- `GraphQLStoreJvmTest` stays in `:shared` jvmTest until its subject `GraphQLStore` moves in 03-03 (tests follow code).
- 03-01 halt resolution (03-10): `shared/src/commonMain/graphql/extra.graphqls` holds five `@targetName` schema extensions, which rename Apollo's schema-type holders to `GraphQLVenue`/`GraphQLSession`/`GraphQLSpeaker`/`GraphQLRoom`/`GraphQLPartner`. It moves with `schema.graphqls`, byte-identical. Apollo picks it up through the plugin's `**/*.graphqls` fallback, so there is no `schemaFiles` line to carry, unless 03-10 recorded that contingency; in that case the line moves verbatim with the `apollo { }` block. Once `:core:network` is implementation-only and not exported (D-11), the Apollo classes, including the `GraphQL*` holders, leave `shared.h`. The gate diff shows them as removed; classify them as core:network-internal. The gate still passes because no Swift-referenced name or member belongs to them (03-10 excluded `Link`, which binds to SwiftUI).
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract :core:network (Apollo) — pure move, then repackage incl. Apollo packageName</name>
  <files>settings.gradle.dcl, core/network/build.gradle.kts, core/network/src/commonMain/kotlin/com/gdgnantes/devfest/core/network/Apollo.kt, core/network/src/commonMain/kotlin/com/gdgnantes/devfest/core/network/ApolloCache.kt, core/network/src/commonMain/graphql/operations.graphql, core/network/src/commonMain/graphql/schema.graphqls, core/network/src/commonMain/graphql/extra.graphqls, shared/build.gradle.kts, shared/src/commonMain/kotlin/com/gdgnantes/devfest/store/graphql/GraphQLStore.kt, shared/src/commonMain/kotlin/com/gdgnantes/devfest/store/graphql/Mappers.kt, shared/src/commonMain/kotlin/com/gdgnantes/devfest/domain/RoomSortIndex.kt, shared/src/commonMain/kotlin/com/gdgnantes/devfest/store/DevFestNantesStoreBuilder.kt, shared/src/jvmTest/kotlin/com/gdgnantes/devfest/store/graphql/GraphQLStoreJvmTest.kt</files>
  <read_first>
    - shared/build.gradle.kts (apollo { } block lines 74-80, plugin list, commonMain deps)
    - shared/src/commonMain/kotlin/com/gdgnantes/devfest/store/graphql/Apollo.kt
    - shared/src/commonMain/kotlin/com/gdgnantes/devfest/store/graphql/ApolloCache.kt
    - shared/src/commonMain/kotlin/com/gdgnantes/devfest/store/graphql/GraphQLStore.kt (imports of generated types + apolloClient)
    - shared/src/commonMain/kotlin/com/gdgnantes/devfest/store/graphql/Mappers.kt
    - shared/src/jvmTest/kotlin/com/gdgnantes/devfest/store/graphql/GraphQLStoreJvmTest.kt
    - shared/src/commonMain/graphql/extra.graphqls (03-10's @targetName schema extension, moves unchanged)
    - core/model/build.gradle.kts (leaf build-file shape from 03-01)
    - .planning/STATE.md section "Apollo cache import paths actually used"
  </read_first>
  <action>
Commit 1 (pure move, packages unchanged, D-16): add `include(":core:network")` to `settings.gradle.dcl`. `git mv` `shared/src/commonMain/kotlin/com/gdgnantes/devfest/store/graphql/Apollo.kt` and `ApolloCache.kt` to `core/network/src/commonMain/kotlin/com/gdgnantes/devfest/store/graphql/`, and `shared/src/commonMain/graphql/operations.graphql` + `schema.graphqls` + `extra.graphqls` (03-10's `@targetName` renames, content unchanged) to `core/network/src/commonMain/graphql/`. Create `core/network/build.gradle.kts`: `plugins { id("devfest.kmp.library"); alias(libs.plugins.appollo) }`; `kotlin { sourceSets { commonMain.dependencies { api(libs.bundles.appollo) } } }` (api because the public `apolloClient` exposes `ApolloClient`; add `implementation(project(":core:model"))` only if a moved file imports a model type); and the `apollo { service("service") { ... } }` block moved verbatim from `shared/build.gradle.kts` (packageName still `com.gdgnantes.devfest.graphql` in this commit, compiler plugin `normalized-cache-apollo-compiler-plugin:${libs.versions.appolloCache.get()}`, pluginArgument unchanged). In `shared/build.gradle.kts`: remove `alias(libs.plugins.appollo)` and the `apollo { }` block, replace `implementation(libs.bundles.appollo)` with `implementation(project(":core:network"))` (keep a direct Apollo dependency only if `:shared` code still fails to compile without it — it should not, core:network exposes apollo runtime as api). Do not export :core:network from the framework (D-11). Verify, commit.
Commit 2 (repackage, D-15): `git mv` the two Kotlin files to `core/network/src/commonMain/kotlin/com/gdgnantes/devfest/core/network/`, change their package from `com.gdgnantes.devfest.store.graphql` to `com.gdgnantes.devfest.core.network`; change the Apollo `packageName.set(...)` to `com.gdgnantes.devfest.core.network.graphql`; update imports of `apolloClient`/cache symbols and of every generated type (`com.gdgnantes.devfest.graphql.*` -> `com.gdgnantes.devfest.core.network.graphql.*`) in `GraphQLStore.kt`, `Mappers.kt`, `RoomSortIndex.kt`, `DevFestNantesStoreBuilder.kt`, `GraphQLStoreJvmTest.kt` and any other importer found by `git grep -n 'com.gdgnantes.devfest.graphql'`. Only package/import lines and the one packageName value change. Verify, commit.
  </action>
  <verify>
    <automated>OLD=$(git grep -n -E 'com\.gdgnantes\.devfest\.graphql\.|package com\.gdgnantes\.devfest\.store\.graphql' -- 'core/network/*.kt' 'shared/*.kt') ; test -z "$OLD" && ./gradlew --no-daemon :core:network:generateApolloSources :core:network:jvmTest :shared:jvmTest :androidApp:assembleDebug detekt && bash .planning/phases/03-multi-module-architecture-extraction/swift-names-gate.sh check</automated>
    <fails_when>non-zero exit (leftover old generated-package import or network file still in the old package), GraphQLStoreJvmTest failing inside :shared:jvmTest, or last line not SWIFT-NAMES-OK</fails_when>
  </verify>
  <acceptance_criteria>
    - `core/network/build.gradle.kts` contains `packageName.set("com.gdgnantes.devfest.core.network.graphql")` and `id("devfest.kmp.library")`, and none of `compileSdk`, `minSdk`, `detekt {`.
    - `shared/build.gradle.kts` contains `implementation(project(":core:network"))`, no `apollo {` block, no `alias(libs.plugins.appollo)`, and `grep -c 'core:network' shared/build.gradle.kts` equals 1 (no export line for it).
    - `git ls-files shared/src/commonMain/graphql` prints nothing; `git ls-files core/network/src/commonMain/graphql` lists `operations.graphql`, `schema.graphqls` and `extra.graphqls`; `grep -c '@targetName(name: "GraphQL' core/network/src/commonMain/graphql/extra.graphqls` prints `5`.
    - After the repackage, `core/network/build/generated/source/apollo/service/com/gdgnantes/devfest/core/network/graphql/type/GraphQLVenue.kt` exists and `.../type/Venue.kt` does not (same for Session, Speaker, Room, Partner).
    - The move commit shows renames and no `package` line change; the repackage commit's Kotlin diff contains only `package`/`import` lines (plus the one `packageName.set` line in core/network/build.gradle.kts).
    - `swift-names-gate.sh check` prints `SWIFT-NAMES-OK`; the SUMMARY lists every removed/added swift_name from the informational diff and classifies each as a core:network-internal (Apollo/GraphQL-generated or Apollo.kt/ApolloCache.kt) name.
  </acceptance_criteria>
  <done>:core:network builds and generates Apollo types in the new package; :shared compiles and its jvmTest (GraphQLStoreJvmTest, DevFestNantesStoreContractTest) passes against it; Swift-referenced names unchanged; two commits.</done>
</task>

<task type="auto">
  <name>Task 2: Extract :core:analytics (commonMain contract + Firebase androidMain impls), export it, push, CI green</name>
  <files>settings.gradle.dcl, core/analytics/build.gradle.kts, core/analytics/src/commonMain/kotlin/com/gdgnantes/devfest/core/analytics/AnalyticsEvent.kt, core/analytics/src/commonMain/kotlin/com/gdgnantes/devfest/core/analytics/AnalyticsPage.kt, core/analytics/src/commonMain/kotlin/com/gdgnantes/devfest/core/analytics/AnalyticsParam.kt, core/analytics/src/commonMain/kotlin/com/gdgnantes/devfest/core/analytics/AnalyticsService.kt, core/analytics/src/androidMain/kotlin/com/gdgnantes/devfest/core/analytics/FirebaseAnalyticsService.kt, core/analytics/src/androidMain/kotlin/com/gdgnantes/devfest/core/analytics/performance/PerformanceMonitoring.kt, core/analytics/src/androidMain/kotlin/com/gdgnantes/devfest/core/analytics/performance/PerformanceExtensions.kt, shared/build.gradle.kts, androidApp/build.gradle.kts, androidApp/src/main/java/com/gdgnantes/devfest/androidapp/core/injection/AppModule.kt, androidApp/src/main/java/** (import updates only)</files>
  <read_first>
    - shared/src/commonMain/kotlin/com/gdgnantes/devfest/analytics/AnalyticsService.kt
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/services/FirebaseAnalyticsService.kt
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/core/performance/PerformanceMonitoring.kt
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/core/performance/PerformanceExtensions.kt
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/core/performance/PerformanceInitializer.kt (same-package consumer that stays in the app)
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/core/injection/AppModule.kt (bindings/provides for AnalyticsService and PerformanceMonitoring)
    - androidApp/build.gradle.kts (firebase/timber/hilt dependency aliases to reuse)
    - shared/build.gradle.kts
  </read_first>
  <action>
Commit 1 (pure move, packages unchanged, D-16): add `include(":core:analytics")` to `settings.gradle.dcl`. `git mv` the four files of `shared/src/commonMain/kotlin/com/gdgnantes/devfest/analytics/` to `core/analytics/src/commonMain/kotlin/com/gdgnantes/devfest/analytics/`; `git mv` `androidApp/.../services/FirebaseAnalyticsService.kt` to `core/analytics/src/androidMain/kotlin/com/gdgnantes/devfest/androidapp/services/`, and `androidApp/.../core/performance/PerformanceMonitoring.kt` + `PerformanceExtensions.kt` to `core/analytics/src/androidMain/kotlin/com/gdgnantes/devfest/androidapp/core/performance/` (D-03). Create `core/analytics/build.gradle.kts`: `plugins { id("devfest.kmp.library") }`; `commonMain.dependencies { api(project(":core:model")) }`; `androidMain.dependencies { implementation(project.dependencies.platform(libs.firebase.bom)); implementation(libs.firebase.analytics.ktx); implementation(libs.firebase.perf.ktx); implementation(libs.timber); implementation(libs.dagger.hilt.android) }` plus only what compilation of the moved files additionally requires, using existing catalog aliases (if an artifact is only transitively available today, add a catalog alias pinned to the version `:androidApp:releaseRuntimeClasspath` already resolves — no version change). In `shared/build.gradle.kts` add `api(project(":core:analytics"))` to commonMain and `export(project(":core:analytics"))` inside the framework lambda (D-11). In `androidApp/build.gradle.kts` add `implementation(project(":core:analytics"))`. `AppModule` stays in `:androidApp` untouched except imports (D-02). Verify, commit.
Commit 2 (repackage, D-15): `git mv` to package-matching directories and rename packages: commonMain `com.gdgnantes.devfest.analytics` -> `com.gdgnantes.devfest.core.analytics`; `FirebaseAnalyticsService` -> `com.gdgnantes.devfest.core.analytics`; `PerformanceMonitoring` + `PerformanceExtensions` -> `com.gdgnantes.devfest.core.analytics.performance`. Update imports everywhere (`AppModule`, `PerformanceInitializer`, ViewModels, `DevFestNantesApplication`, shared code). Only package/import lines change — analytics event names, parameter keys and Firebase trace names are string literals and must stay byte-identical. Verify, commit.
Then push, and run the CI gate (the draft PR from 03-01 triggers both workflows).
  </action>
  <verify>
    <automated>OLD=$(git grep -n -E 'com\.gdgnantes\.devfest\.analytics\.|androidapp\.services\.FirebaseAnalyticsService|androidapp\.core\.performance\.(PerformanceMonitoring|trace)' -- '*.kt') ; test -z "$OLD" && ./gradlew --no-daemon :core:analytics:jvmTest :core:analytics:compileKotlinIosSimulatorArm64 :shared:jvmTest :androidApp:assembleDebug :androidApp:assembleRelease detekt lint && bash .planning/phases/03-multi-module-architecture-extraction/swift-names-gate.sh check</automated>
    <fails_when>non-zero exit (old analytics/performance package still referenced, or a build/lint task fails), or last line not SWIFT-NAMES-OK</fails_when>
    <automated>SHA=$(git rev-parse HEAD) || exit 1; BR=$(git branch --show-current) || exit 1; test -n "$SHA" -a -n "$BR" || { echo "NO-GIT-CONTEXT"; exit 1; }; for WF in android.yml ios.yml; do RID=$(gh run list --workflow "$WF" --branch "$BR" --limit 20 --json databaseId,headSha --jq "[.[]|select(.headSha==\"$SHA\")][0].databaseId"); test -n "$RID" || { echo "NO-RUN $WF $SHA"; exit 1; }; gh run watch "$RID" --exit-status || { echo "RED $WF $RID"; exit 1; }; done && echo "CI-GREEN-BOTH $SHA"</automated>
    <fails_when>non-zero exit, or output contains NO-RUN, RED or NO-GIT-CONTEXT instead of CI-GREEN-BOTH</fails_when>
  </verify>
  <acceptance_criteria>
    - `shared/build.gradle.kts` contains `api(project(":core:analytics"))` and `export(project(":core:analytics"))`; `androidApp/build.gradle.kts` contains `implementation(project(":core:analytics"))`.
    - `git ls-files core/analytics/src/androidMain` lists `FirebaseAnalyticsService.kt`, `PerformanceMonitoring.kt`, `PerformanceExtensions.kt`; none of these three names is tracked under `androidApp/`.
    - `AppModule.kt` is still at `androidApp/src/main/java/com/gdgnantes/devfest/androidapp/core/injection/AppModule.kt` and still contains `FirebaseAnalyticsService` and `AnalyticsService`.
    - `git show HEAD -- '*.kt' | grep -E '^[-+]' | grep -vE '^(\+\+\+|---)' | grep -vE '^[-+](package|import) '` prints nothing (repackage commit touches only package/import lines).
    - `core/analytics/build.gradle.kts` contains none of `compileSdk`, `minSdk`, `detekt {`.
    - `./gradlew :core:analytics:dependencies` output contains no `project :feature:`.
    - `swift-names-gate.sh check` prints `SWIFT-NAMES-OK` (AnalyticsService/AnalyticsEvent/AnalyticsPage/AnalyticsParam still unprefixed); CI gate prints `CI-GREEN-BOTH`.
  </acceptance_criteria>
  <done>:core:analytics is exported to Swift under unchanged names, its Android implementations are bound by the unchanged AppModule, four commits landed across the plan, both CI workflows green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kotlin umbrella framework -> Swift | Exported public API surface |
| App -> Firebase SDKs | Analytics/performance events leave the device |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Information Disclosure | shared/build.gradle.kts (core:network) | medium | mitigate | :core:network is implementation-only in :shared; acceptance asserts exactly one `core:network` reference (no export line); swift-names diff classified in SUMMARY (D-11) |
| T-03-05 | Tampering | Analytics event names / trace names during repackage | low | mitigate | Repackage commit diff restricted to package/import lines (acceptance grep), so event/param string literals cannot drift |
| T-03-06 | Elevation of Privilege | Hilt graph after moving @Inject classes out of the app | low | mitigate | AppModule unchanged except imports (D-02); :androidApp assembleDebug/Release compile the full Dagger graph, failing on any missing binding |
</threat_model>

<verification>
- `./gradlew --no-daemon :core:network:jvmTest :core:analytics:jvmTest :shared:jvmTest :androidApp:assembleDebug :androidApp:assembleRelease detekt lint` green.
- `swift-names-gate.sh check` -> `SWIFT-NAMES-OK`.
- CI gate -> `CI-GREEN-BOTH <sha>`.
Edge coverage handled: ARCH-02 concurrency (two green commits per module), ARCH-04 adjacency (Swift names gate).
</verification>

<success_criteria>
- :core:network (internal) and :core:analytics (exported) exist, correctly wired into the umbrella (ARCH-02, ARCH-04).
- Android app still builds its Hilt graph from the single AppModule (D-02).
- Both CI workflows green.
</success_criteria>

## Artifacts this phase produces (this plan)

- Gradle modules `:core:network` (package `com.gdgnantes.devfest.core.network`, Apollo generated package `com.gdgnantes.devfest.core.network.graphql`) and `:core:analytics` (packages `com.gdgnantes.devfest.core.analytics`, `com.gdgnantes.devfest.core.analytics.performance`)
- Files: `core/network/build.gradle.kts`, `core/analytics/build.gradle.kts`
- Moved symbols (new FQNs): `com.gdgnantes.devfest.core.network.apolloClient`, `com.gdgnantes.devfest.core.analytics.AnalyticsService`, `...core.analytics.FirebaseAnalyticsService`, `...core.analytics.performance.PerformanceMonitoring`, `...core.analytics.performance.trace`, `...core.analytics.performance.traceDataLoading`

<output>
Create `.planning/phases/03-multi-module-architecture-extraction/03-02-SUMMARY.md` when done (include swift-names diff classification and CI run URLs).
</output>
