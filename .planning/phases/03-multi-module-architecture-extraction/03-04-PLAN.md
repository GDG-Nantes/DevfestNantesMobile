---
phase: 03-multi-module-architecture-extraction
plan: 04
type: execute
wave: 5
depends_on: ["03-03"]
files_modified:
  - settings.gradle.dcl
  - core/testing/build.gradle.kts
  - core/testing/src/commonMain/kotlin/com/gdgnantes/devfest/core/testing/FakeStores.kt
  - core/testing/src/commonTest/kotlin/com/gdgnantes/devfest/core/testing/FakeStoresTest.kt
  - build-logic/convention/build.gradle.kts
  - build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/AndroidLibraryConventionPlugin.kt
  - build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/AndroidHiltConventionPlugin.kt
  - build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/AndroidFeatureConventionPlugin.kt
  - build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/AndroidApplicationConventionPlugin.kt
  - build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/AndroidCommon.kt
  - build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/DependencyResolutionRules.kt
  - build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/DetektConventionPlugin.kt
  - gradle/libs.versions.toml
  - build.gradle.kts
  - androidApp/build.gradle.kts
autonomous: true
requirements: [ARCH-01, ARCH-02]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - ":core:testing exists as a KMP module (D-19) exposing core:model, core:data, kotlin-test and kotlinx-coroutines-test as api plus a fakeDevFestNantesStore() entry point; it is never a main (non-test) dependency of any production module"
    - "build-logic registers devfest.android.library, devfest.android.hilt, devfest.android.feature and devfest.android.application in addition to devfest.detekt and devfest.kmp.library (D-13)"
    - "devfest.android.feature applies the Android library + Compose + Hilt plugin + KSP with the Hilt compiler, so every feature module gets Hilt uniformly (D-02, RESEARCH Pitfall 3)"
    - ":androidApp applies devfest.android.application + devfest.android.hilt and its build file no longer contains compileSdk, minSdk, targetSdk, compileOptions, testOptions, a detekt block or the configurations.configureEach resolution block (ARCH-01: no duplicated build configuration)"
    - "The kotlin-metadata-jvm force and the firebase-auth-ktx -> firebase-auth:24.2.0 substitution (with their explanatory comments) apply to every Android module via one build-logic helper"
    - ":androidApp's releaseRuntimeClasspath dependency report and merged release AndroidManifest are identical before and after adopting the convention plugins (zero behavior change)"
    - "Every commit builds green and both CI workflows are green on the pushed HEAD (ARCH-02 concurrency edge, D-18)"
  artifacts:
    - path: core/testing/build.gradle.kts
      provides: "devfest.kmp.library leaf exposing test fixtures"
    - path: core/testing/src/commonMain/kotlin/com/gdgnantes/devfest/core/testing/FakeStores.kt
      provides: "fun fakeDevFestNantesStore(): DevFestNantesStore"
    - path: build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/AndroidFeatureConventionPlugin.kt
      provides: "AndroidFeatureConventionPlugin : Plugin<Project>"
    - path: build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/AndroidApplicationConventionPlugin.kt
      provides: "AndroidApplicationConventionPlugin : Plugin<Project>"
    - path: build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/DependencyResolutionRules.kt
      provides: "Project.configureDependencyResolutionRules()"
  key_links:
    - from: androidApp/build.gradle.kts
      to: build-logic/convention
      via: "plugins { id(\"devfest.android.application\"); id(\"devfest.android.hilt\") }"
      pattern: "devfest\\.android\\.application"
    - from: build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/AndroidFeatureConventionPlugin.kt
      to: core/ui, core/data, core/model, core/analytics
      via: "implementation(project(...)) added by the feature convention"
      pattern: "project\\(\":core:ui\"\\)"
  prohibitions:
    - requirement_id: ARCH-01
      category: values
      status: resolved
      verification: test
      resolution: null
      reason: null
      statement: "MUST NOT change what the Android app ships while consolidating build logic — same applicationId, versionCode/Name, SDK levels, manifest and release runtime dependencies"
---

<objective>
Complete the convention-plugin set (D-13) for the Android side and create `:core:testing` (D-19), following D-18's order (core/testing before core/ui). `:androidApp` adopts the application + Hilt conventions so its build file keeps only app-specific configuration (ARCH-01). The feature/library conventions are registered now so 03-05..03-08 modules are one-line build files.

Purpose: remove the remaining duplicated Android build configuration before 7 new Android modules appear.
Output: `:core:testing`, four new convention plugins, shared resolution-rules helper, slimmed `androidApp/build.gradle.kts`, green CI.
</objective>

<execution_context>
@/Users/robin/Development/Android/DevFestNantes/.claude/gsd-core/workflows/execute-plan.md
@/Users/robin/Development/Android/DevFestNantes/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-multi-module-architecture-extraction/03-CONTEXT.md
@.planning/phases/03-multi-module-architecture-extraction/03-RESEARCH.md
@.planning/phases/03-multi-module-architecture-extraction/03-PATTERNS.md
@.planning/phases/03-multi-module-architecture-extraction/03-01-SUMMARY.md
@androidApp/build.gradle.kts
@build-logic/convention/build.gradle.kts

## Decisions (planner discretion, D-13 "planner may refine")

- Plugin set: `devfest.detekt`, `devfest.kmp.library` (03-01) + `devfest.android.library` (Android library + Compose), `devfest.android.hilt` (Hilt plugin + KSP + Hilt deps, used by :androidApp, :core:ui and features), `devfest.android.feature` (library + hilt + core module deps), `devfest.android.application`. A separate hilt plugin exists because `:core:ui` hosts `@HiltViewModel BookmarksViewModel` (D-06) but must not get the feature convention's `:core:ui` self-dependency.
- `:core:testing` content (D-19): the existing fakes stay in production modules (verified in 03-03: production `DevFestNantesStoreBuilder` uses `DevFestNantesStoreMocked`; Compose `@Preview`s use the stubs). `:core:testing` therefore starts as the shared test-dependency entry point (api re-exports + `fakeDevFestNantesStore()`), consumed by feature tests via the feature convention's `testImplementation(project(":core:testing"))`; Phase 4 (DI-05) / Phase 5 extend it. StoreStubs' RNG is not touched (TEST-01, Phase 5).
- AGP 9 built-in Kotlin: Android conventions do not apply a separate Kotlin Android plugin (matching `:androidApp` today).
- The application convention does NOT derive `namespace` (the app keeps `com.gdgnantes.devfest.androidapp` explicitly); library conventions use `moduleNamespace()`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create :core:testing (KMP test-fixture entry point)</name>
  <files>settings.gradle.dcl, core/testing/build.gradle.kts, core/testing/src/commonMain/kotlin/com/gdgnantes/devfest/core/testing/FakeStores.kt, core/testing/src/commonTest/kotlin/com/gdgnantes/devfest/core/testing/FakeStoresTest.kt</files>
  <read_first>
    - core/data/src/commonMain/kotlin/com/gdgnantes/devfest/core/data/DevFestNantesStoreMocked.kt
    - core/data/src/commonTest/kotlin/com/gdgnantes/devfest/core/data/DevFestNantesStoreContractTest.kt (runTest usage pattern)
    - core/data/build.gradle.kts (leaf build shape)
  </read_first>
  <action>Add `include(":core:testing")` to `settings.gradle.dcl`. Create `core/testing/build.gradle.kts`: `plugins { id("devfest.kmp.library") }`, `commonMain.dependencies { api(project(":core:model")); api(project(":core:data")); api(libs.kotlin.test); api(libs.kotlinx.coroutines.test) }`. Create `FakeStores.kt` in package `com.gdgnantes.devfest.core.testing` with KDoc stating it is the shared fixture entry point for module tests (extended by DI-05/Phase 5) and one function `fun fakeDevFestNantesStore(): DevFestNantesStore = DevFestNantesStoreMocked()`. Create `FakeStoresTest` (commonTest, `kotlin.test` + `runTest`) asserting `fakeDevFestNantesStore().sessions.first()` is not empty. Do not move DevFestNantesStoreMocked or the stubs (D-19 verification above). Single commit (new module, nothing moved).</action>
  <verify>
    <automated>./gradlew --no-daemon :core:testing:jvmTest :core:testing:compileKotlinIosSimulatorArm64 detekt && ls core/testing/build/test-results/jvmTest/*.xml > "${TMPDIR:-/tmp}/p3-testing-results.txt" && grep -q FakeStoresTest "${TMPDIR:-/tmp}/p3-testing-results.txt" && echo "CORE-TESTING-OK"</automated>
    <fails_when>non-zero exit, or output lacks CORE-TESTING-OK (FakeStoresTest did not execute)</fails_when>
  </verify>
  <acceptance_criteria>
    - `./gradlew projects` lists `:core:testing`.
    - `core/testing/build.gradle.kts` contains `api(project(":core:data"))` and none of `compileSdk`, `minSdk`, `detekt {`.
    - `git grep -n 'project(":core:testing")' -- '*.gradle.kts'` shows it only in `testImplementation`/`commonTest`/`androidTestImplementation` contexts (never plain `implementation`/`api`).
    - `FakeStoresTest` executed with 0 failures.
  </acceptance_criteria>
  <done>:core:testing builds on all KMP targets and its wiring test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Android convention plugins + :androidApp adopts them (identical app output), push, CI green</name>
  <files>build-logic/convention/build.gradle.kts, build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/AndroidLibraryConventionPlugin.kt, build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/AndroidHiltConventionPlugin.kt, build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/AndroidFeatureConventionPlugin.kt, build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/AndroidApplicationConventionPlugin.kt, build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/AndroidCommon.kt, build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/DependencyResolutionRules.kt, build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/DetektConventionPlugin.kt, gradle/libs.versions.toml, build.gradle.kts, androidApp/build.gradle.kts</files>
  <read_first>
    - androidApp/build.gradle.kts (full — every block to move or keep)
    - build-logic/convention/build.gradle.kts and KmpLibraryConventionPlugin.kt / DetektConventionPlugin.kt (03-01 conventions to mirror)
    - build.gradle.kts (root apply-false list)
    - gradle/libs.versions.toml ([plugins] and hilt/compose/test aliases)
    - .planning/STATE.md Blockers/Concerns (firebase-auth-ktx substitution follow-up note)
  </read_first>
  <action>
Step 0 (before editing anything): capture `./gradlew --no-daemon :androidApp:dependencies --configuration releaseRuntimeClasspath` to `${TMPDIR:-/tmp}/p3-app-deps-before.txt`, run `./gradlew --no-daemon :androidApp:processReleaseMainManifest` and copy the merged release manifest (locate it with `find androidApp/build/intermediates -path '*merged_manifest*release*' -name AndroidManifest.xml`) to `${TMPDIR:-/tmp}/p3-app-manifest-before.xml`.
Step 1: catalog — add plugin alias `android-library = { id = "com.android.library", version.ref = "agp" }` and, if the Kotlin DSL types for Hilt/KSP/Compose extensions are needed at compile time, compileOnly library aliases on existing version refs (`ksp-gradlePlugin` com.google.devtools.ksp:symbol-processing-gradle-plugin ref ksp, `compose-gradlePlugin` org.jetbrains.kotlin:compose-compiler-gradle-plugin ref kotlin); prefer applying plugins by id without typed config to avoid new compileOnly entries. Root `build.gradle.kts`: add `apply false` for `android.library` and `kotlin.compose.compiler` (keep all existing lines).
Step 2: build-logic (register ids in `gradlePlugin { }`):
- `DependencyResolutionRules.kt`: `internal fun Project.configureDependencyResolutionRules()` containing, verbatim with their comment blocks, the `force("org.jetbrains.kotlin:kotlin-metadata-jvm:<kotlin version from libs>")` rule and the `firebase-auth-ktx` -> `firebase-auth:24.2.0` substitution with its `.because(...)` text, applied via `configurations.configureEach`.
- `AndroidCommon.kt`: `internal fun Project.configureAndroidCommon(...)` setting `compileSdk = AndroidSdk.compile`, `minSdk = AndroidSdk.min`, `testInstrumentationRunner = "androidx.test.runner.AndroidJUnitRunner"`, `compileOptions` source/target `JavaVersion.VERSION_17`, `buildFeatures.compose = true`, `testOptions.unitTests.isReturnDefaultValues = true` and `isIncludeAndroidResources = true`; Kotlin `compilerOptions { jvmTarget = JVM_17; freeCompilerArgs += "-Xopt-in=kotlin.RequiresOptIn", "-Xopt-in=kotlin.Experimental" }`; Compose deps `implementation(platform(libs.androidx.compose.bom))`, `testImplementation(platform(...))`, `androidTestImplementation(platform(...))`, `implementation(libs.androidx.compose.material3)`, `implementation(libs.androidx.compose.ui.tooling.preview)`, `debugImplementation(libs.bundles.debug)`, `testImplementation(libs.bundles.test)`; calls `configureDependencyResolutionRules()`.
- `DetektConventionPlugin`: add the Android mode (default sources + `jvmTarget = "1.8"` on `Detekt` and `DetektCreateBaselineTask`, exactly as androidApp today).
- `AndroidLibraryConventionPlugin` (`devfest.android.library`): applies `com.android.library`, `org.jetbrains.kotlin.plugin.compose`, `devfest.detekt` (Android mode); `namespace = moduleNamespace()`; `configureAndroidCommon`.
- `AndroidHiltConventionPlugin` (`devfest.android.hilt`): applies `com.google.dagger.hilt.android` + `com.google.devtools.ksp`; deps `implementation(libs.dagger.hilt.android)`, `ksp(libs.dagger.hilt.compiler)`, `androidTestImplementation(libs.dagger.hilt.android.testing)`, `kspAndroidTest(libs.dagger.hilt.compiler)`, `testImplementation(libs.dagger.hilt.android.testing)`, `kspTest(libs.dagger.hilt.compiler)`, `implementation(libs.androidx.hilt.navigation.compose)` (D-02).
- `AndroidFeatureConventionPlugin` (`devfest.android.feature`): applies `devfest.android.library` + `devfest.android.hilt`; adds `implementation(project(":core:ui"))`, `implementation(project(":core:data"))`, `implementation(project(":core:model"))`, `implementation(project(":core:analytics"))`, `testImplementation(project(":core:testing"))`. It must never add any `:feature:` project dependency (ARCH-02).
- `AndroidApplicationConventionPlugin` (`devfest.android.application`): applies `com.android.application`, `org.jetbrains.kotlin.plugin.compose`, `devfest.detekt` (Android mode); `configureAndroidCommon`; `targetSdk = AndroidSdk.target`; does not set namespace/applicationId.
Step 3: `androidApp/build.gradle.kts` — plugins become `id("devfest.android.application")`, `id("devfest.android.hilt")`, `alias(libs.plugins.kotlin.serialization)`, `alias(libs.plugins.google.services)`, `alias(libs.plugins.crashlytics)`, `alias(libs.plugins.firebase.perf)`, `alias(libs.plugins.secrets)`. Delete what the conventions now provide: the `configurations.configureEach` block, `detekt { }` + both Detekt task `withType` blocks + their imports, `compileSdk`, `minSdk`, `targetSdk`, `testInstrumentationRunner`, `compileOptions`, `buildFeatures.compose` (keep `buildConfig = true`), `testOptions`, the `kotlin { compilerOptions }` block, `detektPlugins(...)`, the six Hilt dependency lines, `hilt.navigation.compose`, and the compose BOM/material3/tooling-preview/debug/test-bundle lines. Keep `namespace = "com.gdgnantes.devfest.androidapp"`, applicationId, versionCode 37, versionName "2025.10.00", buildConfigFields, buildTypes, packaging, and all other app dependencies.
Step 4: re-capture deps and merged manifest to `...-after` files and diff them against `...-before` — dependencies compared as a normalized sorted set (tree prefixes and `(*)`/`(c)`/`(n)` markers stripped, because declaration order legitimately changes when deps move into conventions); both diffs must be empty. Commit, push, run the CI gate.
  </action>
  <verify>
    <automated>./gradlew --no-daemon :androidApp:dependencies --configuration releaseRuntimeClasspath > "${TMPDIR:-/tmp}/p3-app-deps-after.txt" && N() { sed -E 's/^[|+\\ -]+//; s/ \([*cn]\)$//' "$1" | grep -E '^(project |[A-Za-z0-9_.-]+:)' | sort -u; } && diff <(N "${TMPDIR:-/tmp}/p3-app-deps-before.txt") <(N "${TMPDIR:-/tmp}/p3-app-deps-after.txt") && ./gradlew --no-daemon :androidApp:processReleaseMainManifest :androidApp:assembleDebug :androidApp:assembleRelease :androidApp:testDebugUnitTest detekt lint && M=$(find androidApp/build/intermediates -path '*merged_manifest*release*' -name AndroidManifest.xml) && test -n "$M" && diff "${TMPDIR:-/tmp}/p3-app-manifest-before.xml" "$M" && echo "APP-OUTPUT-IDENTICAL"</automated>
    <fails_when>non-zero exit, any diff line printed (the normalized, order-insensitive set of release runtime dependencies or the merged manifest changed), or output lacks APP-OUTPUT-IDENTICAL</fails_when>
    <automated>SHA=$(git rev-parse HEAD) || exit 1; BR=$(git branch --show-current) || exit 1; test -n "$SHA" -a -n "$BR" || { echo "NO-GIT-CONTEXT"; exit 1; }; for WF in android.yml ios.yml; do RID=$(gh run list --workflow "$WF" --branch "$BR" --limit 20 --json databaseId,headSha --jq "[.[]|select(.headSha==\"$SHA\")][0].databaseId"); test -n "$RID" || { echo "NO-RUN $WF $SHA"; exit 1; }; gh run watch "$RID" --exit-status || { echo "RED $WF $RID"; exit 1; }; done && echo "CI-GREEN-BOTH $SHA"</automated>
    <fails_when>non-zero exit, or output contains NO-RUN, RED or NO-GIT-CONTEXT instead of CI-GREEN-BOTH</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -vE '^[[:space:]]*//' androidApp/build.gradle.kts` contains none of `compileSdk`, `minSdk`, `targetSdk`, `compileOptions`, `testOptions`, `detekt {`, `configureEach`, `AndroidSdk.`, and contains `id("devfest.android.application")`, `id("devfest.android.hilt")`, `namespace = "com.gdgnantes.devfest.androidapp"`, `versionCode = 37`.
    - `build-logic/convention/build.gradle.kts` registers `devfest.android.library`, `devfest.android.hilt`, `devfest.android.feature`, `devfest.android.application`.
    - `DependencyResolutionRules.kt` contains `kotlin-metadata-jvm` and `firebase-auth:24.2.0`.
    - `AndroidFeatureConventionPlugin.kt` contains `project(":core:ui")` and no `:feature:` string.
    - `APP-OUTPUT-IDENTICAL` and `CI-GREEN-BOTH` printed.
  </acceptance_criteria>
  <done>All Android build configuration shared by more than one module lives in build-logic; the app's shipped output is byte-for-byte equivalent at the manifest and dependency level; CI green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-logic -> :androidApp configuration | Convention code now defines SDK levels, compile options and dependency substitutions of the shipped app |
| Dependency resolution -> release APK | Substitution/force rules decide which third-party artifacts ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-10 | Tampering | DependencyResolutionRules.kt (force/substitution) | medium | mitigate | Rules moved verbatim; releaseRuntimeClasspath report diffed before/after (must be identical) |
| T-03-11 | Elevation of Privilege | merged AndroidManifest (permissions/components) | medium | mitigate | Merged release manifest diffed before/after (must be identical) — no permission or exported-component drift |
| T-03-12 | Information Disclosure | :core:testing leaking fakes into release | low | mitigate | Acceptance asserts :core:testing only appears in test configurations |
</threat_model>

<verification>
- `APP-OUTPUT-IDENTICAL`, `CORE-TESTING-OK`, `CI-GREEN-BOTH`.
Edge coverage: ARCH-02 concurrency (each commit green). FA-1 (ARCH-01 unclassified edge) remains a flagged assumption — duplication checked by string-absence greps.
</verification>

<success_criteria>
- Six convention plugins exist; :androidApp keeps only app-specific configuration (ARCH-01).
- :core:testing exists and is test-only (ARCH-02 module set progress).
</success_criteria>

## Artifacts this phase produces (this plan)

- Gradle module `:core:testing` (package `com.gdgnantes.devfest.core.testing`), function `fakeDevFestNantesStore()`, test class `FakeStoresTest`
- Plugin ids: `devfest.android.library`, `devfest.android.hilt`, `devfest.android.feature`, `devfest.android.application`
- Classes: `AndroidLibraryConventionPlugin`, `AndroidHiltConventionPlugin`, `AndroidFeatureConventionPlugin`, `AndroidApplicationConventionPlugin` (package `com.gdgnantes.devfest.buildlogic`)
- Functions: `Project.configureAndroidCommon(...)`, `Project.configureDependencyResolutionRules()`
- Catalog plugin alias `android-library` (+ optional compileOnly aliases `ksp-gradlePlugin`, `compose-gradlePlugin`)

<output>
Create `.planning/phases/03-multi-module-architecture-extraction/03-04-SUMMARY.md` when done; note in it that the firebase-auth-ktx substitution referenced by STATE.md's follow-up now lives in `build-logic/.../DependencyResolutionRules.kt`.
</output>
