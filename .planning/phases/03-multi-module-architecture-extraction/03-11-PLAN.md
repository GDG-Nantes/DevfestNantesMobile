---
phase: 03-multi-module-architecture-extraction
plan: 11
type: execute
wave: 2
depends_on: ["03-10"]
files_modified:
  - .planning/phases/03-multi-module-architecture-extraction/03-01-SUMMARY.md
  - .planning/STATE.md
  - .planning/ROADMAP.md
autonomous: false
requirements: [ARCH-04]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The iOS app runs on a simulator against the umbrella framework with :core:model exported and the collision removed: agenda, speakers, venue and about render and load data (D-12 checkpoint 1, carried over from 03-01 Task 3 which never ran)"
    - "03-01's halt is resolved only after 03-10's gates and this plan's D-12 checkpoint pass: 03-01-SUMMARY.md frontmatter reads status: complete, its D3 (ARCH-04 umbrella re-export) and D4 (iOS simulator smoke) coverage entries cite the 03-10/03-11 evidence, and a Halt Resolution section records options 1+3, the @targetName mechanism, the corrected collision attribution and the D-11 amendment"
    - "gsd-tools phase-plan-index 3 reports 03-01 with halted: false, and no plan lists 03-01 in blocked_by"
    - "The resume sequence is explicit: 03-02..03-09 become runnable on the NEXT /gsd-execute-phase 3 run (blocked_by is computed once per run), and STATE.md's BLOCKING Phase 3 concern is marked resolved"
    - "ROADMAP.md phase-3 plan list agrees with the plan index: 03-01 checked with a halt-resolved note, 03-10 and 03-11 checked in waves 1 and 2"
  artifacts:
    - path: .planning/phases/03-multi-module-architecture-extraction/03-01-SUMMARY.md
      provides: "status: complete + Halt Resolution section + D3/D4 coverage evidence"
  key_links:
    - from: .planning/phases/03-multi-module-architecture-extraction/03-01-SUMMARY.md
      to: "gsd-tools phase-plan-index (halted flag -> blocked_by of 03-02..03-09)"
      via: "frontmatter status field"
      pattern: "^status: complete$"
  prohibitions:
    - requirement_id: ARCH-04
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT mark the 03-01 halt resolved (status: complete) unless the D-12 simulator checkpoint was approved by the human — a flipped halt unblocks eight module-moving plans"
---

<objective>
Close the 03-01 halt. First run the D-12 iOS simulator checkpoint 1 that 03-01 Task 3 never reached, on 03-10's fixed framework. Then, only after approval, re-summarize 03-01 as `complete`, citing where its unmet deliverables D3 and D4 were completed, so the GSD plan index unblocks 03-02..03-09.

Purpose: `/gsd-execute-phase` keeps every plan that depends on 03-01 blocked while `03-01-SUMMARY.md` says `status: halted` (summary template #2830). Only a human-verified iOS run may lift that block (D-09/D-12).
Output: an approved simulator smoke run, an updated `03-01-SUMMARY.md`, STATE.md and ROADMAP.md consistent with the plan index, and an explicit resume instruction.
</objective>

<execution_context>
@/Users/robin/Development/Android/DevFestNantes/.claude/gsd-core/workflows/execute-plan.md
@/Users/robin/Development/Android/DevFestNantes/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/03-multi-module-architecture-extraction/03-01-SUMMARY.md
@.planning/phases/03-multi-module-architecture-extraction/03-10-SUMMARY.md

## Why a separate plan

The D-12 checkpoint is a blocking human-verify gate. It stays mid-flight, despite `workflow.human_verify_mode = end-of-phase`, because D-09 requires it to pass before any other module moves, and a batched end-of-phase check cannot gate that. The halt flip must come strictly after it. Folding both into 03-10 would have made a 4-task plan.

## Resume mechanics (verified in gsd-core by the orchestrator)

`/gsd-execute-phase` skips `has_summary` plans and plans whose `blocked_by` is non-empty, and computes both once at plan discovery. In the run that executes this plan, 03-02..03-09 stay skipped even after the flip. The user must start a new `/gsd-execute-phase 3` run. 03-02 declares `depends_on: ["03-01", "03-10", "03-11"]`.
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: D-12 checkpoint 1 — iOS simulator smoke on the fixed umbrella framework</name>
  <files>none (verification only)</files>
  <read_first>
    - iosApp/iosApp/Model/BaseViewModel.swift (store construction the smoke exercises)
    - .planning/phases/03-multi-module-architecture-extraction/03-10-SUMMARY.md (CI run URLs, binding table)
  </read_first>
  <action>Executor prepares, then pauses:
- Run `swift-names-gate.sh check` and show its last line.
- Print the two green CI run URLs from 03-10-SUMMARY.md.
- List the five Swift types whose names changed once (D-11 amendment), so the human knows which screens exercise them: Session and Room on Agenda, Speaker on Speakers, Venue on Venue, Partner on About.
The human then runs the app from Xcode (`iosApp/iosApp.xcodeproj`, scheme `iosApp`). The Xcode build phase still calls `./gradlew :shared:embedAndSignAppleFrameworkForXcode` (D-10).</action>
  <what-built>:core:model exported through the single `shared` framework (03-01), with the Apollo schema-type holders renamed via @targetName and the Swift call sites moved to the clean domain names (03-10).</what-built>
  <how-to-verify>
    1. Open `iosApp/iosApp.xcodeproj` in Xcode, select an iPhone simulator, Run.
    2. Agenda tab: sessions list renders with data for both days; room names and speaker names appear on rows; open one session detail.
    3. Speakers tab: speaker list renders with photos/names; open one speaker detail and confirm its sessions list.
    4. Venue tab: venue details render (address, map/floor plan).
    5. About tab: renders links and the partners section with logos.
    6. Confirm no crash and no Swift compile error mentioning Venue/Session/Speaker/Room/Partner.
  </how-to-verify>
  <verify>
    <automated>bash .planning/phases/03-multi-module-architecture-extraction/swift-names-gate.sh check | tail -1 | grep -qx 'SWIFT-NAMES-OK' && echo "GATE-OK"</automated>
    <fails_when>GATE-OK missing: the gate's last line is not SWIFT-NAMES-OK (a SWIFT-NAME-MISSING, SWIFT-TYPE-COLLISION, SWIFT-MEMBERS-CHANGED or SWIFT-NAME-COLLISION line was printed)</fails_when>
  </verify>
  <acceptance_criteria>
    - Human replies "approved" after steps 1-6 pass. Any failure = "failed: <what>": execution stops per D-09, Task 2 does NOT run, and 03-01 stays halted.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe what failed</resume-signal>
  <done>User confirmed the iOS app renders agenda, speakers, venue and about with data on the simulator.</done>
</task>

<task type="auto">
  <name>Task 2: Resolve the 03-01 halt — re-summarize as complete, align STATE/ROADMAP with the plan index</name>
  <files>.planning/phases/03-multi-module-architecture-extraction/03-01-SUMMARY.md, .planning/STATE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/phases/03-multi-module-architecture-extraction/03-01-SUMMARY.md (frontmatter `status: halted` line 101, coverage D3 lines 83-91, D4 lines 92-96, "Next Phase Readiness")
    - .planning/phases/03-multi-module-architecture-extraction/03-10-SUMMARY.md (commit SHAs, CI run URLs, corrected attribution, re-baseline diff)
    - .planning/STATE.md "Blockers/Concerns" (the BLOCKING Phase 3 entry) and "Decisions" (`[Phase 03]` halt-resolution entry)
    - .planning/ROADMAP.md "### Phase 3" plan list
    - /Users/robin/Development/Android/DevFestNantes/.claude/gsd-core/templates/summary.md around line 175 (#2830 halt semantics)
  </read_first>
  <precondition>Task 1 was approved by the human ("approved"), and 03-10-SUMMARY.md exists with CI-GREEN-BOTH recorded.</precondition>
  <action>
In `03-01-SUMMARY.md`, edit these frontmatter fields only:
- `status: halted` becomes `status: complete`.
- D3 (ARCH-04): append two verification entries. First: `kind: other`, `ref: "swift-names-gate.sh check (strengthened in 03-10: member sets + type-level collisions) -> SWIFT-NAMES-OK"`, `status: pass`. Second: `kind: integration`, `ref: "CI-GREEN-BOTH <sha> — android.yml <run url>, ios.yml <run url> (03-10 Task 2)"`, `status: pass`. Replace its `rationale` with one sentence stating D3 was completed by 03-10 after the halt (collision removed via @targetName, Swift call sites updated once under the D-11 amendment). Keep the original failing entry above it.
- D4: set `verification` to one entry: `kind: manual`, `ref: "03-11 Task 1 — D-12 checkpoint 1 approved <date>"`, `status: pass`. Replace its `rationale` with one sentence saying it was carried over to and completed by 03-11.
Leave `requirements-completed`, the actuals, and every existing body section untouched (history). Append a body section `## Halt Resolution (03-10 + 03-11)` with:
- the user decision (options 1+3, STATE.md `[Phase 03]` 2026-09-23);
- the mechanism (Apollo 5.2.0 `@targetName` schema extension in `extra.graphqls`, target names GraphQLVenue/GraphQLSession/GraphQLSpeaker/GraphQLRoom/GraphQLPartner);
- the correction to this SUMMARY's collision table: the colliding classes are the schema-type holders `graphql.type.X`, not `GetXQuery.X`;
- the strengthened gate's new checks;
- a pointer to the D-11 amendment in 03-CONTEXT.md;
- the note that this plan's "Swift sources need no change" assumption was superseded by that amendment;
- the resume instruction (next `/gsd-execute-phase 3` run).

In `.planning/STATE.md`, change only the BLOCKING Phase 3 entry under Blockers/Concerns. Prefix it with `**RESOLVED <date> by 03-10 + 03-11:**` and keep the text as history, or replace it with a one-line resolved note that points to 03-01-SUMMARY.md "Halt Resolution". Append one Decisions bullet: `- [Phase 03]: 03-01 halt resolved — Apollo schema-type holders renamed via @targetName (GraphQL<Type>), swift-names-gate.sh strengthened (member sets + type-level collisions), D-11 one-time Swift rename amendment; 03-02..03-09 runnable on next /gsd-execute-phase 3`. Leave the frontmatter/position fields to the executor's normal state tooling.

In `.planning/ROADMAP.md` phase-3 plan list, change the 03-01 line's checkbox to `[x]` and replace its halted note with `(halt resolved by 03-10 + 03-11)`. Plan-completion checkboxes for 03-10/03-11 follow the executor's normal roadmap update.

Commit the three files. The output tells the user: "Re-run `/gsd-execute-phase 3` to continue with 03-02 (the current run computed blocked_by before the flip)."
  </action>
  <verify>
    <automated>test "$(sed -n '/^---$/,/^---$/p' .planning/phases/03-multi-module-architecture-extraction/03-01-SUMMARY.md | grep -c '^status: complete$')" = "1" && grep -q '^## Halt Resolution (03-10 + 03-11)' .planning/phases/03-multi-module-architecture-extraction/03-01-SUMMARY.md && node .claude/gsd-core/bin/gsd-tools.cjs query phase-plan-index 3 | jq -e '([.plans[] | select(.id=="03-01") | .halted] == [false]) and ([.plans[] | .blocked_by[]] | index("03-01") | not)' > /dev/null && echo "HALT-RESOLVED"</automated>
    <fails_when>HALT-RESOLVED missing: 03-01-SUMMARY frontmatter lacks exactly one "status: complete" line, the Halt Resolution section is absent, phase-plan-index still reports 03-01 halted: true, or some plan still lists 03-01 in blocked_by (jq -e exits non-zero)</fails_when>
  </verify>
  <acceptance_criteria>
    - `grep -n '^status: halted' .planning/phases/03-multi-module-architecture-extraction/03-01-SUMMARY.md` prints nothing; `grep -c 'SWIFT-NAMES-OK' .planning/phases/03-multi-module-architecture-extraction/03-01-SUMMARY.md` is at least 2 (original D3 entry + new one).
    - The D4 coverage entry contains `03-11 Task 1` and `status: pass`.
    - With `S=$(git log -1 --format=%H -- .planning/phases/03-multi-module-architecture-extraction/03-01-SUMMARY.md)`: `D=$(git diff "$S^" "$S" -- .planning/phases/03-multi-module-architecture-extraction/03-01-SUMMARY.md) && printf '%s\n' "$D" | grep -E '^-' | grep -vE '^---' | grep -vE '^-(status:|    rationale:|    verification: \[\])'` prints nothing. Only the status line, the D3/D4 rationales and D4's empty verification list were removed; body history is intact.
    - `.planning/STATE.md` contains `03-01 halt resolved` in Decisions and the BLOCKING Phase 3 entry is marked `RESOLVED` (or replaced by a resolved note).
    - `.planning/ROADMAP.md` 03-01 line starts with `- [x] 03-01-PLAN.md` and contains `halt resolved by 03-10 + 03-11`.
    - `HALT-RESOLVED` printed.
  </acceptance_criteria>
  <done>03-01 is re-summarized as complete with its D3/D4 evidence pointing at 03-10/03-11, the plan index no longer blocks 03-02..03-09, STATE/ROADMAP agree, and the user is told to re-run /gsd-execute-phase 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human approval -> halt flip | A status flip unblocks eight module-moving plans |
| SUMMARY frontmatter -> gsd-tools plan index | The `status` field drives blocked_by for every dependent plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-11-01 | Elevation of Privilege | 03-01-SUMMARY status flip | medium | mitigate | Task 2 carries a precondition on Task 1's "approved" signal; checkpoint is gate="blocking"; failure path keeps status: halted |
| T-03-11-02 | Repudiation | 03-01-SUMMARY history | low | mitigate | Acceptance diff check allows removal of only the status line and the D3/D4 rationale/empty-verification lines; original failing evidence and body sections remain |
| T-03-11-SC | Tampering | package installs | low | accept | Documentation-only plan; no dependency changes |
</threat_model>

<verification>
- `GATE-OK` and human "approved" (Task 1).
- `HALT-RESOLVED` (Task 2).

Edge coverage: ARCH-04 unclassified is covered by the human simulator smoke (judgment) plus 03-10's flagged assumption FA-10-1. No other probe row applies to this plan.
</verification>

<success_criteria>
- D-12 checkpoint 1 approved on the fixed framework.
- 03-01 re-summarized as complete; the plan index unblocks 03-02..03-09 on the next execute run.
- STATE.md and ROADMAP.md agree with the plan index.
</success_criteria>

## Artifacts this phase produces (this plan)

- No code symbols.
- Document changes: `03-01-SUMMARY.md` (`status: complete`, section `## Halt Resolution (03-10 + 03-11)`, D3/D4 coverage evidence), STATE.md Decisions bullet `03-01 halt resolved`, ROADMAP 03-01 line `halt resolved by 03-10 + 03-11`.

<output>
Create `.planning/phases/03-multi-module-architecture-extraction/03-11-SUMMARY.md` when done. Include the simulator smoke result, the phase-plan-index before/after for 03-01 and 03-02, and the resume instruction: re-run `/gsd-execute-phase 3`.
</output>
