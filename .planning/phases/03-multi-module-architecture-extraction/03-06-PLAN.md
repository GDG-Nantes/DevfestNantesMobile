---
phase: 03-multi-module-architecture-extraction
plan: 06
type: execute
wave: 7
depends_on: ["03-05"]
files_modified:
  - settings.gradle.dcl
  - feature/venue/build.gradle.kts
  - feature/venue/src/main/AndroidManifest.xml
  - feature/venue/src/main/java/** (moved venue screens, VenueViewModel, VenueFloorPlan, LocalUtils, NavigationUtils; new VenueRoute.kt)
  - feature/venue/src/main/res/** (venue-only resources)
  - feature/about/build.gradle.kts
  - feature/about/src/main/AndroidManifest.xml
  - feature/about/src/main/java/** (moved About*, partners/*, GithubCard; new AboutRoute.kt)
  - feature/about/src/main/res/** (about-only resources)
  - core/ui/src/main/res/** (resources that become multi-module)
  - androidApp/build.gradle.kts
  - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/Home.kt
  - androidApp/src/main/java/** (import updates only)
  - androidApp/src/main/res/** (resources leaving the app)
autonomous: true
requirements: [ARCH-02, ARCH-03]

estimate:
  tokens: 120000
  raw_tokens: 120000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - ":feature:venue exists (D-05, D-14) with VenueViewModel + Venue/VenueDetails/VenueFloorPlan Compose screens, package com.gdgnantes.devfest.feature.venue, and exposes the public entry point VenueRoute taking only callbacks (D-07)"
    - ":feature:about exists (D-05) with the About tab (About, AboutHeader, AboutLinks, AboutLocalCommunities, AboutSocial, AboutVersion, GithubCard) and Partners (PartnersViewModel, Partners, PartnerCard), package com.gdgnantes.devfest.feature.about, entry point AboutRoute taking callbacks plus versionName/versionCode supplied by :androidApp"
    - "No feature module references any com.gdgnantes.devfest.androidapp symbol (app R, BuildConfig, Screen routes) and no Navigation Compose controller type appears in feature code (D-07)"
    - "Each feature build file applies only id(\"devfest.android.feature\") plus module-specific dependencies; no feature depends on another feature and no core module depends on a feature (ARCH-02, ARCH-01)"
    - ":androidApp's Home.kt nested NavHost calls VenueRoute/AboutRoute; route strings and arguments are unchanged (D-07)"
    - "Resources follow the ownership rule (D-17) and resources-gate.sh prints RESOURCES-OK (translations byte-identical, no duplicate names — ARCH-03 adjacency edge)"
    - "Each feature lands as two green commits (move with only compile-enabling edits, then repackage + Route entry point); both CI workflows green on pushed HEAD (D-16, D-18, ARCH-02 concurrency edge)"
  artifacts:
    - path: feature/venue/build.gradle.kts
      provides: "devfest.android.feature leaf"
    - path: feature/venue/src/main/java/com/gdgnantes/devfest/feature/venue/VenueRoute.kt
      provides: "@Composable fun VenueRoute(...)"
    - path: feature/about/build.gradle.kts
      provides: "devfest.android.feature leaf"
    - path: feature/about/src/main/java/com/gdgnantes/devfest/feature/about/AboutRoute.kt
      provides: "@Composable fun AboutRoute(..., versionName: String, versionCode: Int)"
  key_links:
    - from: androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/Home.kt
      to: feature/venue VenueRoute, feature/about AboutRoute
      via: "composable(Screen.Venue.route) { VenueRoute(...) } / composable(Screen.About.route) { AboutRoute(...) }"
      pattern: "VenueRoute\\(|AboutRoute\\("
    - from: androidApp/build.gradle.kts
      to: feature/venue, feature/about
      via: "implementation(project(\":feature:venue\")), implementation(project(\":feature:about\"))"
      pattern: "project\\(\":feature:(venue|about)\"\\)"
  prohibitions:
    - requirement_id: ARCH-03
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT ship any user-visible behavior change while moving the venue and about screens (same texts, links, analytics events, version display)"
---

<objective>
Extract the two least-coupled features first (D-18 order: venue -> about): `:feature:venue` and `:feature:about` (About + Partners, D-05), each exposing a callback-only `*Route` entry point (D-07) called from `:androidApp`'s Home shell.

Purpose: prove the feature-module pattern (convention plugin, Hilt ViewModel in a library, own R, callbacks-only boundary) on the simplest screens.
Output: two feature modules, Home.kt wired through Route entry points, green CI.
</objective>

<execution_context>
@/Users/robin/Development/Android/DevFestNantes/.claude/gsd-core/workflows/execute-plan.md
@/Users/robin/Development/Android/DevFestNantes/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-multi-module-architecture-extraction/03-CONTEXT.md
@.planning/phases/03-multi-module-architecture-extraction/03-05-PLAN.md
@.planning/phases/03-multi-module-architecture-extraction/03-05-SUMMARY.md
@androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/Home.kt

## Feature extraction recipe (applies to 03-06, 03-07, 03-08)

- Files per feature come from the module map in 03-05-PLAN.md (authoritative).
- **Commit 1 (move, packages unchanged, D-16):** `include(":feature:<name>")` in `settings.gradle.dcl`; `git mv` the mapped files to the same relative path under `feature/<name>/src/main/java/`; create `feature/<name>/build.gradle.kts` = `plugins { id("devfest.android.feature") }` + only module-specific dependencies from existing catalog aliases; minimal `src/main/AndroidManifest.xml` (`<manifest />`); `implementation(project(":feature:<name>"))` in `androidApp/build.gradle.kts`; move resources per the 03-05 ownership rule (feature-only -> feature, newly multi-module -> :core:ui). The ONLY code edits allowed in this commit are the ones needed to compile across the new boundary: (a) R-class imports (feature's own R `com.gdgnantes.devfest.feature.<name>.R`, `com.gdgnantes.devfest.core.ui.R`), (b) app `BuildConfig` values lifted into parameters supplied by `:androidApp`, (c) references to the app's `Screen` object replaced by the exact resource id `Screen.X.<field>` resolved to.
- **Commit 2 (repackage + entry point, D-15/D-07):** packages `com.gdgnantes.devfest.androidapp.ui.screens.<x>` -> `com.gdgnantes.devfest.feature.<name>` (sub-folders kept as sub-packages; `androidapp.utils` files -> `com.gdgnantes.devfest.feature.<name>.utils`; `androidapp.ui.components` files -> `com.gdgnantes.devfest.feature.<name>.components`; `androidapp.services` files -> `com.gdgnantes.devfest.feature.<name>.services`); add `<Name>Route.kt` in the feature root package with a public `@Composable fun <Name>Route(...)` whose parameters are exactly the callbacks/state the app passes today (plus lifted BuildConfig values) and whose body delegates to the existing composable unchanged; switch the app call site to the Route; update imports. No other code change.
- Gates after each commit: `./gradlew --no-daemon :feature:<name>:assembleDebug :androidApp:assembleDebug :androidApp:assembleRelease :androidApp:testDebugUnitTest detekt lint` + `resources-gate.sh`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract :feature:venue (move, then repackage + VenueRoute)</name>
  <files>settings.gradle.dcl, feature/venue/build.gradle.kts, feature/venue/src/main/AndroidManifest.xml, feature/venue/src/main/java/com/gdgnantes/devfest/feature/venue/Venue.kt, feature/venue/src/main/java/com/gdgnantes/devfest/feature/venue/VenueDetails.kt, feature/venue/src/main/java/com/gdgnantes/devfest/feature/venue/VenueViewModel.kt, feature/venue/src/main/java/com/gdgnantes/devfest/feature/venue/VenueRoute.kt, feature/venue/src/main/java/com/gdgnantes/devfest/feature/venue/plan/VenueFloorPlan.kt, feature/venue/src/main/java/com/gdgnantes/devfest/feature/venue/utils/LocalUtils.kt, feature/venue/src/main/java/com/gdgnantes/devfest/feature/venue/utils/NavigationUtils.kt, feature/venue/src/main/res/**, core/ui/src/main/res/**, androidApp/build.gradle.kts, androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/Home.kt, androidApp/src/main/res/**</files>
  <read_first>
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/venue/Venue.kt, VenueDetails.kt, VenueViewModel.kt, venue/plan/VenueFloorPlan.kt
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/utils/LocalUtils.kt, NavigationUtils.kt
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/Home.kt (the composable(Screen.Venue.route) call site)
    - build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/AndroidFeatureConventionPlugin.kt
    - .planning/phases/03-multi-module-architecture-extraction/resources-gate.sh
  </read_first>
  <action>Apply the feature extraction recipe with `<name>` = `venue` and the :feature:venue row of the module map (6 files). Package targets in commit 2: `ui.screens.venue` -> `com.gdgnantes.devfest.feature.venue`, `ui.screens.venue.plan` -> `com.gdgnantes.devfest.feature.venue.plan`, `utils` (LocalUtils, NavigationUtils) -> `com.gdgnantes.devfest.feature.venue.utils`. `VenueRoute` parameters = the exact callbacks Home.kt passes to `Venue(...)` today (e.g. `onNavigationClick`, `onVenuePlanClick` — read the call site); Home.kt calls `VenueRoute(...)`. Add module-specific deps only as compilation requires (e.g. `libs.coil.compose` if the floor plan/venue image uses it). Two commits.</action>
  <verify>
    <automated>APP=$(git grep -n -E 'com\.gdgnantes\.devfest\.androidapp|NavController' -- 'feature/venue/*.kt') ; test -z "$APP" && ./gradlew --no-daemon :feature:venue:assembleDebug :androidApp:assembleDebug :androidApp:assembleRelease :androidApp:testDebugUnitTest detekt lint && bash .planning/phases/03-multi-module-architecture-extraction/resources-gate.sh</automated>
    <fails_when>non-zero exit (feature still references an androidapp symbol or a navigation controller, build/lint failure), or RESOURCES-DRIFT</fails_when>
  </verify>
  <acceptance_criteria>
    - `feature/venue/build.gradle.kts` contains `id("devfest.android.feature")` and none of `compileSdk`, `minSdk`, `detekt {`, `:feature:`.
    - `git grep -n 'fun VenueRoute(' -- feature/venue` returns one match; `git grep -n 'VenueRoute(' -- androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/Home.kt` returns a match.
    - `git ls-files androidApp/src/main/java | grep -E '/screens/venue/|/utils/(LocalUtils|NavigationUtils)\.kt'` prints nothing.
    - `./gradlew :feature:venue:dependencies --configuration releaseRuntimeClasspath` contains no `project :feature:`.
    - Move commit changes no `package` line; repackage commit's Kotlin diff contains only `package`/`import` lines plus the new `VenueRoute.kt` and the Home.kt call-site switch.
    - `RESOURCES-OK` printed.
  </acceptance_criteria>
  <done>:feature:venue builds as a leaf, the Venue tab renders through VenueRoute from the app shell; two commits.</done>
</task>

<task type="auto">
  <name>Task 2: Extract :feature:about (About + Partners; version values lifted to params), push, CI green</name>
  <files>settings.gradle.dcl, feature/about/build.gradle.kts, feature/about/src/main/AndroidManifest.xml, feature/about/src/main/java/com/gdgnantes/devfest/feature/about/*.kt, feature/about/src/main/java/com/gdgnantes/devfest/feature/about/AboutRoute.kt, feature/about/src/main/java/com/gdgnantes/devfest/feature/about/partners/*.kt, feature/about/src/main/java/com/gdgnantes/devfest/feature/about/components/GithubCard.kt, feature/about/src/main/res/**, core/ui/src/main/res/**, androidApp/build.gradle.kts, androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/Home.kt, androidApp/src/main/res/**</files>
  <read_first>
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/about/About.kt and AboutVersion.kt (BuildConfig.VERSION_NAME / VERSION_CODE usage)
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/about/partners/Partners.kt, PartnerCard.kt, PartnersViewModel.kt
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/components/GithubCard.kt
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/Home.kt (composable(Screen.About.route) call site)
    - androidApp/src/main/res/drawable/github.xml and strings used by About*
  </read_first>
  <action>Apply the feature extraction recipe with `<name>` = `about` and the :feature:about row of the module map (10 files, D-05). Commit 1's compile-enabling edit: `AboutVersion` no longer reads the app's BuildConfig — it takes `versionName: String` and `versionCode: Int` parameters threaded from `About(...)`, and Home.kt passes `BuildConfig.VERSION_NAME` / `BuildConfig.VERSION_CODE` (same displayed values). Package targets in commit 2: `ui.screens.about` -> `com.gdgnantes.devfest.feature.about`, `ui.screens.about.partners` -> `com.gdgnantes.devfest.feature.about.partners`, `ui.components.GithubCard` -> `com.gdgnantes.devfest.feature.about.components`. `AboutRoute` parameters = the callbacks Home.kt passes to `About(...)` today + `versionName`, `versionCode`. Two commits, then push and run the CI gate.</action>
  <verify>
    <automated>APP=$(git grep -n -E 'com\.gdgnantes\.devfest\.androidapp|NavController|BuildConfig' -- 'feature/about/*.kt') ; test -z "$APP" && ./gradlew --no-daemon :feature:about:assembleDebug :feature:venue:assembleDebug :androidApp:assembleDebug :androidApp:assembleRelease :androidApp:testDebugUnitTest detekt lint && bash .planning/phases/03-multi-module-architecture-extraction/resources-gate.sh</automated>
    <fails_when>non-zero exit (feature references an androidapp symbol, BuildConfig or a navigation controller; build/lint failure), or RESOURCES-DRIFT</fails_when>
    <automated>SHA=$(git rev-parse HEAD) || exit 1; BR=$(git branch --show-current) || exit 1; test -n "$SHA" -a -n "$BR" || { echo "NO-GIT-CONTEXT"; exit 1; }; for WF in android.yml ios.yml; do RID=$(gh run list --workflow "$WF" --branch "$BR" --limit 20 --json databaseId,headSha --jq "[.[]|select(.headSha==\"$SHA\")][0].databaseId"); test -n "$RID" || { echo "NO-RUN $WF $SHA"; exit 1; }; gh run watch "$RID" --exit-status || { echo "RED $WF $RID"; exit 1; }; done && echo "CI-GREEN-BOTH $SHA"</automated>
    <fails_when>non-zero exit, or output contains NO-RUN, RED or NO-GIT-CONTEXT instead of CI-GREEN-BOTH</fails_when>
  </verify>
  <acceptance_criteria>
    - `feature/about/build.gradle.kts` contains `id("devfest.android.feature")` and none of `compileSdk`, `minSdk`, `detekt {`, `:feature:`.
    - `git grep -n 'fun AboutRoute(' -- feature/about` returns one match whose parameter list contains `versionName` and `versionCode`; Home.kt calls `AboutRoute(` passing `BuildConfig.VERSION_NAME` and `BuildConfig.VERSION_CODE`.
    - `git ls-files androidApp/src/main/java | grep -E '/screens/about/|/components/GithubCard\.kt'` prints nothing; `PartnersViewModel.kt` is tracked under `feature/about` and still annotated `@HiltViewModel`.
    - `./gradlew :feature:about:dependencies --configuration releaseRuntimeClasspath` contains no `project :feature:`.
    - `RESOURCES-OK` and `CI-GREEN-BOTH` printed.
  </acceptance_criteria>
  <done>:feature:about builds as a leaf and renders About + Partners through AboutRoute; four commits across the plan; CI green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Feature module -> app shell | Only callbacks/values cross; no navigation or BuildConfig access from features |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-15 | Spoofing | External links opened from About/Venue (partner, social, maps URLs) | low | mitigate | Link opening stays in the app shell (ExternalContentService in :androidApp, callbacks only); URLs and callback wiring unchanged (repackage diff limited to package/import + Route) |
| T-03-16 | Information Disclosure | App BuildConfig (OpenFeedback secrets) reachable from features | medium | mitigate | Features never import the app BuildConfig (acceptance grep); only VERSION_NAME/VERSION_CODE are passed as plain values |
</threat_model>

<verification>
- Both feature leaf builds, app debug/release/unit/lint green; `RESOURCES-OK`; `CI-GREEN-BOTH`.
Edge coverage: ARCH-03 adjacency (resources gate; shared pieces already in core:ui), ARCH-02 concurrency.
</verification>

<success_criteria>
- :feature:venue and :feature:about exist with ViewModels + screens, callbacks-only entry points (ARCH-03, D-07); no feature->feature or core->feature edge (ARCH-02).
</success_criteria>

## Artifacts this phase produces (this plan)

- Gradle modules `:feature:venue` (package `com.gdgnantes.devfest.feature.venue`, `.plan`, `.utils`) and `:feature:about` (package `com.gdgnantes.devfest.feature.about`, `.partners`, `.components`)
- Functions: `com.gdgnantes.devfest.feature.venue.VenueRoute`, `com.gdgnantes.devfest.feature.about.AboutRoute`
- Files: `feature/venue/build.gradle.kts`, `feature/about/build.gradle.kts`, their `src/main/AndroidManifest.xml`

<output>
Create `.planning/phases/03-multi-module-architecture-extraction/03-06-SUMMARY.md` when done (list resources moved and the Route signatures).
</output>
