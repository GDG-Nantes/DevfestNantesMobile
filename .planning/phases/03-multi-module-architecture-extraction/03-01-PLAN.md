---
phase: 03-multi-module-architecture-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - build-logic/settings.gradle.kts
  - build-logic/convention/build.gradle.kts
  - build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/AndroidSdk.kt
  - build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/ProjectExtensions.kt
  - build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/DetektConventionPlugin.kt
  - build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/KmpLibraryConventionPlugin.kt
  - settings.gradle.dcl
  - build.gradle.kts
  - gradle/libs.versions.toml
  - core/model/build.gradle.kts
  - core/model/src/commonMain/kotlin/** (moved from shared/src/commonMain/kotlin/com/gdgnantes/devfest/model/** and .../utils/SessionExtensions.kt)
  - core/model/src/commonTest/kotlin/** (moved ScheduleSlotDateParsingTest.kt)
  - shared/build.gradle.kts
  - shared/src/commonMain/kotlin/** (import updates only)
  - shared/src/commonTest/kotlin/** (import updates only)
  - shared/src/jvmTest/kotlin/** (import updates only)
  - androidApp/src/main/java/** (import updates only)
  - androidApp/src/androidTest/java/** (import updates only)
  - .github/workflows/ios.yml
  - .planning/phases/03-multi-module-architecture-extraction/swift-names-gate.sh
  - .planning/phases/03-multi-module-architecture-extraction/03-shared-h-baseline.txt
  - .planning/phases/03-multi-module-architecture-extraction/03-swift-referenced-names.txt
  - .planning/STATE.md
autonomous: false
requirements: [ARCH-01, ARCH-02, ARCH-04]

estimate:
  tokens: 140000
  raw_tokens: 140000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "A build-logic included build (wired by includeBuild(\"build-logic\") inside pluginManagement of settings.gradle.dcl, per D-13/D-20) exposes plugin ids devfest.detekt and devfest.kmp.library, and reads the root gradle/libs.versions.toml as the libs catalog"
    - ":core:model exists at core/model (D-14), applies only id(\"devfest.kmp.library\") plus module-specific dependencies, and holds every domain model class, the model/stubs files and SessionExtensions.kt"
    - ":shared declares api(project(\":core:model\")) and export(project(\":core:model\")) inside the framework block of all three iOS targets, while keeping baseName = \"shared\", isStatic = true and the unchanged :shared:embedAndSignAppleFrameworkForXcode Xcode build phase (D-10, D-11)"
    - "Every Kotlin type name referenced from iosApp Swift sources is still present as a swift_name in the regenerated shared.h, and the count of collision-disambiguated names (swift_name ending in an underscore) equals the pre-phase baseline (ARCH-04 adjacency edge, D-16)"
    - "core:model code lives in package com.gdgnantes.devfest.core.model (and .stubs / .utils sub-packages) after the second commit, per D-15/D-16; the first commit is a pure git mv with packages unchanged"
    - "Every pushed commit on the phase branch builds green in isolation: the pure-move commit and the repackage commit each pass the local gate before the next begins, and both GitHub workflows (android.yml, ios.yml) are green on the pushed HEAD (ARCH-02 concurrency edge, D-18)"
    - "The iOS app runs on a simulator with agenda, speakers, venue and about rendering real data (D-12 checkpoint 1); if the umbrella export approach fails, execution stops here (D-09)"
    - "No DI framework other than Dagger Hilt is introduced (D-01)"
  artifacts:
    - path: build-logic/settings.gradle.kts
      provides: "included-build settings with versionCatalogs create(\"libs\") from ../gradle/libs.versions.toml"
    - path: build-logic/convention/build.gradle.kts
      provides: "kotlin-dsl build registering devfest.detekt and devfest.kmp.library"
    - path: build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/KmpLibraryConventionPlugin.kt
      provides: "KmpLibraryConventionPlugin : Plugin<Project>"
    - path: build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/DetektConventionPlugin.kt
      provides: "DetektConventionPlugin : Plugin<Project>"
    - path: build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/AndroidSdk.kt
      provides: "AndroidSdk constants (min 26 / compile 37 / target 36 + decoupling comment verbatim)"
    - path: core/model/build.gradle.kts
      provides: "tiny leaf build file applying devfest.kmp.library"
    - path: .planning/phases/03-multi-module-architecture-extraction/swift-names-gate.sh
      provides: "baseline/check gate over shared.h swift_name set, reused by 03-02, 03-03, 03-09"
  key_links:
    - from: settings.gradle.dcl
      to: build-logic
      via: "pluginManagement { includeBuild(\"build-logic\") }"
      pattern: "includeBuild\\(\"build-logic\"\\)"
    - from: shared/build.gradle.kts
      to: core/model
      via: "api(project(\":core:model\")) + export(project(\":core:model\"))"
      pattern: "export\\(project\\(\":core:model\"\\)\\)"
    - from: core/model/build.gradle.kts
      to: build-logic/convention
      via: "plugins { id(\"devfest.kmp.library\") }"
      pattern: "devfest\\.kmp\\.library"
  prohibitions:
    - requirement_id: ARCH-04
      category: values
      status: resolved
      verification: judgment
      resolution: null
      reason: null
      statement: "MUST NOT ship any user-visible behavior change on Android or iOS while moving code (no drive-by fixes, no text/translation/route change) — the project's core value is zero regression for users"
---

<objective>
Tracer for the whole phase (D-09): prove the architecture end-to-end on the thinnest possible slice before any other module moves. Create the `build-logic` included build with the first two convention plugins (D-13), extract `core/model` as the first KMP leaf module (D-14), re-export it through the `:shared` umbrella so iOS still links ONE framework named `shared` (D-10, D-11), repackage it (D-15/D-16), and prove it with local builds, a Swift-visible-name gate on `shared.h`, both CI workflows green (D-18), and a manual iOS simulator run (D-12 checkpoint 1).

Purpose: the three-framework / umbrella-export problem is the highest architectural risk of the phase. If this tracer fails, stop and rethink (D-09) — nothing else moves.
Output: `build-logic/` (devfest.detekt, devfest.kmp.library), `:core:model`, updated `:shared` umbrella wiring, the Swift-name gate script + baseline, widened iOS CI cache keys, a draft phase PR with green CI.
</objective>

<execution_context>
@/Users/robin/Development/Android/DevFestNantes/.claude/gsd-core/workflows/execute-plan.md
@/Users/robin/Development/Android/DevFestNantes/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-multi-module-architecture-extraction/03-CONTEXT.md
@.planning/phases/03-multi-module-architecture-extraction/03-RESEARCH.md
@.planning/phases/03-multi-module-architecture-extraction/03-PATTERNS.md
@shared/build.gradle.kts
@settings.gradle.dcl
@build.gradle.kts
@buildSrc/src/main/java/Dependencies.kt
@gradle/libs.versions.toml

## Decisions this plan records (planner discretion, applied phase-wide)

- **Package scheme (D-15):** `com.gdgnantes.devfest.<module path with ':' -> '.' and '-' removed>` — `:core:model` -> `com.gdgnantes.devfest.core.model`, `:feature:session-detail` -> `com.gdgnantes.devfest.feature.sessiondetail`. Android `namespace` uses the same string (unique per module, D-14).
- **KMP target set:** every KMP core module declares the SAME targets as today's `:shared` (android, jvm, iosX64, iosArm64, iosSimulatorArm64). Reason: a KMP module that declares `jvm()` cannot depend on a KMP module lacking `jvm()`; `:shared` (jvm today) now depends on `:core:model`. `jvm()` hosts `commonTest` execution via `jvmTest`. Core modules declare iOS targets but NO framework binaries — `:shared` is the only module declaring `binaries.framework` (anti three-framework).
- **CI test coverage parity (RESEARCH Open Question 2):** `commonTest`/`jvmTest` of `:shared` were never executed by `android.yml` (it runs unqualified `testDebugUnitTest`, which KMP modules do not register). Parity is preserved: moved common tests keep running locally via `:core:<m>:jvmTest` in every plan's verify gate; no CI workflow task edit is needed. Task 1 records the actual verification task names of `:core:model` in the SUMMARY.
- **Tests follow code:** `ScheduleSlotDateParsingTest` (commonTest, subject `ScheduleSlot`) moves to `core/model/src/commonTest`.
- **SessionExtensions.kt** (`com.gdgnantes.devfest.utils`, pure `Session` extensions used by Android UI only) moves with the model into `:core:model` (package `com.gdgnantes.devfest.core.model.utils` after repackage).
- **AndroidSdk interim:** `build-logic` gets its own `com.gdgnantes.devfest.buildlogic.AndroidSdk` (distinct FQN, no clash with buildSrc's root-package `AndroidSdk`); `buildSrc` is deleted last per D-18 (plan 03-09), where the remaining consumers are repointed.
- **Stubs stay in production code (D-19 verification):** `model/stubs/*` are consumed by `DevFestNantesStoreMocked`, which production `DevFestNantesStoreBuilder` instantiates (and Swift `BaseViewModel` calls `DevFestNantesStoreBuilder()`), and by Compose `@Preview`s in production source sets — so they stay in `:core:model` main code, not `:core:testing`.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — build-logic + core/model pure move + umbrella export, one path end-to-end</name>
  <files>.planning/phases/03-multi-module-architecture-extraction/swift-names-gate.sh, .planning/phases/03-multi-module-architecture-extraction/03-shared-h-baseline.txt, .planning/phases/03-multi-module-architecture-extraction/03-swift-referenced-names.txt, build-logic/settings.gradle.kts, build-logic/convention/build.gradle.kts, build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/AndroidSdk.kt, build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/ProjectExtensions.kt, build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/DetektConventionPlugin.kt, build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/KmpLibraryConventionPlugin.kt, settings.gradle.dcl, build.gradle.kts, gradle/libs.versions.toml, core/model/build.gradle.kts, core/model/src/commonMain/kotlin/com/gdgnantes/devfest/model/*.kt, core/model/src/commonMain/kotlin/com/gdgnantes/devfest/model/stubs/*.kt, core/model/src/commonMain/kotlin/com/gdgnantes/devfest/utils/SessionExtensions.kt, core/model/src/commonTest/kotlin/com/gdgnantes/devfest/model/ScheduleSlotDateParsingTest.kt, shared/build.gradle.kts, .github/workflows/ios.yml</files>
  <read_first>
    - shared/build.gradle.kts (full — the KMP plugin/target/detekt block the convention plugin replicates)
    - androidApp/build.gradle.kts lines 52-63 (the duplicated detekt block + jvmTarget "1.8" Detekt task config)
    - settings.gradle.dcl (full — DCL settings you extend)
    - build.gradle.kts (root apply-false plugin list)
    - buildSrc/src/main/java/Dependencies.kt (AndroidSdk values + comment to copy verbatim)
    - buildSrc/settings.gradle.kts (existing catalog-from-file pattern to mirror in build-logic/settings.gradle.kts)
    - gradle/libs.versions.toml (plugin aliases and versions: agp, kotlin, ksp, detekt)
    - .github/workflows/ios.yml lines 32-41 and 69-78 (cache keys to widen)
    - .planning/phases/03-multi-module-architecture-extraction/03-RESEARCH.md sections "Pattern 3", "Pitfall 1", "Pitfall 2", "Pitfall 5", "Code Examples"
    - .planning/STATE.md section "Phase 02 DCL pilot outcome"
  </read_first>
  <action>
Step 0 — Swift-name baseline (MUST run on the current, untouched HEAD before any build file changes; D-16):
Create `.planning/phases/03-multi-module-architecture-extraction/swift-names-gate.sh` (bash, `set -euo pipefail`) with two modes. Both modes first run `./gradlew --no-daemon -q :shared:linkDebugFrameworkIosSimulatorArm64` and read the header `shared/build/bin/iosSimulatorArm64/debugFramework/shared.framework/Headers/shared.h`, extracting the sorted unique set of names inside `swift_name("...")` attributes. Mode `baseline` writes that set to `03-shared-h-baseline.txt`, then computes the intersection (`comm -12`) of the baseline set with the sorted unique set of capitalized identifiers found in all git-tracked `iosApp/**/*.swift` files (use `git ls-files -z` + `xargs -0` because one iosApp folder name contains a space) and writes it to `03-swift-referenced-names.txt`. Mode `check` regenerates the current set and fails (non-zero exit, message `SWIFT-NAME-MISSING <name>`) if any name from `03-swift-referenced-names.txt` is absent, fails (message `SWIFT-NAME-COLLISION`) if the count of names ending in `_` differs from the baseline count, prints the `comm -3` diff of baseline vs current (informational, to be classified in the SUMMARY), and prints `SWIFT-NAMES-OK` on success. Run `bash <script> baseline` now and confirm `03-swift-referenced-names.txt` contains at least `Session`, `Speaker`, `DevFestNantesStore`, `DevFestNantesStoreBuilder`, `AnalyticsService`.

Step 1 — build-logic included build (D-13, D-20). Create:
- `build-logic/settings.gradle.kts` (plain Kotlin DSL, NOT .dcl — RESEARCH Pitfall 1/2): `dependencyResolutionManagement { repositories { google(); mavenCentral(); gradlePluginPortal() }; versionCatalogs { create("libs") { from(files("../gradle/libs.versions.toml")) } } }`, `rootProject.name = "build-logic"`, `include(":convention")`.
- `gradle/libs.versions.toml`: add `[libraries]` entries for compileOnly plugin artifacts, all on EXISTING version refs (no version change): `android-gradlePlugin` (com.android.tools.build:gradle, ref agp), `kotlin-gradlePlugin` (org.jetbrains.kotlin:kotlin-gradle-plugin, ref kotlin), `detekt-gradlePlugin` (dev.detekt:detekt-gradle-plugin, ref detekt — confirm the exact artifact coordinate by running `./gradlew --no-daemon :shared:buildEnvironment` and reading the resolved detekt plugin GAV; use that coordinate).
- `build-logic/convention/build.gradle.kts`: `plugins { \`kotlin-dsl\` }`, `group = "com.gdgnantes.devfest.buildlogic"`, Java/Kotlin jvmTarget 17, `dependencies { compileOnly(libs.android.gradlePlugin); compileOnly(libs.kotlin.gradlePlugin); compileOnly(libs.detekt.gradlePlugin) }`, and `gradlePlugin { plugins { register("devfestDetekt") { id = "devfest.detekt"; implementationClass = "com.gdgnantes.devfest.buildlogic.DetektConventionPlugin" }; register("devfestKmpLibrary") { id = "devfest.kmp.library"; implementationClass = "com.gdgnantes.devfest.buildlogic.KmpLibraryConventionPlugin" } } }`.
- `AndroidSdk.kt` in package `com.gdgnantes.devfest.buildlogic`: `object AndroidSdk` with `min = 26`, `compile = 37`, `target = 36` and the 6-line targetSdk decoupling comment copied verbatim from buildSrc (D-13).
- `ProjectExtensions.kt`: `internal val Project.libs: VersionCatalog` via `extensions.getByType<VersionCatalogsExtension>().named("libs")`, plus a `Project.moduleNamespace()` helper returning `"com.gdgnantes.devfest." + path.removePrefix(":").replace(':', '.').replace("-", "")` (package/namespace scheme recorded in context).
- `DetektConventionPlugin` (`devfest.detekt`): applies plugin id `dev.detekt`; configures the detekt extension with `buildUponDefaultConfig = true`, `allRules = false`, `autoCorrect = false`, `config.setFrom(rootProject.file("linters/detekt-config.yml"))` (the single shared config — equivalent to today's `$rootDir/linters/detekt-config.yml`); adds `detektPlugins` dependency `libs.findLibrary("detekt-fomatting")`. Expose an internal helper so KMP modules set `source.setFrom("src/commonMain/kotlin", "src/androidMain/kotlin", "src/iosMain/kotlin")` (exactly the :shared set today) and Android modules (plan 03-04) keep default sources + Detekt/DetektCreateBaselineTask `jvmTarget = "1.8"` (exactly androidApp today).
- `KmpLibraryConventionPlugin` (`devfest.kmp.library`): applies `org.jetbrains.kotlin.multiplatform`, `com.android.kotlin.multiplatform.library`, `org.jetbrains.kotlin.plugin.serialization`, `devfest.detekt` (with the KMP source set list). Configures the KotlinMultiplatformExtension: android target (AGP 9 KMP android target type — RESEARCH A5 flags `com.android.build.api.dsl.KotlinMultiplatformAndroidLibraryTarget` as the probable type; confirm with `android docs search "kotlin multiplatform android library plugin"` before writing) with `namespace = moduleNamespace()`, `compileSdk = AndroidSdk.compile`, `minSdk = AndroidSdk.min`, `compilerOptions { jvmTarget.set(JvmTarget.JVM_11) }`; `jvm()`; `iosX64()`, `iosArm64()`, `iosSimulatorArm64()` with NO framework binaries; `sourceSets.all { languageSettings.optIn("kotlin.experimental.ExperimentalObjCName") }`; `commonTest` dependencies `libs.kotlin.test` and `libs.kotlinx.coroutines.test`. Do not add KSP, Apollo or native-coroutines here — those are per-module (core/network, core/data).
- Root `build.gradle.kts`: add `alias(libs.plugins.detekt) apply false` so the convention plugins' by-id application resolves from the root classloader (build-logic uses compileOnly, NIA pattern). Keep every existing line.
- `settings.gradle.dcl`: inside `pluginManagement { }` add `includeBuild("build-logic")` (before `repositories`), and add `include(":core:model")` after the existing includes. If Gradle 9.7.1 rejects `includeBuild` in the .dcl file, this is D-20's documented fallback: stop, record the exact error in the SUMMARY, and raise it to the user before converting the settings file (do not silently revert the Phase 2 DCL pilot).

Step 2 — core/model pure move (D-16 commit 1: packages UNCHANGED). With `git mv` only: `shared/src/commonMain/kotlin/com/gdgnantes/devfest/model/` (all 16 model files + `stubs/` 4 files) -> `core/model/src/commonMain/kotlin/com/gdgnantes/devfest/model/`; `shared/src/commonMain/kotlin/com/gdgnantes/devfest/utils/SessionExtensions.kt` -> `core/model/src/commonMain/kotlin/com/gdgnantes/devfest/utils/SessionExtensions.kt`; `shared/src/commonTest/kotlin/com/gdgnantes/devfest/model/ScheduleSlotDateParsingTest.kt` -> `core/model/src/commonTest/kotlin/com/gdgnantes/devfest/model/`. Create `core/model/build.gradle.kts` = `plugins { id("devfest.kmp.library") }` + `kotlin { sourceSets { commonMain.dependencies { api(libs.kotlinx.datetime); implementation(libs.kotlinx.serialization.json) } } }` (add only what compilation of the moved files requires). No AndroidManifest needed unless AGP demands one; if it does, use the empty `<manifest />` shape of `shared/src/androidMain/AndroidManifest.xml`.
In `shared/build.gradle.kts`: add `api(project(":core:model"))` to `commonMain.dependencies` and `export(project(":core:model"))` inside the existing `iosTarget.binaries.framework { }` lambda (it runs for all three iOS targets). Do not enable transitive export. Keep `baseName = "shared"`, `isStatic = true`, jvm(), apollo, detekt, ksp, native-coroutines exactly as they are (D-10).
`.github/workflows/ios.yml` (RESEARCH Pitfall 5): widen the KMP cache key hashFiles to `'shared/**/*.kt', 'shared/build.gradle.kts', 'core/**/*.kt', 'core/**/build.gradle.kts', 'build-logic/**/*.kt', 'build-logic/**/*.kts', 'gradle/libs.versions.toml'`, add `core/*/build` to that cache step's `path`, and widen the DerivedData key's second hashFiles to `'shared/**/*.kt', 'core/**/*.kt'`. Do not touch the `./gradlew :shared:embedAndSignAppleFrameworkForXcode` step (D-10).

Run the verify gate below; then commit twice: (a) build-logic + settings + root build + catalog + gate script/baseline files, (b) the core/model pure move + shared wiring + ios.yml. If the build cannot be green without a package change, the pure-move commit is wrong — fix it, never fold the repackage in. Run `./gradlew --no-daemon :core:model:tasks --group verification` and record the listed task names in the SUMMARY (Validation Wave 0 item). Append to `.planning/STATE.md` under "Phase 02 DCL pilot outcome" a 3-line "Phase 03 re-check" note: includeBuild in the .dcl root settings parsed on Gradle 9.7.1 (or the fallback outcome), build-logic settings + every new module build file stay Kotlin DSL per D-20 / RESEARCH Pitfall 2.
  </action>
  <verify>
    <automated>./gradlew --no-daemon projects > "${TMPDIR:-/tmp}/p3-projects.txt" && grep -q "Project ':core:model'" "${TMPDIR:-/tmp}/p3-projects.txt" && ./gradlew --no-daemon :core:model:jvmTest :core:model:compileKotlinIosSimulatorArm64 :shared:jvmTest :androidApp:assembleDebug detekt && bash .planning/phases/03-multi-module-architecture-extraction/swift-names-gate.sh check</automated>
    <fails_when>non-zero exit from any gradle task or the grep, or the final line is not SWIFT-NAMES-OK (a SWIFT-NAME-MISSING or SWIFT-NAME-COLLISION line is printed)</fails_when>
    <automated>LEFT=$(git ls-files shared/src/commonMain/kotlin/com/gdgnantes/devfest/model) || exit 1; STAT=$(git show --stat --format= HEAD) || exit 1; PKG=$(git show HEAD -- '*.kt') || exit 1; test -z "$LEFT" && printf '%s\n' "$STAT" | grep -q "=>" && ! printf '%s\n' "$PKG" | grep -qE '^[-+]package ' && echo "PURE-MOVE-OK"</automated>
    <fails_when>output lacks PURE-MOVE-OK — model files still tracked under shared/, the HEAD commit shows no rename entries (moves recorded as delete+add), or a package line changed in the move commit</fails_when>
  </verify>
  <acceptance_criteria>
    - `settings.gradle.dcl` contains `includeBuild("build-logic")` inside `pluginManagement` and `include(":core:model")`; `./gradlew projects` lists `:core:model`.
    - `build-logic/settings.gradle.kts` contains `from(files("../gradle/libs.versions.toml"))`; no `build-logic/**/*.dcl` file exists.
    - `build-logic/convention/build.gradle.kts` registers ids `devfest.detekt` and `devfest.kmp.library`.
    - `grep -vE '^[[:space:]]*//' build-logic/convention/src/main/kotlin/com/gdgnantes/devfest/buildlogic/AndroidSdk.kt` shows `min = 26`, `compile = 37`, `target = 36`, identical to buildSrc's values.
    - `core/model/build.gradle.kts` contains `id("devfest.kmp.library")` and contains none of the strings `compileSdk`, `minSdk`, `detekt {`, `jvmTarget` (ARCH-01: no duplicated config).
    - `shared/build.gradle.kts` contains `api(project(":core:model"))`, `export(project(":core:model"))`, `baseName = "shared"`, `isStatic = true`, and does not contain `transitiveExport`.
    - `git ls-files core/model/src/commonMain` lists 21 `.kt` files (16 model + 4 stubs + SessionExtensions) and `core/model/src/commonTest` lists `ScheduleSlotDateParsingTest.kt`; `:core:model:jvmTest` runs ScheduleSlotDateParsingTest green.
    - `.github/workflows/ios.yml` KMP cache key contains `core/**/*.kt` and still runs `./gradlew :shared:embedAndSignAppleFrameworkForXcode`.
    - `swift-names-gate.sh check` prints `SWIFT-NAMES-OK`; baseline and referenced-names files are committed.
    - The core/model commit's `git show --stat` shows renames (`=>`) and `git show HEAD -- '*.kt' | grep '^[-+]package '` prints nothing (packages unchanged in the move commit, D-16).
  </acceptance_criteria>
  <done>The tracer path exists end-to-end: build-logic plugin -> :core:model leaf -> :shared api/export -> shared.framework header with unchanged Swift-visible names; Android debug build, detekt and moved commonTest are green; two commits landed.</done>
</task>

<task type="auto">
  <name>Task 2: Repackage core/model to com.gdgnantes.devfest.core.model, push, both CI workflows green</name>
  <files>core/model/src/commonMain/kotlin/com/gdgnantes/devfest/core/model/**, core/model/src/commonTest/kotlin/com/gdgnantes/devfest/core/model/ScheduleSlotDateParsingTest.kt, shared/src/commonMain/kotlin/**, shared/src/commonTest/kotlin/**, shared/src/jvmTest/kotlin/**, androidApp/src/main/java/**, androidApp/src/androidTest/java/**</files>
  <read_first>
    - core/model/src/commonMain/kotlin/com/gdgnantes/devfest/model/Session.kt (package + import shape)
    - core/model/src/commonMain/kotlin/com/gdgnantes/devfest/utils/SessionExtensions.kt
    - shared/src/commonMain/kotlin/com/gdgnantes/devfest/store/graphql/Mappers.kt (heaviest model importer)
    - androidApp/src/main/java/com/gdgnantes/devfest/androidapp/ui/screens/agenda/AgendaRow.kt (Android importer of model + stubs + utils)
    - .planning/phases/03-multi-module-architecture-extraction/swift-names-gate.sh
  </read_first>
  <action>
D-16 commit 2 (package rename + import updates only, per D-15): with `git mv`, move `core/model/src/commonMain/kotlin/com/gdgnantes/devfest/model/` to `core/model/src/commonMain/kotlin/com/gdgnantes/devfest/core/model/` (stubs follow to `.../core/model/stubs/`), `.../devfest/utils/SessionExtensions.kt` to `.../devfest/core/model/utils/SessionExtensions.kt`, and the commonTest file to `core/model/src/commonTest/kotlin/com/gdgnantes/devfest/core/model/`. Change package declarations: `com.gdgnantes.devfest.model` -> `com.gdgnantes.devfest.core.model`, `com.gdgnantes.devfest.model.stubs` -> `com.gdgnantes.devfest.core.model.stubs`, `com.gdgnantes.devfest.utils` (SessionExtensions only) -> `com.gdgnantes.devfest.core.model.utils`. Update every import in `shared/src/**` and `androidApp/src/**` (main and androidTest) that referenced those packages. Edit ONLY `package`/`import` lines — no string literal, no code, no comment content changes (RESEARCH Pitfall 4). Swift sources need no change (Kotlin/Native drops the package prefix; the gate proves it). Kotlin/Native class names derive from class names and file names, both unchanged.
Run the verify gate. Commit. Then push the branch; if `gh pr list --head "$(git branch --show-current)" --state open` returns no PR, open one with `gh pr create --draft --base main --title "Phase 3: Multi-Module Architecture Extraction"` and a short body listing the D-18 step order (both workflows only run on pull_request to main). Wait for both workflows on the pushed HEAD with the CI gate command below. If iOS CI fails on Swift compilation, stop: that is the D-09 abort signal — record the failure, do not continue to other modules.
  </action>
  <verify>
    <automated>test -z "$(git grep -n -E 'com\.gdgnantes\.devfest\.model([.;[:space:]]|$)|import com\.gdgnantes\.devfest\.utils\.' -- '*.kt')" && ./gradlew --no-daemon :core:model:jvmTest :shared:jvmTest :androidApp:assembleDebug :androidApp:assembleRelease detekt lint && bash .planning/phases/03-multi-module-architecture-extraction/swift-names-gate.sh check</automated>
    <fails_when>non-zero exit (a leftover old-package reference makes the first test fail), or the last line is not SWIFT-NAMES-OK</fails_when>
    <automated>SHA=$(git rev-parse HEAD) || exit 1; BR=$(git branch --show-current) || exit 1; test -n "$SHA" -a -n "$BR" || { echo "NO-GIT-CONTEXT"; exit 1; }; for WF in android.yml ios.yml; do RID=$(gh run list --workflow "$WF" --branch "$BR" --limit 20 --json databaseId,headSha --jq "[.[]|select(.headSha==\"$SHA\")][0].databaseId"); test -n "$RID" || { echo "NO-RUN $WF $SHA"; exit 1; }; gh run watch "$RID" --exit-status || { echo "RED $WF $RID"; exit 1; }; done && echo "CI-GREEN-BOTH $SHA"</automated>
    <fails_when>non-zero exit, or output contains NO-RUN, RED or NO-GIT-CONTEXT instead of CI-GREEN-BOTH</fails_when>
  </verify>
  <acceptance_criteria>
    - Every file under `core/model/src/commonMain` declares a package starting with `com.gdgnantes.devfest.core.model`.
    - `git grep -n 'package com.gdgnantes.devfest.model' -- '*.kt'` prints nothing.
    - The repackage commit's diff (`git show HEAD -- '*.kt' | grep -E '^[-+]' | grep -vE '^(\+\+\+|---)'`) contains only `package` / `import` lines.
    - `swift-names-gate.sh check` prints `SWIFT-NAMES-OK`.
    - A PR from the phase branch to main exists (`gh pr view --json state` returns OPEN) and both android.yml and ios.yml runs for HEAD are green (`CI-GREEN-BOTH`).
  </acceptance_criteria>
  <done>core:model is repackaged, every importer compiles, Swift names unchanged, both CI workflows green on the pushed HEAD.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: D-12 checkpoint 1 — iOS simulator smoke on the umbrella framework</name>
  <files>none (verification only)</files>
  <read_first>
    - iosApp/iosApp/Model/BaseViewModel.swift (store construction the smoke exercises)
    - .planning/phases/03-multi-module-architecture-extraction/03-shared-h-baseline.txt
  </read_first>
  <action>Executor prepares: print the informational `comm -3` output of `swift-names-gate.sh check` (expected empty for this plan — only core:model, which is exported, moved) and the URLs of the two green CI runs. Then pause for the human to run the app on an iOS simulator from Xcode (`iosApp/iosApp.xcodeproj`, scheme `iosApp`; the Xcode build phase still calls `./gradlew :shared:embedAndSignAppleFrameworkForXcode`).</action>
  <what-built>The build-logic convention plugins and the first extracted KMP module (:core:model), re-exported through the single `shared` framework.</what-built>
  <how-to-verify>
    1. Open `iosApp/iosApp.xcodeproj` in Xcode, select an iPhone simulator, Run.
    2. Agenda tab: sessions list renders with data for both days.
    3. Speakers tab: speaker list renders with photos/names; open one speaker detail.
    4. Venue tab: venue details render.
    5. About tab: renders (links, partners).
    6. Confirm no Swift compile error mentioning a renamed/prefixed Kotlin type.
  </how-to-verify>
  <verify>
    <automated>bash .planning/phases/03-multi-module-architecture-extraction/swift-names-gate.sh check</automated>
    <fails_when>last line is not SWIFT-NAMES-OK</fails_when>
  </verify>
  <acceptance_criteria>
    - Human replies "approved" after steps 1-6 pass; any failure = "failed: <what>" and execution stops per D-09 (no further module extraction until the umbrella approach is rethought).
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe what failed</resume-signal>
  <done>User confirmed the iOS app renders agenda, speakers, venue and about with data on the simulator.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build-logic included build -> every module's configuration | Convention plugin code executes at configuration time for all modules (same trust as buildSrc today) |
| Kotlin umbrella framework -> Swift | Public Kotlin API surface exported to the iOS binary |
| CI cache -> iOS build | Restored Konan/DerivedData caches feed the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | build-logic/convention | low | accept | In-repo code reviewed in the phase PR; replaces buildSrc with identical trust level; no external plugin added (only compileOnly GAVs on existing catalog version refs) |
| T-03-02 | Information Disclosure | shared/build.gradle.kts export list | medium | mitigate | Only :core:model exported in this plan (D-11); acceptance forbids transitive export; swift-names-gate.sh diff classifies any new Swift-visible name |
| T-03-03 | Denial of Service | ios.yml cache keys | low | mitigate | hashFiles widened to core/** and build-logic/** so a stale cache cannot mask source changes (RESEARCH Pitfall 5) |
| T-03-SC | Tampering | gradle/libs.versions.toml new compileOnly entries | low | mitigate | Only Gradle plugin artifacts of the same group/version already applied via plugin aliases (agp, kotlin, detekt refs); no npm/pip/cargo installs, package-legitimacy gate not applicable |
</threat_model>

<verification>
- `./gradlew --no-daemon :core:model:jvmTest :shared:jvmTest :androidApp:assembleDebug :androidApp:assembleRelease detekt lint` green.
- `swift-names-gate.sh check` prints `SWIFT-NAMES-OK`.
- CI gate prints `CI-GREEN-BOTH <sha>`.
- Human approval of the iOS simulator smoke (D-12 checkpoint 1).

Edge-coverage (spec-less fallback) handled here: ARCH-02 concurrency (explicit truth: every pushed commit green), ARCH-04 adjacency (explicit truth: Swift-referenced names present + collision count unchanged). ARCH-01 `unclassified` edge stays unresolved and is surfaced as flagged assumption FA-1 below.

Flagged assumptions:
- FA-1 (ARCH-01, unclassified edge, unresolved): "no duplicated build configuration" is checked by string-absence greps in leaf build files (compileSdk/minSdk/detekt {/jvmTarget); duplication expressed in other forms (e.g. repeated dependency lists) is judged at code review, not mechanically.
- FA-2 (RESEARCH A5): the AGP 9.4.0 KMP android target type name used inside the convention plugin is confirmed at implementation time.
</verification>

<success_criteria>
- build-logic with devfest.detekt + devfest.kmp.library exists and is consumed by :core:model (ARCH-01 started).
- :core:model exists, depends on no feature module, and is exported by :shared (ARCH-02, ARCH-04).
- Swift-visible names unchanged; iOS CI and Android CI green; iOS simulator smoke approved.
</success_criteria>

## Artifacts this phase produces (this plan)

- Gradle included build `build-logic` with subproject `:convention` (group `com.gdgnantes.devfest.buildlogic`)
- Plugin ids: `devfest.detekt`, `devfest.kmp.library`
- Classes: `com.gdgnantes.devfest.buildlogic.DetektConventionPlugin`, `com.gdgnantes.devfest.buildlogic.KmpLibraryConventionPlugin`, `com.gdgnantes.devfest.buildlogic.AndroidSdk`
- Functions: `Project.libs` (internal extension), `Project.moduleNamespace()`
- Gradle module `:core:model` (namespace/package `com.gdgnantes.devfest.core.model`, sub-packages `.stubs`, `.utils`)
- Catalog library aliases: `android-gradlePlugin`, `kotlin-gradlePlugin`, `detekt-gradlePlugin`
- Files: `build-logic/settings.gradle.kts`, `build-logic/convention/build.gradle.kts`, `core/model/build.gradle.kts`, `.planning/phases/03-multi-module-architecture-extraction/swift-names-gate.sh`, `03-shared-h-baseline.txt`, `03-swift-referenced-names.txt`

<output>
Create `.planning/phases/03-multi-module-architecture-extraction/03-01-SUMMARY.md` when done (include: DCL includeBuild outcome, `:core:model:tasks --group verification` task names, swift-names-gate diff classification, CI run URLs, iOS smoke result).
</output>
