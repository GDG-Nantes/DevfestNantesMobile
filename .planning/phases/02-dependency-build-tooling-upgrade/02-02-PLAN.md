---
phase: 02-dependency-build-tooling-upgrade
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - gradle/wrapper/gradle-wrapper.properties
  - gradle/libs.versions.toml
  - build.gradle.kts
  - shared/build.gradle.kts
  - androidApp/build.gradle.kts
  - androidApp/proguard-rules.pro
autonomous: false
requirements: [BUILD-02, BUILD-03]

estimate:
  tokens: 75000
  raw_tokens: 75000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`./gradlew --version` reports Gradle 9.7.1 and the project builds on it (BUILD-03), landed as its own revertible commit before AGP moves — AGP 9.2.0's documented minimum Gradle is 9.4.1, so the wrapper is its prerequisite"
    - "`gradle/wrapper/gradle-wrapper.properties` carries a `distributionSha256Sum` for the 9.7.1 distribution, so the downloaded Gradle bytes are integrity-checked rather than trusted on transport alone"
    - "A human has explicitly verified the pre-1.0 `dev.detekt` 2.0.0-alpha release against its official registry page before CI's static-analysis step is pinned to it (02-RESEARCH.md § Package Legitimacy Audit, `[SUS]` verdict)"
    - "`shared` compiles as a KMP Android library through `com.android.kotlin.multiplatform.library`, with the `com.android.library` plugin no longer applied anywhere and no top-level `android { }` block left in `shared/build.gradle.kts` (BUILD-02)"
    - "Dagger Hilt is at or above 2.59 and the Firebase Performance Gradle plugin at or above 2.0.2 — AGP 9's documented compatibility floors — so `ksp`/`kspTest`/`kspAndroidTest` and the Perf instrumentation still run"
    - "`androidApp`'s resource-packaging exclusions are expressed as individual entries rather than a brace-expansion pattern, so release/debug APK packaging behaves identically under AGP 9's new DSL"
    - "All four `android.yml` jobs (checks, unit-tests, instrumentation-tests, build-debug) and `ios.yml` pass on the pushed AGP-9 commit (D-02, D-05: iOS needs CI-green only, no manual iOS pass)"
    - "The Android app still renders and navigates Agenda, Speakers, Venue and Bookmarks on a live device after the plugin swap (D-04 smoke pass)"
  artifacts:
    - path: "gradle/wrapper/gradle-wrapper.properties"
      provides: "Gradle 9.7.1 distribution pin with an integrity checksum"
      contains: "gradle-9.7.1"
    - path: "shared/build.gradle.kts"
      provides: "KMP Android target configured through the AGP 9 `com.android.kotlin.multiplatform.library` plugin inside `kotlin { android { … } }`"
      contains: "android.kotlin.multiplatform.library"
    - path: "androidApp/build.gradle.kts"
      provides: "AGP-9-compatible application config: new packaging DSL block, AGP-9-floor Hilt and Firebase-perf plugin aliases"
      contains: "packaging"
    - path: "gradle/libs.versions.toml"
      provides: "AGP 9.x, AGP-9-compatible Detekt plugin id/group, Hilt >= 2.59, firebase-perf >= 2.0.2, and the new KMP-library plugin alias"
      contains: "dev.detekt"
  key_links:
    - from: "gradle/wrapper/gradle-wrapper.properties"
      to: ".github/actions/android-setup/action.yml"
      via: "the composite action calls gradle/actions/setup-gradle@v4 with `gradle-version: wrapper`, so CI adopts the bumped distribution with zero CI-file edits"
      pattern: "gradle-version:\\s*wrapper"
    - from: "gradle/libs.versions.toml"
      to: "shared/build.gradle.kts"
      via: "alias(libs.plugins.android.kotlin.multiplatform.library) replaces the forbidden AGP-8-era library plugin alias"
      pattern: "alias\\(libs\\.plugins\\.android\\.kotlin\\.multiplatform\\.library\\)"
    - from: "build.gradle.kts"
      to: "shared/build.gradle.kts"
      via: "the root plugins block must declare the new KMP-library plugin `apply false` for the subproject alias to resolve"
      pattern: "android\\.kotlin\\.multiplatform\\.library.*apply false"
  prohibitions:
    - statement: "MUST NOT achieve a green CI signal by weakening the verification surface — no removing or soft-failing a job in android.yml/ios.yml, no deleting the Detekt or lint step, no adding a Detekt baseline to suppress new findings, no narrowing shared/build.gradle.kts's detekt `source.setFrom(...)` source sets"
      status: unverified
    - statement: "MUST NOT shrink the build surface to make the toolchain bump pass — `AndroidSdk.min`/`AndroidSdk.target`/`AndroidSdk.compile` keep their current values, the three iOS targets and the `jvm()` target stay declared, and no Firebase SDK is dropped"
      status: unverified
    - statement: "MUST NOT land a version pinned below its REQUIREMENTS.md target without recording the deviation and its reason — D-03 grants the escape valve and the documentation obligation together, never the first without the second"
      status: unverified
---

<objective>
Move the project onto Gradle 9.7.1 and then onto AGP 9.x's `com.android.kotlin.multiplatform.library` plugin, replacing the `kotlin.multiplatform` + `com.android.library` coexistence AGP 9 forbids — plus the three sibling plugin bumps and the one DSL syntax fix AGP 9 forces but BUILD-01…07 never names.

Purpose: this is the phase's highest-risk stage. 02-RESEARCH.md's central finding is that scoping work to BUILD-02's named packages alone makes this commit fail CI on `detekt`, `lint` and Hilt annotation processing for reasons that look unrelated to the bump being made — Detekt 1.23.8 has no stable release compatible with AGP 9's new DSL, Hilt 2.57.2 is below AGP 9's 2.59 floor, the Firebase Performance plugin 2.0.1 is one patch below its 2.0.2 floor, and `androidApp`'s brace-expansion packaging exclusion is unreliable under the new DSL. All four move in the same commit as AGP. Gradle 9.7.1 lands first, as its own commit, because AGP 9.2.0's documented minimum Gradle is 9.4.1.

Output: two revertible commits (Gradle wrapper; AGP 9 + forced siblings), both green on `android.yml` and `ios.yml`, with the AGP-9 build smoke-passed on a live device per D-04.
</objective>

<execution_context>
@/Users/robin/Development/Android/DevFestNantes/.claude/gsd-core/workflows/execute-plan.md
@/Users/robin/Development/Android/DevFestNantes/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dependency-build-tooling-upgrade/02-CONTEXT.md
@.planning/phases/02-dependency-build-tooling-upgrade/02-RESEARCH.md
@.planning/phases/02-dependency-build-tooling-upgrade/02-PATTERNS.md
@.planning/phases/02-dependency-build-tooling-upgrade/02-01-SUMMARY.md
@shared/build.gradle.kts
@androidApp/build.gradle.kts
@build.gradle.kts
@gradle/libs.versions.toml
@gradle/wrapper/gradle-wrapper.properties
@.github/actions/android-setup/action.yml
</context>

## Artifacts this phase produces

Symbols and files **created** by Phase 02 (none exist in the repo today — exclude them from drift/source-grounding verification):

| Artifact | Created by | Kind |
|---|---|---|
| `settings.gradle.dcl` (or `buildSrc/build.gradle.dcl` if the primary pilot is blocked) | 02-05 | new file |
| `shared/src/commonTest/kotlin/com/gdgnantes/devfest/model/ScheduleSlotDateParsingTest.kt` | 02-04 | new file |
| `appolloCache` — new `[versions]` key in `gradle/libs.versions.toml` | 02-03 | new catalog key |
| **`android-kotlin-multiplatform-library` — new `[plugins]` alias in `gradle/libs.versions.toml`** | **02-02 (this plan)** | new catalog key |
| **`kotlin { android { … } }` — new DSL block in `shared/build.gradle.kts`** | **02-02 (this plan)** | new config block |
| **`packaging { … }` — new DSL block in `androidApp/build.gradle.kts`** | **02-02 (this plan)** | new config block |
| **`distributionSha256Sum` — new key in `gradle/wrapper/gradle-wrapper.properties`** | **02-02 (this plan)** | new config key |
| `## Phase 02 version deviations` — new section in `.planning/STATE.md` | 02-05 | new doc section |

<assumptions>
## Flagged assumptions (spec-less phase — no SPEC.md exists)

Automated edge-coverage classification returned `category: unclassified`, `status: unresolved` for **all seven** of BUILD-01 … BUILD-07 — expected, not an error: the edge-probe engine classifies user-facing feature/CRUD requirements and version-bump/build-tooling requirements do not fit its patterns. Per the spec-less fallback protocol each unresolved row is surfaced as an explicit assumption rather than auto-resolved into `must_haves.truths`.

- **BUILD-01 … BUILD-07 (all 7):** automated edge-coverage classification did not apply. What grounds each requirement's acceptance criteria here is (a) the D-01 … D-09 locked decisions in `02-CONTEXT.md` and (b) the live-verified version numbers in `02-RESEARCH.md` § Version Verification. No edge-probe-derived predicate backs them.
- **BUILD-02, assumption A1:** that a Detekt 2.0.0-alpha (alpha.3+; alpha.6 was latest at research time) fixes AGP-9 compatibility without opt-out flags is sourced from GitHub issues via WebSearch, not an official detekt.dev release announcement. Confirm against the live build; D-03's escape valve covers the case where the chosen alpha still regresses.
- **BUILD-02, assumption A4:** that the new plugin's default Android manifest location is `src/androidMain/AndroidManifest.xml` (making the current explicit `srcFile(...)` override redundant) is unconfirmed. If wrong, re-declare the path in whatever the new DSL's equivalent location is rather than moving the file.
- **BUILD-02, not-applicable migration steps:** the generic AGP 9 migration guide's source-directory renames (`src/main` → `src/androidMain`, `src/test` → `src/androidHostTest`, `src/androidTest` → `src/androidDeviceTest`) do **not** apply — `shared/src` already contains only `androidMain`, `commonMain`, `commonTest`, `iosMain`, `jvmTest`, and no `androidUnitTest`/`androidInstrumentedTest` directories exist to rename. Do not perform them.
- **BUILD-03 placement:** D-01's staged list names Kotlin, AGP, Compose, Apollo, Firebase/kotlinx and the DCL pilot, but never names Gradle. It is placed here — its own commit, its own green gate, immediately before the AGP commit — because AGP 9.2.0's documented minimum Gradle is 9.4.1, making the wrapper AGP's hard prerequisite. This preserves D-01's one-group-per-commit discipline rather than deviating from it.
- **`kmp-native-coroutines`:** not analysed by 02-RESEARCH.md; handled as a Kotlin-coupled forced sibling in 02-01. If it breaks here instead, treat it the same way and record the version chosen.
</assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Gradle wrapper to 9.7.1 with an integrity checksum — AGP 9's prerequisite, its own commit</name>
  <reversibility rating="reversible">One properties file; `git revert` of this commit restores the prior distribution URL and the build runs on it again with no migration.</reversibility>
  <files>gradle/wrapper/gradle-wrapper.properties</files>
  <read_first>
    - `gradle/wrapper/gradle-wrapper.properties` — the file being edited; note it currently pins a `gradle-8.14.3-bin.zip` `distributionUrl` and has **no** `distributionSha256Sum` key, and that `validateDistributionUrl=true` is already set
    - `.github/actions/android-setup/action.yml` — confirms `gradle/actions/setup-gradle@v4` is invoked with `gradle-version: wrapper`, which is why this bump needs no CI-file edit
    - `.planning/phases/02-dependency-build-tooling-upgrade/02-RESEARCH.md` — § Version Verification (the Gradle row: 9.7.1 verified current stable, released 2026-08-19, exact match to BUILD-03) and § Environment Availability (the wrapper-auto-resolution row)
    - `gradle.properties` — `org.gradle.jvmargs` is set to `-Xmx2048M`; Gradle 9 is heavier than 8.x, so if the daemon OOMs during the AGP task in Task 3 this is the knob, and any change here must be recorded
  </read_first>
  <action>
    Implements BUILD-03. Own commit, own green gate, placed before AGP because AGP 9.2.0's documented minimum Gradle is 9.4.1.

    Re-verify 9.7.1 is still the current stable release before writing (`curl -s https://services.gradle.org/versions/current`), then perform the bump through Gradle's own wrapper task rather than by hand-editing the URL, so the wrapper jar and scripts stay consistent with the distribution and the checksum is written for you:

    `./gradlew wrapper --gradle-version 9.7.1 --distribution-type bin --gradle-distribution-sha256-sum <sha256 from https://gradle.org/release-checksums/ for gradle-9.7.1-bin.zip>`

    Fetch that checksum from `https://services.gradle.org/distributions/gradle-9.7.1-bin.zip.sha256` (or the release-checksums page) — do not invent it and do not omit the flag. Adding `distributionSha256Sum` is the mitigation for threat T-02-04 below: without it the distribution is trusted on transport alone.

    Run the wrapper task twice (Gradle's documented two-pass requirement: the first pass writes the new properties, the second regenerates the scripts and jar using the new version). Keep `distributionBase`, `distributionPath`, `zipStoreBase`, `zipStorePath`, `networkTimeout` and `validateDistributionUrl=true` as they are.

    Do not touch `gradle/libs.versions.toml` in this commit — `agp` moves in Task 3. If Gradle 9.7.1 will not build the project on the current AGP 8.13.0 (a plausible forward-compatibility limit), apply D-03: record the exact error in the SUMMARY and fold the wrapper bump into Task 3's commit so Gradle and AGP move together, noting that the two-commit separation was forced closed. Do not silently pin a lower Gradle.

    Commit as `build(02-02): bump Gradle wrapper to 9.7.1 with distribution checksum`, push, and drive both workflows to green.
  </action>
  <verify>
    <automated>grep -q 'gradle-9\.7\.1-bin\.zip' gradle/wrapper/gradle-wrapper.properties && grep -qE '^distributionSha256Sum=[0-9a-f]{64}$' gradle/wrapper/gradle-wrapper.properties && grep -q '^validateDistributionUrl=true$' gradle/wrapper/gradle-wrapper.properties && grep -qE '^distributionUrl=https\\?://services\.gradle\.org/' gradle/wrapper/gradle-wrapper.properties && echo "WRAPPER-PINNED-OK"</automated>
    <fails_when>non-zero exit, or the `WRAPPER-PINNED-OK` line is absent. A missing `gradle-9.7.1-bin.zip` means the bump did not land; a missing or malformed 64-hex `distributionSha256Sum` means the distribution is unverified (threat T-02-04 unmitigated); a `distributionUrl` host other than `services.gradle.org` means the distribution source was redirected.</fails_when>

    <automated>./gradlew --no-daemon --version | grep -qE '^Gradle +9\.7\.1$' && ./gradlew --no-daemon :shared:assemble :androidApp:assembleDebug :shared:jvmTest :androidApp:testDebugUnitTest</automated>
    <fails_when>non-zero exit; the absence of a `Gradle 9.7.1` line in `--version` output means the wrapper did not adopt the new distribution, and a `FAILURE: Build failed` / `Execution failed for task` line means AGP 8.13.0 does not run on Gradle 9.7.1 — which is the D-03 branch described in the action, not a reason to lower the Gradle pin.</fails_when>

    <automated>SHA=$(git rev-parse HEAD) || exit 1; BR=$(git branch --show-current) || exit 1; test -n "$SHA" -a -n "$BR" || { echo "NO-GIT-CONTEXT"; exit 1; }; for WF in android.yml ios.yml; do RID=$(gh run list --workflow "$WF" --branch "$BR" --limit 20 --json databaseId,headSha --jq "[.[]|select(.headSha==\"$SHA\")][0].databaseId"); test -n "$RID" || { echo "NO-RUN $WF $SHA"; exit 1; }; gh run watch "$RID" --exit-status || { echo "RED $WF $RID"; exit 1; }; done && echo "CI-GREEN-BOTH $SHA"</automated>
    <fails_when>non-zero exit, a `NO-RUN <workflow> <sha>` line, a `RED <workflow> <run-id>` line, or the absence of the final `CI-GREEN-BOTH <sha>` line. Reuses the `gh run list`/`gh run view` idiom Phase 01 proved; this is D-02's green gate for the Gradle group.</fails_when>
  </verify>
  <acceptance_criteria>
    - `gradle/wrapper/gradle-wrapper.properties` `distributionUrl` names `gradle-9.7.1-bin.zip` on the `services.gradle.org` host.
    - The same file contains a `distributionSha256Sum` whose value is exactly 64 lowercase hex characters, and it matches the published checksum for `gradle-9.7.1-bin.zip`.
    - `./gradlew --version` prints a line `Gradle 9.7.1`.
    - `./gradlew :shared:assemble :androidApp:assembleDebug :shared:jvmTest :androidApp:testDebugUnitTest` exits 0.
    - `gh run watch` exits 0 for the latest `android.yml` and `ios.yml` runs whose `headSha` equals this commit's SHA.
    - `git show --stat HEAD` lists only `gradle/wrapper/gradle-wrapper.properties`, `gradlew`, `gradlew.bat` and `gradle/wrapper/gradle-wrapper.jar` — the wrapper task's own outputs, and nothing from `gradle/libs.versions.toml`.
  </acceptance_criteria>
  <done>Gradle 9.7.1 is pinned with a verified distribution checksum as its own revertible commit; the project builds and unit-tests on it locally and both CI workflows are green on that commit.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Package-legitimacy gate: approve the pre-1.0 dev.detekt 2.0.0-alpha release before CI is pinned to it</name>
  <what-built>
    Nothing is installed or pinned yet — this gate runs *before* the Detekt bump in Task 3.

    Task 3 must move Detekt off 1.23.8, because 02-RESEARCH.md § Pitfall 1 found that no stable Detekt release is compatible with AGP 9's new DSL and built-in Kotlin support, and the alternative (setting `android.newDsl=false` / `android.builtInKotlin=false`) reverts the whole project off the very DSL that `com.android.kotlin.multiplatform.library` requires. The only forward path is Detekt's 2.0.0 alpha line, which also renames the plugin id and Maven group.

    02-RESEARCH.md § Package Legitimacy Audit returns a `[SUS]` verdict on that artifact — explicitly **not** a supply-chain trust concern (it is the official detekt org's own next major line, github.com/detekt/detekt), but a build-stability concern: pinning CI's static-analysis step to a pre-1.0 release makes CI-breaking regressions more likely than with a stable release. The `[SUS]` verdict is also the one LOW-confidence claim in the research: the specific alpha number came from WebSearch of GitHub issues, not an official detekt.dev announcement.

    Per the package-legitimacy protocol this gate is `blocking-human` and is never auto-approved — `workflow.auto_advance` and `workflow.human_verify_mode: end-of-phase` do not apply to it, because harvesting it at end-of-phase would place the verification after the pin it exists to gate.
  </what-built>
  <how-to-verify>
    1. Open the official registry page for the artifact and confirm it is the detekt org's own publication, not a look-alike: https://plugins.gradle.org/plugin/dev.detekt — check the publisher/source-repository link points at `github.com/detekt/detekt`.
    2. Open https://github.com/detekt/detekt/releases and note the newest `2.0.0-alpha.N` tag. Confirm from its release notes (or the linked issue) that AGP 9 / built-in-Kotlin compatibility is addressed in that alpha or an earlier one, and that the `dev.detekt` group/plugin-id rename is real.
    3. Confirm the Maven coordinate for the formatting ruleset exists under the new group: https://repo1.maven.org/maven2/dev/detekt/detekt-formatting/ should list the same alpha version.
    4. Decide whether you accept pinning CI's `Check Detekt` step to that pre-1.0 release for this phase, on the understanding that its findings are treated as advisory until Detekt reaches a stable 2.x.
  </how-to-verify>
  <resume-signal>Reply with the alpha version you approve (e.g. "approved: 2.0.0-alpha.6"), or "blocked" plus what you want done instead — if blocked, Task 3 halts rather than falling back to the DSL opt-out flags.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: AGP 9 KMP-library plugin swap plus the four bumps AGP 9 forces — one commit</name>
  <reversibility rating="costly">Undo is a single `git revert`, but the commit restructures `shared`'s entire Android configuration, renames the Detekt plugin id and group across three files, and moves two plugin versions to compatibility floors — so reverting it is a coordinated four-file change, not a one-line flip. No on-disk format changes and no published contract breaks, so this is not a one-way door: D-01's commit-level granularity remains the working reversal mechanism.</reversibility>
  <precondition>`android info` reports at least one connected device or running emulator — D-04's smoke pass installs and drives the debug app on a live target. If none is attached, start one with `android emulator start`; if that fails, halt rather than skipping the smoke pass, because CI green alone does not satisfy D-04.</precondition>
  <files>gradle/libs.versions.toml, build.gradle.kts, shared/build.gradle.kts, androidApp/build.gradle.kts, androidApp/proguard-rules.pro</files>
  <read_first>
    - `shared/build.gradle.kts` — the primary migration target. Note the current `plugins { }` block, the `detekt { }` block's `source.setFrom(...)` multiplatform source sets, the `kotlin { androidTarget { compilerOptions { jvmTarget.set(JvmTarget.JVM_11) } } }` shape, the `jvm()` and three iOS targets, the `apollo { service("service") { … } }` block (untouched here — 02-03 owns it), and the top-level `android { }` block with its `compileSdk`/`minSdk`/`namespace`/`compileOptions`/manifest-`srcFile` contents
    - `androidApp/build.gradle.kts` — note the two Detekt task-type imports at the top of the file, the `detekt { }` block plus the two `tasks.withType<…>().configureEach { jvmTarget = "1.8" }` blocks, the `plugins { }` block's Hilt and Firebase-perf aliases, the resource-packaging block, and that `compileOptions`/`kotlinOptions` already target Java 17 here
    - `build.gradle.kts` (root) — every plugin alias is declared `apply false` here; the AGP library alias must be replaced by the new KMP-library alias for the subproject `alias(...)` to resolve
    - `gradle/libs.versions.toml` — the `[versions]` keys `agp`, `detekt`, `dagger`, `firebasePerfPlugin`; the `[plugins]` entries for the AGP library plugin and Detekt; the `detekt-fomatting` `[libraries]` entry (note the existing typo in the key name — keep it, renaming it is out of scope)
    - `androidApp/proguard-rules.pro` — reviewed for AGP 9's stricter R8 defaults (Pitfall 6); `androidApp` sets `isMinifyEnabled`/`isShrinkResources` true on release
    - `.planning/phases/02-dependency-build-tooling-upgrade/02-RESEARCH.md` — § Pattern 1 (the full verified before/after for this exact file), § Common Pitfalls 1, 2, 5 and 6, and the cross-cutting version table's Detekt/Hilt/firebase-perf rows
    - `.planning/phases/02-dependency-build-tooling-upgrade/02-PATTERNS.md` — § `shared/build.gradle.kts`, § `androidApp/build.gradle.kts`, § Detekt dual-module consistency
    - `buildSrc/src/main/java/Dependencies.kt` — defines the `AndroidSdk` object whose `compile`/`min`/`target` values both modules read; these values must not change
  </read_first>
  <action>
    Implements BUILD-02 and D-04/D-05. One commit containing the plugin swap **and** every bump AGP 9 forces — 02-RESEARCH.md's primary recommendation is that these are a required sub-task of this commit, not optional cleanup, because scoping to BUILD-02's named packages alone makes the commit fail on `detekt`, `lint` and Hilt annotation processing.

    Re-verify each target version against its registry metadata before writing (`dl.google.com/dl/android/maven2/com/android/tools/build/gradle/maven-metadata.xml`, `repo1.maven.org/maven2/com/google/dagger/hilt-android/maven-metadata.xml`, `dl.google.com/dl/android/maven2/com/google/firebase/perf-plugin/maven-metadata.xml`, and the Detekt alpha the previous checkpoint approved).

    **`gradle/libs.versions.toml`:**
    - `[versions]` `agp` → `9.2.0` if 02-01's checkpoint resolved `pinned`, or `9.4.0` if it resolved `newest-verified`.
    - `[versions]` `detekt` → the exact `2.0.0-alpha.N` string the previous checkpoint approved.
    - `[versions]` `dagger` → `2.60.1` (AGP 9's documented floor is 2.59; 2.60.1 was verified current).
    - `[versions]` `firebasePerfPlugin` → `2.0.2` (exactly AGP 9's documented floor — no headroom).
    - `[plugins]` add `android-kotlin-multiplatform-library = { id = "com.android.kotlin.multiplatform.library", version.ref = "agp" }` and remove the `android-library` entry, which nothing may reference after this commit.
    - `[plugins]` change the Detekt entry's `id` from the `io.gitlab.arturbosch.detekt` coordinate to `dev.detekt`.
    - `[libraries]` change `detekt-fomatting`'s `module` group from `io.gitlab.arturbosch.detekt` to `dev.detekt`, keeping the artifact name `detekt-formatting` and the existing (misspelled) catalog key.
    - Change nothing else: `composeBom`, `appollo`, `firebaseBom`, `kotlinxCoroutines`, `kotlinxSerialization`, `kotlinxDatetime`, `googleServicesPlugin` and `firebaseCrashlyticsPlugin` belong to 02-03/02-04.

    **`build.gradle.kts` (root):** replace `alias(libs.plugins.android.library) apply false` with `alias(libs.plugins.android.kotlin.multiplatform.library) apply false`. Leave every other `apply false` alias as-is.

    **`shared/build.gradle.kts`** — follow 02-RESEARCH.md § Pattern 1's verified after-shape:
    - In `plugins { }`, replace the AGP library alias with `alias(libs.plugins.android.kotlin.multiplatform.library)`.
    - Delete the top-level `android { }` block entirely and re-declare its contents inside `kotlin { android { … } }`: `namespace = "com.gdgnantes.devfest"`, `compileSdk = AndroidSdk.compile`, `minSdk = AndroidSdk.min` (flat, not nested in `defaultConfig`), and `compilerOptions { jvmTarget.set(JvmTarget.JVM_11) }` as the single source of truth for both Kotlin and Java compilation — which supersedes the old `compileOptions { sourceCompatibility/targetCompatibility = JavaVersion.VERSION_1_8 }` pair. The `androidTarget { … }` declaration is replaced by this `android { … }` block; do not keep both.
    - Drop the manifest `srcFile` override: the new plugin's default manifest location is `src/androidMain/AndroidManifest.xml`, which is already where the file lives. This rests on RESEARCH assumption A4 — if the build reports a missing manifest, re-declare the path in the new DSL's equivalent location rather than moving the file.
    - Do **not** add `androidResources { enable = true }` (no `res/` directory under `shared/src`), `withJava()` (no `.java` sources), or `withHostTest { }` / `withDeviceTest { }` (no `androidUnitTest`/`androidInstrumentedTest` directories exist).
    - Leave `jvm()`, the three iOS `binaries.framework` declarations, the `KotlinNativeTarget` memory-model block, the `languageSettings.optIn` block, the `apollo { }` block, the `commonMain`/`commonTest` dependency blocks and `dependencies { detektPlugins(...) }` unchanged.
    - If the Detekt 2.0 DSL renames or drops `buildUponDefaultConfig`, `allRules`, `autoCorrect`, `config` or `source`, adapt to the new knob names but keep the same effective configuration — in particular keep all three source-set paths in `source.setFrom(...)`.

    **`androidApp/build.gradle.kts`:**
    - Replace the resource-packaging block: the AGP-8-era `packagingOptions { resources { excludes += "/META-INF/{AL2.0,LGPL2.1}" } }` becomes `packaging { resources { excludes.add("/META-INF/AL2.0"); excludes.add("/META-INF/LGPL2.1") } }`. The brace-expansion pattern is documented as unreliable under AGP 9's new DSL, and this change is forced by AGP itself, independent of the KMP-library swap.
    - Update the two Detekt task-type imports at the top of the file to the packages Detekt 2.0 publishes them under. The exact new import path is the LOW-confidence part of RESEARCH's Detekt findings — resolve it from the alpha's own published sources or the compile error, and if the 2.0 DSL has consolidated the `jvmTarget` knob onto the `detekt { }` extension, delete the two `tasks.withType<…>().configureEach` blocks rather than inventing an equivalent. Preserve the existing `1.8` Detekt-task JVM target; the `1.8` vs `JVM_11` inconsistency between the two modules is pre-existing and out of scope for this phase (D-01 scope discipline) unless a build failure forces it.
    - Leave `applicationId`, `versionCode`, `versionName`, the `AndroidSdk.*` values, both `buildTypes`, `compileOptions`/`kotlinOptions`, `buildFeatures`, `testOptions`, `namespace` and the entire `dependencies { }` block untouched. `kotlinOptions` is AGP-8-era; only migrate it to `compilerOptions` if AGP 9 rejects it, and record it in the SUMMARY if so.

    **`androidApp/proguard-rules.pro`:** review only. AGP 9.2.0 defaults `android.r8.strictFullModeForKeepRules` to true, so keep rules no longer implicitly retain default constructors, and global options such as a no-obfuscation directive are disallowed inside consumer keep rules. The project already uses `getDefaultProguardFile("proguard-android-optimize.txt")`, which is the only supported default file, so no change is expected there. Add explicit constructor keep rules only if the release build actually breaks; leave the file untouched otherwise.

    Do not reach for `android.newDsl=false` or `android.builtInKotlin=false` in `gradle.properties` to unblock anything — those flags revert the project off the DSL the new plugin requires, and BUILD-02 cannot be satisfied with them set. If the build cannot be made green without them, halt and surface it rather than setting them silently.

    If any target version does not build, apply D-03: pin the newest version that does, record the deviation and the forcing error in the SUMMARY for 02-05 to consolidate, and continue.

    Commit as `build(02-02): migrate shared to com.android.kotlin.multiplatform.library on AGP <version>`, push, drive both workflows to green, then run the D-04 device smoke pass.
  </action>
  <!-- planner-discipline-allow: libs.plugins.android.library -->
  <!-- planner-discipline-allow: packagingOptions -->
  <!-- planner-discipline-allow: io.gitlab.arturbosch.detekt -->
  <verify>
    <automated>grep -vE '^[[:space:]]*#' gradle/libs.versions.toml > /tmp/cat-02-02.txt && grep -qE '^agp = "9\.' /tmp/cat-02-02.txt && grep -qE '^detekt = "2\.0\.0-alpha\.' /tmp/cat-02-02.txt && grep -qE '^dagger = "2\.(59|6[0-9])' /tmp/cat-02-02.txt && grep -qE '^firebasePerfPlugin = "2\.0\.[2-9]' /tmp/cat-02-02.txt && grep -q 'id = "com.android.kotlin.multiplatform.library"' /tmp/cat-02-02.txt && ! grep -q 'io.gitlab.arturbosch.detekt' /tmp/cat-02-02.txt && grep -qE '^composeBom = "2025\.09\.01"' /tmp/cat-02-02.txt && grep -qE '^appollo = "4\.3\.3"' /tmp/cat-02-02.txt && echo "STAGE2-CATALOG-OK"</automated>
    <fails_when>non-zero exit, or the `STAGE2-CATALOG-OK` line is absent. A failing `agp`/`detekt`/`dagger`/`firebasePerfPlugin` match means one of the four forced bumps was skipped; a missing new-plugin id means the catalog alias was never added; a surviving legacy Detekt group string means the plugin-id/group rename is only half done (the build can still resolve, so nothing else catches this); a failing `composeBom`/`appollo` match means a later stage's group leaked into this commit.</fails_when>

    <automated>grep -vE '^[[:space:]]*//' shared/build.gradle.kts > /tmp/shared-02-02.txt && grep -q 'alias(libs.plugins.android.kotlin.multiplatform.library)' /tmp/shared-02-02.txt && ! grep -q 'libs.plugins.android.library' /tmp/shared-02-02.txt && ! grep -qE '^android \{' /tmp/shared-02-02.txt && ! grep -q 'sourceSets\["main"\]' /tmp/shared-02-02.txt && grep -qE '^[[:space:]]+android \{' /tmp/shared-02-02.txt && grep -q 'namespace = "com.gdgnantes.devfest"' /tmp/shared-02-02.txt && grep -q 'AndroidSdk.compile' /tmp/shared-02-02.txt && grep -q 'AndroidSdk.min' /tmp/shared-02-02.txt && grep -q 'JvmTarget.JVM_11' /tmp/shared-02-02.txt && grep -cq 'iosX64()' /tmp/shared-02-02.txt && grep -q 'jvm()' /tmp/shared-02-02.txt && grep -q '"src/iosMain/kotlin"' /tmp/shared-02-02.txt && echo "SHARED-MIGRATED-OK"</automated>
    <fails_when>non-zero exit, or the `SHARED-MIGRATED-OK` line is absent. A surviving top-level `android {` at column zero means the block was copied rather than moved; a surviving AGP-8 library plugin alias means the forbidden coexistence is still declared (BUILD-02 unmet); a surviving `sourceSets["main"]` means the redundant manifest override was not dropped; a missing indented `android {`, `namespace`, `AndroidSdk.compile`, `AndroidSdk.min` or `JvmTarget.JVM_11` means configuration was lost in the move rather than relocated; a missing `iosX64()`, `jvm()` or `src/iosMain/kotlin` entry means the build surface was shrunk to make the bump pass, which the plan's second prohibition forbids.</fails_when>

    <automated>grep -vE '^[[:space:]]*//' androidApp/build.gradle.kts > /tmp/app-02-02.txt && ! grep -q 'packagingOptions' /tmp/app-02-02.txt && grep -q 'excludes.add("/META-INF/AL2.0")' /tmp/app-02-02.txt && grep -q 'excludes.add("/META-INF/LGPL2.1")' /tmp/app-02-02.txt && ! grep -q 'io.gitlab.arturbosch.detekt' /tmp/app-02-02.txt && grep -q 'AndroidSdk.min' /tmp/app-02-02.txt && grep -q 'AndroidSdk.target' /tmp/app-02-02.txt && grep -q 'libs.firebase.perf.ktx' /tmp/app-02-02.txt && echo "APP-MIGRATED-OK"</automated>
    <fails_when>non-zero exit, or the `APP-MIGRATED-OK` line is absent. A surviving AGP-8 packaging block name means Pitfall 5's DSL fix was not applied; missing individual `excludes.add(...)` entries mean the brace-expansion pattern survived in some other form; a surviving legacy Detekt import package means the plugin rename is half done here; a missing `AndroidSdk.min`/`AndroidSdk.target` or `libs.firebase.perf.ktx` means the SDK levels or a Firebase SDK were dropped to make the bump pass.</fails_when>

    <automated>./gradlew --no-daemon detekt lint :shared:assemble :androidApp:assembleDebug :androidApp:assembleRelease :shared:jvmTest :androidApp:testDebugUnitTest</automated>
    <fails_when>non-zero exit, or a `FAILURE: Build failed` / `Execution failed for task` line. Specific signals to read rather than retry blindly: a `:detekt` task that reports **zero** issues across the whole codebase is Pitfall 1's silent no-op (the task registered but stopped seeing Android variants) and must be treated as a failure even though the exit code is 0 — compare the issue count against the pre-bump run; `ksp`/`kspTest`/`kspAndroidTest` failures naming Hilt-generated code are Pitfall 2's Hilt-floor signal; an `assembleRelease` R8 failure is Pitfall 6's strict-keep-rules signal.</fails_when>

    <automated>SHA=$(git rev-parse HEAD) || exit 1; BR=$(git branch --show-current) || exit 1; test -n "$SHA" -a -n "$BR" || { echo "NO-GIT-CONTEXT"; exit 1; }; for WF in android.yml ios.yml; do RID=$(gh run list --workflow "$WF" --branch "$BR" --limit 20 --json databaseId,headSha --jq "[.[]|select(.headSha==\"$SHA\")][0].databaseId"); test -n "$RID" || { echo "NO-RUN $WF $SHA"; exit 1; }; gh run watch "$RID" --exit-status || { echo "RED $WF $RID"; exit 1; }; done && RID=$(gh run list --workflow android.yml --branch "$BR" --limit 20 --json databaseId,headSha --jq "[.[]|select(.headSha==\"$SHA\")][0].databaseId") && test "$(gh run view "$RID" --json jobs --jq '[.jobs[]|select(.conclusion=="success")]|length')" = "4" && echo "CI-GREEN-BOTH-4JOBS $SHA"</automated>
    <fails_when>non-zero exit, a `NO-RUN <workflow> <sha>` line, a `RED <workflow> <run-id>` line, a successful-job count other than 4 for `android.yml` (the four jobs are checks, unit-tests, instrumentation-tests, build-debug — a count below 4 means a job was skipped, removed or soft-failed, which the plan's first prohibition forbids), or the absence of the final `CI-GREEN-BOTH-4JOBS <sha>` line. The `ios.yml` half is D-05's CI-green-only requirement for iOS.</fails_when>

    <automated>android run && android layout > /tmp/agp9-layout.txt && grep -q 'com.gdgnantes.devfest.mobile.androidapp.dev' /tmp/agp9-layout.txt && android screen capture && echo "AGP9-SMOKE-LAUNCH-OK"</automated>
    <fails_when>non-zero exit from `android run` (build, install or launch failed on the AGP 9 toolchain), the package id absent from the `android layout` dump (launched but crashed before attaching a window), or the absence of the `AGP9-SMOKE-LAUNCH-OK` line. This automates the launch half of D-04; the per-screen render/navigate half is the human check below.</fails_when>

    <human-check>
      D-04's smoke pass. The debug app has been built on AGP 9 with the new KMP-library plugin, installed and launched on the attached device, and both CI workflows are green on the commit (all four `android.yml` jobs plus `ios.yml`).

      Drive the app and confirm each of the four screens D-04 names still renders and navigates:
      1. **Agenda** — sessions list populated (not an empty or error state), day switching works.
      2. **Speakers** — speaker list populated, tapping one opens the speaker detail.
      3. **Venue** — venue content renders.
      4. **Bookmarks** — the list renders, and bookmarking a session from its detail screen then returning shows it in Bookmarks.

      While you are in Bookmarks, confirm any bookmarks that existed before this phase started are still there. D-06's data-loss allowance covers **only** Apollo's normalized GraphQL cache; `BookmarksStore` is SharedPreferences-backed and separate, so bookmark loss here is a regression, not an accepted cost.

      This is a smoke pass, not a full per-screen regression (D-04), and there is deliberately **no** manual iOS pass — iOS needs CI-green only for this phase (D-05); the deeper iOS framework risk is Phase 3's concern. `android screen capture` output is attached in the SUMMARY.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `gradle/libs.versions.toml` shows `agp` at `9.2.0` or `9.4.0` (matching 02-01's policy decision), `detekt` at the approved `2.0.0-alpha.N`, `dagger` at ≥ 2.59, `firebasePerfPlugin` at ≥ 2.0.2, a `[plugins]` entry whose id is `com.android.kotlin.multiplatform.library`, and no occurrence of the legacy Detekt group/plugin coordinate anywhere in the file.
    - The same file still shows `composeBom = "2025.09.01"` and `appollo = "4.3.3"` — no later stage leaked in.
    - `shared/build.gradle.kts` applies the new KMP-library plugin alias, no longer applies the AGP-8 library plugin alias, has no top-level `android { }` block and no `sourceSets["main"]` manifest override, and carries `namespace`, `AndroidSdk.compile`, `AndroidSdk.min` and `JvmTarget.JVM_11` inside an indented `android { }` block; `jvm()`, `iosX64()`, `iosArm64()`, `iosSimulatorArm64()` and all three detekt `source.setFrom` paths are still present.
    - `build.gradle.kts` (root) declares the new KMP-library plugin `apply false` and no longer declares the AGP-8 library plugin.
    - `androidApp/build.gradle.kts` has no AGP-8 packaging block name, has two individual `excludes.add("/META-INF/…")` entries, no legacy Detekt import package, and still carries `AndroidSdk.min`, `AndroidSdk.target` and `libs.firebase.perf.ktx`.
    - `./gradlew detekt lint :shared:assemble :androidApp:assembleDebug :androidApp:assembleRelease :shared:jvmTest :androidApp:testDebugUnitTest` exits 0, and the `:detekt` task's reported issue count is not zero-across-the-whole-codebase (compare against the pre-bump run recorded in the SUMMARY).
    - `gh run view` reports exactly 4 successful jobs for the `android.yml` run on this commit's SHA, and `gh run watch` exits 0 for the `ios.yml` run on the same SHA.
    - `android run` exits 0 and the app's UI tree is reachable.
    - `gradle.properties` contains neither of AGP 9's new-DSL/built-in-Kotlin opt-out flags.
    - `git log --oneline` shows this stage as exactly one commit, distinct from Task 1's wrapper commit.
  </acceptance_criteria>
  <done>`shared` builds as a KMP Android library through `com.android.kotlin.multiplatform.library` on AGP 9.x, with Detekt on its AGP-9-compatible alpha under the new plugin id, Hilt and the Firebase Performance plugin at their AGP-9 floors, and `androidApp`'s packaging DSL migrated — landed as one revertible commit, green on all four Android CI jobs plus iOS, and smoke-passed on a device.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dev machine / CI runner → `services.gradle.org` | The Gradle distribution zip is downloaded and *executed* as the build tool itself; every byte of the build system crosses here |
| Dev machine / CI runner → Google Maven + Maven Central + Gradle Plugin Portal | AGP, the new KMP-library plugin, Detekt's alpha, Hilt and the Firebase Performance plugin are fetched and executed at Gradle configuration time |
| `gradle/libs.versions.toml` → Gradle build graph | Plugin ids and artifact groups are declared only here; this commit changes two of them (the AGP library plugin and the Detekt coordinate), which is the largest coordinate churn in the phase |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04 | Tampering | `gradle/wrapper/gradle-wrapper.properties` `distributionUrl` — the Gradle distribution is downloaded and run as the build tool | high | mitigate | Task 1 performs the bump via `./gradlew wrapper --gradle-distribution-sha256-sum <official sum>`, adding a `distributionSha256Sum` the file does not have today, and keeps `validateDistributionUrl=true`. The `WRAPPER-PINNED-OK` gate asserts a 64-hex checksum is present and that the URL host is `services.gradle.org`, so a distribution swap or a redirected host fails the gate. |
| T-02-05 | Elevation of Privilege | Plugin-coordinate change: the AGP library plugin id is replaced by `com.android.kotlin.multiplatform.library`, and the Detekt plugin id and Maven group are renamed — Gradle executes plugin code at configuration time with developer/CI privileges | high | mitigate | Both new coordinates are first-party and verified before use: the AGP plugin id comes from `developer.android.com/kotlin/multiplatform/plugin` and resolves under the already-trusted `agp` version ref on Google's Maven; the Detekt coordinate is confirmed against `plugins.gradle.org/plugin/dev.detekt` and `repo1.maven.org/maven2/dev/detekt/` in the `blocking-human` checkpoint before it is pinned. The `STAGE2-CATALOG-OK` gate asserts the new plugin id string is present and the legacy Detekt coordinate is fully gone, so a half-finished rename (old group still resolving) cannot pass. |
| T-02-06 | Tampering | Pinning CI's static-analysis step to `dev.detekt` 2.0.0-alpha — a pre-1.0 release, `[SUS]` verdict in 02-RESEARCH.md § Package Legitimacy Audit | medium | mitigate | A `checkpoint:human-verify` carrying `gate="blocking-human"` runs before the pin; it is never auto-approved in any mode, so no unattended run can adopt the alpha. Findings from this Detekt version are treated as advisory until it reaches stable, and the `:detekt` verify gate explicitly treats a zero-issues-everywhere result as a failure so a silently no-op'ing analyser is not mistaken for a clean codebase. |
| T-02-07 | Tampering | AGP 9's `android.r8.strictFullModeForKeepRules=true` default changes what release builds retain; a reflex-added broad keep rule would weaken shrinking and enlarge the shipped attack surface | medium | mitigate | `androidApp/proguard-rules.pro` is review-only; explicit constructor keep rules are added only if `assembleRelease` actually breaks, never pre-emptively, and the release build is exercised by the verify gate (`:androidApp:assembleRelease`) rather than assumed. |
| T-02-08 | Information Disclosure | The `secrets-gradle-plugin` and `google-services` configuration could be disturbed by the AGP 9 DSL migration, changing how build-time secrets reach `BuildConfig` | medium | accept | This commit makes no change to the `secrets` or `google-services` plugin aliases, their versions, or the `buildConfigField` declarations that consume them; the acceptance criteria assert the `androidApp` `defaultConfig` block is untouched. No new secret surface is introduced. |
| T-02-SC | Tampering | npm/pip/cargo package installs | low | mitigate | Not applicable — this phase introduces no npm/pip/cargo packages (Gradle/Maven ecosystem only), so the automated `package-legitimacy check` gate has no supported ecosystem. The manual audit in `02-RESEARCH.md` § Package Legitimacy Audit covers the new coordinates, and the one `[SUS]` artifact is gated by the `blocking-human` checkpoint in this plan. |
</threat_model>

<verification>
- `./gradlew --version` prints `Gradle 9.7.1`; `gradle/wrapper/gradle-wrapper.properties` carries a 64-hex `distributionSha256Sum`.
- `grep -vE '^[[:space:]]*#' gradle/libs.versions.toml | grep -E '^(agp|detekt|dagger|firebasePerfPlugin|composeBom|appollo) ='` shows the four forced bumps landed and the two later-stage groups untouched.
- `shared/build.gradle.kts` has no top-level `android {` block, applies the new KMP-library plugin, and retains all three iOS targets plus `jvm()`.
- `./gradlew detekt lint :shared:assemble :androidApp:assembleDebug :androidApp:assembleRelease :shared:jvmTest :androidApp:testDebugUnitTest` exits 0 with a non-empty `:detekt` issue baseline comparable to the pre-bump run.
- `android.yml` reports 4 successful jobs and `ios.yml` reports success, both on the AGP-9 commit's SHA.
- The device smoke pass covers Agenda, Speakers, Venue and Bookmarks, and pre-existing bookmarks survived.
</verification>

<success_criteria>
BUILD-03 and BUILD-02 are satisfied as two separate revertible commits: the project builds on Gradle 9.7.1 with a checksum-verified distribution, and `shared` builds as a KMP Android library through `com.android.kotlin.multiplatform.library` with every AGP-9-forced sibling bump applied — verified by all four Android CI jobs plus iOS CI green, and by a device smoke pass over the four screens D-04 names.
</success_criteria>

<output>
Create `.planning/phases/02-dependency-build-tooling-upgrade/02-02-SUMMARY.md` when done. Record: the Gradle checksum used, the exact `agp`/`detekt`/`dagger`/`firebasePerfPlugin` strings written, the Detekt alpha the developer approved, the pre-bump and post-bump `:detekt` issue counts (so a silent no-op is auditable later), the new Detekt task-type import package actually used, whether `kotlinOptions`/`proguard-rules.pro`/`gradle.properties` needed any change, any D-03 deviation with its forcing error, both CI run IDs per commit, and the `android screen capture` output from the D-04 smoke pass.
</output>
