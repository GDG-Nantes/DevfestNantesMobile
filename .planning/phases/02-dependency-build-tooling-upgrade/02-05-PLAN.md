---
phase: 02-dependency-build-tooling-upgrade
plan: 05
type: execute
wave: 5
depends_on: ["02-04"]
files_modified:
  - settings.gradle.dcl
  - settings.gradle.kts
  - buildSrc/build.gradle.kts
  - .planning/STATE.md
  - .planning/PROJECT.md
autonomous: true
requirements: [BUILD-07]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Either one build file is in Gradle Declarative DSL (`.gradle.dcl`) form and the project still builds through it, or the specific DCL limitation that blocked each attempted target is documented — BUILD-07 and D-08 accept both outcomes, and neither may be left unrecorded"
    - "`./gradlew projects` still reports root project `DevFest_Nantes` with exactly the `:androidApp` and `:shared` subprojects, whichever settings-file form is in effect — project topology survives the format change"
    - "Exactly one settings file is in effect: `settings.gradle.dcl` and `settings.gradle.kts` never coexist"
    - "`androidApp` and `shared` remain on Kotlin DSL, with the reason recorded — D-08 scoped the pilot to one low-risk leaf target, not a full-project migration"
    - "Every one of BUILD-01 … BUILD-07 has a recorded outcome in `.planning/STATE.md`: the version actually pinned, whether it matched the REQUIREMENTS.md target, and the forcing error for any deviation (D-03's documentation obligation, which the escape valve does not come without)"
    - "`.planning/PROJECT.md`'s Key Decisions table carries a row for the Phase 02 outcome, consistent with how Phase 1's outcome was recorded"
  artifacts:
    - path: ".planning/STATE.md"
      provides: "Phase 02 per-requirement version outcome table and the DCL pilot outcome, including any D-03 deviation with its reason"
      contains: "BUILD-07"
    - path: ".planning/PROJECT.md"
      provides: "Key Decisions row recording the Phase 02 toolchain outcome"
      contains: "Phase 2"
  key_links:
    - from: "settings.gradle.dcl"
      to: "build.gradle.kts"
      via: "the settings file declares the root project name and the two include()d subprojects the root build script configures"
      pattern: "androidApp"
    - from: ".planning/STATE.md"
      to: ".planning/REQUIREMENTS.md"
      via: "the outcome table is keyed by the BUILD-01..BUILD-07 requirement ids so a later audit can match pinned versions against requirement targets"
      pattern: "BUILD-0[1-7]"
  prohibitions:
    - statement: "MUST NOT report the DCL pilot as complete without a recorded outcome — D-08's fallback is an explicitly planned result ('document what worked and what is blocked'), so a silent revert with no documentation fails BUILD-07 just as surely as no attempt at all"
      status: unverified
    - statement: "MUST NOT land a version pinned below its REQUIREMENTS.md target without recording the deviation and its reason — this plan is where that obligation is discharged for the whole phase, so an unrecorded earlier deviation is a gap here, not there"
      status: unverified
    - statement: "MUST NOT expand the DCL pilot beyond one low-risk target — `androidApp/build.gradle.kts` and `shared/build.gradle.kts` stay on Kotlin DSL (D-08), and no module's build logic may be rewritten to fit a DCL schema"
      status: unverified
---

<objective>
Pilot the Gradle Declarative DSL on one low-risk target, and consolidate the whole phase's version outcomes into the project's durable records.

Purpose: BUILD-07 asks for build files migrated to `.gradle.dcl` "wherever AGP/KMP support allows", with anything left on Kotlin DSL documented with the reason support is missing. D-08 turns that into a bounded experiment — one low-risk leaf target, `androidApp`/`shared` explicitly staying on `.kts` — and makes a documented partial migration an acceptable planned outcome rather than a failure to escalate. D-09 leaves the target to implementation-time discretion; 02-RESEARCH.md Open Question 2 recommends attempting `settings.gradle.kts` first (its content is purely declarative: repository declarations, two includes, one feature-preview flag) and falling back to `buildSrc/build.gradle.kts`, documenting the specific error hit for each.

The second task discharges D-03's other half. D-03 grants permission to pin below a REQUIREMENTS.md target when something does not build, but only together with an obligation to record the gap in STATE.md/PROJECT.md. Five stages have now run; this is where those records land in one place, keyed by requirement id, so a later audit can match every pinned version against its target.

Output: one revertible commit for the DCL pilot outcome (converted file, or reverted attempt plus documentation), and one docs commit consolidating the phase's version outcomes.
</objective>

<execution_context>
@/Users/robin/Development/Android/DevFestNantes/.claude/gsd-core/workflows/execute-plan.md
@/Users/robin/Development/Android/DevFestNantes/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-dependency-build-tooling-upgrade/02-CONTEXT.md
@.planning/phases/02-dependency-build-tooling-upgrade/02-RESEARCH.md
@.planning/phases/02-dependency-build-tooling-upgrade/02-PATTERNS.md
@.planning/phases/02-dependency-build-tooling-upgrade/02-01-SUMMARY.md
@.planning/phases/02-dependency-build-tooling-upgrade/02-02-SUMMARY.md
@.planning/phases/02-dependency-build-tooling-upgrade/02-03-SUMMARY.md
@.planning/phases/02-dependency-build-tooling-upgrade/02-04-SUMMARY.md
@settings.gradle.kts
@buildSrc/build.gradle.kts
</context>

## Artifacts this phase produces

Symbols and files **created** by Phase 02 (none exist in the repo today — exclude them from drift/source-grounding verification):

| Artifact | Created by | Kind |
|---|---|---|
| **`settings.gradle.dcl` (or `buildSrc/build.gradle.dcl` if the primary pilot is blocked; neither, if both are blocked and documented)** | **02-05 (this plan)** | new file |
| **`## Phase 02 version deviations` — new section in `.planning/STATE.md`** | **02-05 (this plan)** | new doc section |
| **`## Phase 02 DCL pilot outcome` — new section in `.planning/STATE.md`** | **02-05 (this plan)** | new doc section |
| `shared/src/commonTest/kotlin/com/gdgnantes/devfest/model/ScheduleSlotDateParsingTest.kt` | 02-04 | new file |
| `ScheduleSlotDateParsingTest` class and its `@Test` functions | 02-04 | new symbols |
| `appolloCache` — new `[versions]` key in `gradle/libs.versions.toml` | 02-03 | new catalog key |
| `plugin(...)` and `pluginArgument(...)` calls inside `apollo { service("service") { … } }` in `shared/build.gradle.kts` | 02-03 | new config statements |
| `android-kotlin-multiplatform-library` — new `[plugins]` alias in `gradle/libs.versions.toml` | 02-02 | new catalog key |
| `kotlin { android { … } }` — new DSL block in `shared/build.gradle.kts` | 02-02 | new config block |
| `packaging { … }` — new DSL block in `androidApp/build.gradle.kts` | 02-02 | new config block |
| `distributionSha256Sum` — new key in `gradle/wrapper/gradle-wrapper.properties` | 02-02 | new config key |

<assumptions>
## Flagged assumptions (spec-less phase — no SPEC.md exists)

Automated edge-coverage classification returned `category: unclassified`, `status: unresolved` for **all seven** of BUILD-01 … BUILD-07 — expected, not an error: the edge-probe engine classifies user-facing feature/CRUD requirements and version-bump/build-tooling requirements do not fit its patterns. Per the spec-less fallback protocol each unresolved row is surfaced here as an explicit assumption rather than auto-resolved into `must_haves.truths`.

- **BUILD-01 … BUILD-07 (all 7):** automated edge-coverage classification did not apply. What grounds each requirement's acceptance criteria across this plan set is (a) the D-01 … D-09 locked decisions in `02-CONTEXT.md` and (b) the live-verified version numbers in `02-RESEARCH.md` § Version Verification. No edge-probe-derived predicate backs them. This plan's Task 2 is where that grounding is written down permanently, per requirement id.
- **BUILD-07, assumption A5:** that `settings.gradle.kts` and `buildSrc` are viable DCL pilot candidates *today* is MEDIUM-risk and unverified. DCL's own documentation says buildSrc cannot host custom ecosystem plugins — this project's buildSrc hosts no plugin, only a compiled utility object — but whether DCL defines a software type for a bare `embedded-kotlin` build script at all is genuinely unknown. D-08's documented-fallback allowance exists precisely for this.
- **BUILD-07, DCL maturity:** 02-RESEARCH.md rates the Declarative Gradle support matrix MEDIUM confidence, sourced from Gradle blog/docs content current as of an April 2025 EAP, with no confirmed later status found for 2026. Re-check the current DCL status before attempting the conversion rather than working from the research's snapshot.
- **BUILD-07, Phase 3 interaction:** D-09 notes the pilot-target choice may be revisited once Phase 3's multi-module split changes what a "leaf module" means. Record the target chosen and why, so Phase 3 can extend or redo the pilot knowingly.
</assumptions>

<tasks>

<task type="auto">
  <name>Task 1: Gradle Declarative DSL pilot on one low-risk target, with a documented outcome either way</name>
  <reversibility rating="reversible">A build-file format conversion with zero runtime impact by construction; `git revert` restores the Kotlin DSL form, and the fallback path in this task already reverts the attempt itself when it fails.</reversibility>
  <files>settings.gradle.dcl, settings.gradle.kts, buildSrc/build.gradle.kts, .planning/STATE.md</files>
  <read_first>
    - `settings.gradle.kts` — the primary pilot target, 24 lines, entirely declarative in content: a `@file:Suppress("UnstableApiUsage")` file annotation, `enableFeaturePreview("TYPESAFE_PROJECT_ACCESSORS")`, a `pluginManagement { repositories { google(); mavenCentral(); gradlePluginPortal() } }` block, a `dependencyResolutionManagement { repositories { … } }` block with the same three repositories and one commented-out `repositoriesMode` line, `rootProject.name = "DevFest_Nantes"`, and `include(":androidApp")` / `include(":shared")`. The file annotation and the feature-preview call are the two constructs least likely to have a DCL equivalent
    - `buildSrc/build.gradle.kts` — the fallback pilot target, 4 lines: a `plugins { \`embedded-kotlin\` }` block and nothing else
    - `buildSrc/src/main/java/Dependencies.kt` — the only thing buildSrc produces: a plain `AndroidSdk` object read by both module build scripts. Confirms buildSrc hosts no custom Gradle plugin, which is what DCL's documented buildSrc limitation is actually about
    - `build.gradle.kts` (root) — declares every plugin alias `apply false`; it consumes the subprojects the settings file includes, so a settings conversion that loses an `include` breaks it silently rather than loudly
    - `gradle/libs.versions.toml` — the version catalog is resolved through `dependencyResolutionManagement`'s default catalog convention rather than an explicit `versionCatalogs { }` block, so a settings conversion must not break catalog discovery; every `libs.*` accessor in both module scripts depends on it
    - `.planning/phases/02-dependency-build-tooling-upgrade/02-RESEARCH.md` — § Open Questions 2 (the recommended attempt order and why), assumption A5, and the § Recommended Project Structure notes on both candidate files
    - `.planning/phases/02-dependency-build-tooling-upgrade/02-PATTERNS.md` — § `settings.gradle.kts` to `settings.gradle.dcl`, which reproduces the file's full current content as the conversion source and names the two constructs to watch
    - `.planning/STATE.md` — the file this task appends the pilot outcome to; note its existing `## Accumulated Context` structure so the new section is added alongside rather than replacing anything
  </read_first>
  <action>
    Implements BUILD-07 and D-08/D-09. One commit. A documented outcome is the deliverable; a successful conversion is the preferred outcome but not the only acceptable one.

    First re-check the current state of Declarative Gradle support — 02-RESEARCH.md's support matrix is MEDIUM confidence from an April 2025 EAP snapshot with no confirmed 2026 update. Consult the current Gradle documentation for the declarative settings-file schema and note what it actually supports for Gradle 9.7.1 before converting anything.

    **Primary attempt — `settings.gradle.kts` to `settings.gradle.dcl`.** Recommended first because its content is purely declarative and it is the better-documented DCL surface. Convert, preserving all four semantics:
    - root project name `DevFest_Nantes`
    - the two subproject includes, `:androidApp` and `:shared`
    - plugin-management repositories: Google, Maven Central, Gradle Plugin Portal
    - dependency-resolution repositories: the same three

    Two constructs may have no DCL equivalent: the `@file:Suppress("UnstableApiUsage")` file annotation (a Kotlin-compiler construct, not a Gradle one — likely simply unnecessary in DCL) and `enableFeaturePreview("TYPESAFE_PROJECT_ACCESSORS")`. If the feature preview cannot be expressed, check whether typesafe project accessors are on by default in Gradle 9.7.1; if they are not, and nothing in the build actually uses a typesafe project accessor (both module scripts currently use the string form `project(":shared")`), dropping the flag is a behavior-preserving simplification — verify that claim with a build rather than assuming it. Do not carry a commented-out line across; note in the outcome record that the commented `repositoriesMode` line was dropped.

    When the `.dcl` file is in place and building, delete `settings.gradle.kts`. The two must never coexist: Gradle would select one and the other would become a silently-ignored file that the next reader takes as authoritative.

    **Fallback attempt — `buildSrc/build.gradle.kts` to `buildSrc/build.gradle.dcl`.** Only if the primary attempt fails. This is a 4-line file whose only content is the `embedded-kotlin` plugin application. DCL's documented buildSrc limitation concerns hosting custom ecosystem plugins, which this buildSrc does not do — but whether DCL defines a software type for a bare `embedded-kotlin` build script is exactly assumption A5's unknown.

    **If both attempts fail — the planned fallback, not an escalation.** D-08 explicitly allows this: revert both attempts so the repository is back on Kotlin DSL and building, and record the outcome. Do not escalate, and do not rewrite either file's logic to fit a DCL schema — reshaping build logic to satisfy the format would be scope creep past a pilot.

    **Scope boundary.** `androidApp/build.gradle.kts` and `shared/build.gradle.kts` stay on Kotlin DSL regardless of outcome (D-08). Do not attempt them, even if the primary conversion succeeds easily: they apply seven and eight plugins respectively including the AGP 9 KMP-library plugin, and AGP/KMP DCL support is not expected to cover them.

    **Record the outcome in `.planning/STATE.md`** in the same commit, as a new `## Phase 02 DCL pilot outcome` section. Use the `Edit` tool with a scoped replacement — never `Write`, which would destroy the rest of the file. The section must state, for each target attempted: the target file, whether it converted, and if it did not, the specific DCL error or documented limitation hit (verbatim where an error message is available). It must also state explicitly that `androidApp` and `shared` remain on Kotlin DSL and why — that sentence is what discharges BUILD-07's "documented with the reason support is missing" clause, so it is required whether or not the pilot succeeded.

    Commit as `build(02-05): pilot Gradle Declarative DSL on <target> and record the outcome`, push, and drive both workflows to green.
  </action>
  <verify>
    <automated>DCL=$(ls settings.gradle.dcl buildSrc/build.gradle.dcl 2>/dev/null | wc -l | tr -d ' ') && KTS=$(ls settings.gradle.kts 2>/dev/null | wc -l | tr -d ' ') && test ! \( -f settings.gradle.dcl -a -f settings.gradle.kts \) && test "$DCL" -ge 1 -o "$KTS" = "1" && echo "SETTINGS-FORM-EXCLUSIVE-OK dcl=$DCL kts=$KTS"</automated>
    <fails_when>non-zero exit, or the `SETTINGS-FORM-EXCLUSIVE-OK` line is absent. Both `settings.gradle.dcl` and `settings.gradle.kts` existing at once fails the gate — Gradle would pick one and leave the other as a misleading file a future reader treats as authoritative. Neither existing also fails: the project would have no settings file at all. The reported `dcl=`/`kts=` counts record which branch of the pilot was taken.</fails_when>

    <automated>./gradlew --no-daemon projects > /tmp/dcl-projects.txt 2>&1 && grep -q "Root project 'DevFest_Nantes'" /tmp/dcl-projects.txt && grep -q "Project ':androidApp'" /tmp/dcl-projects.txt && grep -q "Project ':shared'" /tmp/dcl-projects.txt && test "$(grep -c "^.*Project ':" /tmp/dcl-projects.txt)" = "2" && echo "PROJECT-TOPOLOGY-INTACT-OK"</automated>
    <fails_when>non-zero exit, or the `PROJECT-TOPOLOGY-INTACT-OK` line is absent. A missing root-project line means the root name was lost in the conversion; a missing `:androidApp` or `:shared` line means an include was dropped — the silent failure mode that matters most here, because the root build script would then configure fewer projects without erroring; a subproject count other than 2 means a project was added or lost.</fails_when>

    <automated>./gradlew --no-daemon clean :shared:assemble :androidApp:assembleDebug :androidApp:assembleRelease :shared:jvmTest :androidApp:testDebugUnitTest detekt lint && ./gradlew --no-daemon :shared:compileKotlinIosSimulatorArm64</automated>
    <fails_when>non-zero exit, or a `FAILURE: Build failed` / `Execution failed for task` line. An unresolved `libs.*` accessor error is the specific signal that the settings conversion broke version-catalog discovery through `dependencyResolutionManagement`; an unresolved-project error means an include was lost. `clean` is deliberate so nothing is served from a build cache populated under the previous settings form.</fails_when>

    <automated>rtk proxy grep -q '## Phase 02 DCL pilot outcome' .planning/STATE.md && rtk proxy grep -q 'BUILD-07' .planning/STATE.md && rtk proxy grep -qE 'androidApp.*(Kotlin DSL|\.kts)' .planning/STATE.md && rtk proxy grep -qE 'shared.*(Kotlin DSL|\.kts)' .planning/STATE.md && echo "DCL-OUTCOME-DOCUMENTED-OK"</automated>
    <fails_when>non-zero exit, or the `DCL-OUTCOME-DOCUMENTED-OK` line is absent. A missing outcome section means the pilot ran without a record, which fails BUILD-07 whichever way the attempt went; missing `androidApp`/`shared` Kotlin-DSL statements mean the "documented with the reason support is missing" clause was not discharged.</fails_when>

    <automated>SHA=$(git rev-parse HEAD) || exit 1; BR=$(git branch --show-current) || exit 1; test -n "$SHA" -a -n "$BR" || { echo "NO-GIT-CONTEXT"; exit 1; }; for WF in android.yml ios.yml; do RID=$(gh run list --workflow "$WF" --branch "$BR" --limit 20 --json databaseId,headSha --jq "[.[]|select(.headSha==\"$SHA\")][0].databaseId"); test -n "$RID" || { echo "NO-RUN $WF $SHA"; exit 1; }; gh run watch "$RID" --exit-status || { echo "RED $WF $RID"; exit 1; }; done && RID=$(gh run list --workflow android.yml --branch "$BR" --limit 20 --json databaseId,headSha --jq "[.[]|select(.headSha==\"$SHA\")][0].databaseId") && test "$(gh run view "$RID" --json jobs --jq '[.jobs[]|select(.conclusion=="success")]|length')" = "4" && echo "CI-GREEN-BOTH-4JOBS $SHA"</automated>
    <fails_when>non-zero exit, a `NO-RUN <workflow> <sha>` line, a `RED <workflow> <run-id>` line, a successful-job count other than 4 for `android.yml`, or the absence of the final `CI-GREEN-BOTH-4JOBS <sha>` line. A settings-file change is the one edit in this phase that can break CI without breaking a local build, because the runner starts from a cold configuration cache.</fails_when>
  </verify>
  <acceptance_criteria>
    - `settings.gradle.dcl` and `settings.gradle.kts` do not both exist; at least one settings file exists.
    - `./gradlew projects` reports root project `DevFest_Nantes` and exactly the two subprojects `:androidApp` and `:shared`.
    - `./gradlew clean :shared:assemble :androidApp:assembleDebug :androidApp:assembleRelease :shared:jvmTest :androidApp:testDebugUnitTest detekt lint` exits 0, and `:shared:compileKotlinIosSimulatorArm64` exits 0.
    - `.planning/STATE.md` contains a `## Phase 02 DCL pilot outcome` section that names every target attempted, states for each whether it converted, quotes the specific DCL error or documented limitation for each that did not, references BUILD-07, and states explicitly that `androidApp` and `shared` remain on Kotlin DSL with the reason.
    - `androidApp/build.gradle.kts` and `shared/build.gradle.kts` are unchanged by this commit (`git show --stat HEAD` does not list them).
    - `gh run view` reports exactly 4 successful jobs for the `android.yml` run on this commit's SHA, and `gh run watch` exits 0 for the `ios.yml` run on the same SHA.
  </acceptance_criteria>
  <done>One low-risk build file is either in `.gradle.dcl` form with the project still building and CI green through it, or every attempt is reverted with its specific blocking limitation recorded — and in both cases `.planning/STATE.md` documents the outcome and why `androidApp`/`shared` stay on Kotlin DSL.</done>
</task>

<task type="auto">
  <name>Task 2: Consolidate every stage's version outcome into STATE.md and PROJECT.md</name>
  <reversibility rating="reversible">Documentation edits only; `git revert` restores the prior text with no code or build effect.</reversibility>
  <precondition>All four prior SUMMARY files for this phase exist and are non-empty (`.planning/phases/02-dependency-build-tooling-upgrade/02-01-SUMMARY.md` through `02-04-SUMMARY.md`) — this task consolidates the version outcomes those SUMMARYs recorded, so it cannot produce a complete record without them. If any is missing, halt rather than reconstructing values from the catalog alone, because the catalog shows what was pinned but not what was intended or why it differs.</precondition>
  <files>.planning/STATE.md, .planning/PROJECT.md</files>
  <read_first>
    - `.planning/phases/02-dependency-build-tooling-upgrade/02-01-SUMMARY.md` — the version-target policy the developer selected at the phase's opening checkpoint, plus the Kotlin/KSP strings actually written and any `kmpNativeCoroutines` bump
    - `.planning/phases/02-dependency-build-tooling-upgrade/02-02-SUMMARY.md` — the Gradle checksum, the AGP/Detekt/Hilt/firebase-perf strings, the Detekt alpha the developer approved, the before/after Detekt issue counts, and whether `kotlinOptions`, `proguard-rules.pro` or `gradle.properties` needed changes
    - `.planning/phases/02-dependency-build-tooling-upgrade/02-03-SUMMARY.md` — the Compose BOM and Apollo/cache strings, whether the Accompanist D-03 branch was taken, and the authoritative new Apollo import paths
    - `.planning/phases/02-dependency-build-tooling-upgrade/02-04-SUMMARY.md` — the four BUILD-06 strings plus the two Firebase-family plugin versions, and which branch of assumption A2 applied for the `Instant` typealias
    - `.planning/REQUIREMENTS.md` — the `### Active` BUILD-01 … BUILD-07 entries whose literal targets the outcome table is compared against, and the requirement-traceability table near the end that tracks each id's phase and status
    - `.planning/STATE.md` — the file being edited. Note its YAML frontmatter, the `## Accumulated Context` section with its `### Decisions`, `### Pending Todos` and `### Blockers/Concerns` subsections, and that `### Blockers/Concerns` currently holds a Phase 2 entry about re-verifying AGP/KGP/KSP versions which this phase has now discharged
    - `.planning/PROJECT.md` — the `## Key Decisions` table at line 60 with its three columns (Decision, Rationale, Outcome) and the existing Phase 1 row, whose wording and level of detail this task's new row should match
    - `.planning/phases/02-dependency-build-tooling-upgrade/02-CONTEXT.md` — D-03's exact wording, which is what makes this record an obligation rather than a nicety
  </read_first>
  <action>
    Discharges D-03's documentation obligation for the whole phase, plus BUILD-07's record. Docs only — no build file is touched.

    Use the `Edit` tool with scoped replacements for both files. Never `Write`: `.planning/STATE.md` and `.planning/PROJECT.md` both carry content outside the sections being changed, and a whole-file overwrite would destroy it.

    **`.planning/STATE.md`** — add a new `## Phase 02 version deviations` section (alongside the existing `## Accumulated Context`, not replacing it) containing a table with one row per requirement id, all seven present:

    | Req | Target in REQUIREMENTS.md | Version pinned | Matched? | Reason if not |

    Fill it from the four SUMMARYs, covering BUILD-01 (Kotlin, plus the KSP and any `kmpNativeCoroutines` version as sibling notes), BUILD-02 (AGP, plus the Detekt/Hilt/firebase-perf forced bumps and the Detekt plugin-id rename), BUILD-03 (Gradle, plus the distribution checksum now pinned), BUILD-04 (Compose BOM, plus any Accompanist bump), BUILD-05 (Apollo runtime and the separate cache version), BUILD-06 (all four latest-stable versions plus the two Firebase-family plugins), and BUILD-07 (pointing at the DCL pilot outcome section Task 1 wrote).

    For each row, `Matched?` compares the pinned version against the literal REQUIREMENTS.md target. Record the version-target policy 02-01's checkpoint selected at the top of the section, because under a `newest-verified` policy four rows are deliberate supersets rather than deviations — that distinction is the whole point of the record. Where a version was genuinely pinned lower than intended, the `Reason if not` cell must carry the forcing build error, not a paraphrase.

    Also add these to the same section, since they are outcomes a later reader needs and no other record holds them:
    - Which branch of assumption A2 applied (whether kotlinx-datetime 0.8.0 kept the old `Instant` typealias, or the import was swapped) — this closes an open research question Phase 3 would otherwise re-investigate.
    - The authoritative Apollo cache import paths actually used, for the same reason.
    - The Detekt alpha pinned, with a note that its findings are advisory until Detekt reaches a stable 2.x, and the before/after issue counts.

    Then update the existing `### Blockers/Concerns` subsection: the Phase 2 entry about re-verifying AGP/KGP/KSP versions before implementation is now discharged, so replace it with a one-line note that the verification was performed during Phase 2 and the outcomes are recorded in the new section. Leave both Phase 3 concerns exactly as they are — they are still open and are Phase 3's inputs.

    **`.planning/PROJECT.md`** — add one row to the `## Key Decisions` table, matching the existing Phase 1 row's shape and level of detail. Decision: the staged one-commit-per-group toolchain upgrade with a green CI gate between stages. Rationale: compounded major bumps are undiagnosable, so each group gets its own revertible commit and its own green signal. Outcome: name what actually shipped — the toolchain versions reached, the AGP 9 KMP-library plugin swap, the Apollo cache-package migration, and the DCL pilot's result. Do not restructure the table or edit the Phase 1 row.

    Write only what the SUMMARYs actually record. If a SUMMARY is silent on something the table needs, mark that cell as not recorded and name the gap in this plan's own SUMMARY rather than inferring a value from the version catalog — the catalog shows what was pinned, never what was intended or why it differs.

    Commit as `docs(02-05): record Phase 02 version outcomes and DCL pilot result`. This commit changes no build file, so no local build gate applies; push it and let CI run.
  </action>
  <verify>
    <automated>rtk proxy grep -q '## Phase 02 version deviations' .planning/STATE.md && for R in BUILD-01 BUILD-02 BUILD-03 BUILD-04 BUILD-05 BUILD-06 BUILD-07; do rtk proxy grep -q "$R" .planning/STATE.md || { echo "MISSING-REQ-ROW $R"; exit 1; }; done && rtk proxy grep -q '## Phase 02 DCL pilot outcome' .planning/STATE.md && echo "STATE-OUTCOMES-RECORDED-OK"</automated>
    <fails_when>non-zero exit, a `MISSING-REQ-ROW <id>` line, or the absence of the `STATE-OUTCOMES-RECORDED-OK` line. Any of the seven ids missing means a requirement's outcome went unrecorded, which is the D-03 documentation half being skipped — the exact failure the plan's second prohibition names. A missing DCL outcome section means Task 1's record was overwritten by this task's edit rather than added alongside it.</fails_when>

    <automated>rtk proxy grep -q 'Phase 1 shipped' .planning/PROJECT.md && test "$(rtk proxy grep -c '^| ' .planning/PROJECT.md)" -ge 2 && rtk proxy grep -qE '^\| .*Phase 2' .planning/PROJECT.md && rtk proxy grep -q 'gsd_state_version' .planning/STATE.md && rtk proxy grep -q '## Accumulated Context' .planning/STATE.md && echo "DOCS-APPENDED-NOT-OVERWRITTEN-OK"</automated>
    <fails_when>non-zero exit, or the `DOCS-APPENDED-NOT-OVERWRITTEN-OK` line is absent. A missing Phase 1 row in PROJECT.md, a missing `gsd_state_version` frontmatter key or a missing `## Accumulated Context` heading in STATE.md all mean a scoped `Edit` was replaced by a whole-file overwrite and pre-existing content was destroyed — this gate exists because that is the one irreversible mistake available in a docs-only task. A missing Phase 2 table row means the Key Decisions entry was never added.</fails_when>

    <automated>SHA=$(git rev-parse HEAD) || exit 1; BR=$(git branch --show-current) || exit 1; test -n "$SHA" -a -n "$BR" || { echo "NO-GIT-CONTEXT"; exit 1; }; for WF in android.yml ios.yml; do RID=$(gh run list --workflow "$WF" --branch "$BR" --limit 20 --json databaseId,headSha --jq "[.[]|select(.headSha==\"$SHA\")][0].databaseId"); test -n "$RID" || { echo "NO-RUN $WF $SHA"; exit 1; }; gh run watch "$RID" --exit-status || { echo "RED $WF $RID"; exit 1; }; done && echo "CI-GREEN-BOTH $SHA"</automated>
    <fails_when>non-zero exit, a `NO-RUN <workflow> <sha>` line, a `RED <workflow> <run-id>` line, or the absence of the final `CI-GREEN-BOTH <sha>` line. A docs-only commit should not be able to turn CI red; if it does, something outside `.planning/` was changed.</fails_when>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` contains a `## Phase 02 version deviations` section whose table names all seven of BUILD-01 … BUILD-07, states the version-target policy selected at 02-01's checkpoint, and gives a pinned version and a matched/not-matched verdict for each requirement.
    - Every not-matched row's reason cell carries the forcing build error rather than a paraphrase; any value a SUMMARY did not record is marked as not recorded rather than inferred.
    - The same section records which branch of assumption A2 applied for kotlinx-datetime's `Instant`, the Apollo cache import paths actually used, and the Detekt alpha pinned with its advisory-findings note and before/after issue counts.
    - `.planning/STATE.md` still contains its `gsd_state_version` frontmatter key, its `## Accumulated Context` heading, both Phase 3 entries under `### Blockers/Concerns`, and Task 1's `## Phase 02 DCL pilot outcome` section — nothing was overwritten.
    - The Phase 2 AGP/KGP/KSP re-verification concern under `### Blockers/Concerns` is replaced by a note that it was discharged during this phase.
    - `.planning/PROJECT.md`'s `## Key Decisions` table has a new row naming Phase 2's staged upgrade, its rationale and its actual outcome, and still has its original Phase 1 row unedited.
    - `git show --stat HEAD` lists only files under `.planning/`.
    - `gh run watch` exits 0 for the latest `android.yml` and `ios.yml` runs whose `headSha` equals this commit's SHA.
  </acceptance_criteria>
  <done>Every BUILD-01 … BUILD-07 outcome — the version pinned, whether it matched its REQUIREMENTS.md target, and the forcing error for any that did not — is recorded in `.planning/STATE.md` alongside the DCL pilot result, with a matching Key Decisions row in `.planning/PROJECT.md` and no pre-existing content lost.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `settings.gradle.*` → Gradle build graph | The settings file declares which repositories every dependency in the project is resolved from, and which projects exist. It is the highest-leverage build file in the repository: a repository-list change here silently redirects every artifact resolution in both modules |
| Dev machine / CI runner → repository list declared in settings | Plugin-management and dependency-resolution repositories are the origin of every executed plugin and every linked artifact |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-19 | Tampering | Repository list in the converted settings file: `pluginManagement` and `dependencyResolutionManagement` each declare Google, Maven Central and Gradle Plugin Portal. A conversion that added, reordered toward, or substituted a different repository would redirect artifact resolution for the entire project | high | mitigate | The action enumerates the exact four semantics that must be preserved, naming all three repositories in both blocks, and forbids reshaping build logic to fit the format. The `PROJECT-TOPOLOGY-INTACT-OK` gate proves the settings file is being read and produces exactly the expected root name and two subprojects, and the full `clean` build gate proves every `libs.*` accessor and every plugin still resolves from a working repository set — a redirected or missing repository fails resolution rather than passing quietly. Any DCL conversion that cannot express all three repositories is a blocking limitation to document under D-08, not something to work around. |
| T-02-20 | Spoofing | Lost `include()` during conversion: the root build script would then configure fewer projects without erroring, so a module could silently stop being built, linted or tested while CI still reports green | high | mitigate | The `PROJECT-TOPOLOGY-INTACT-OK` gate asserts both `:androidApp` and `:shared` appear in `./gradlew projects` output **and** that the subproject count is exactly 2, so neither a dropped nor an added include passes. The build gate additionally compiles and tests both modules by explicit task path rather than relying on a lifecycle task that would simply do less work. |
| T-02-21 | Tampering | Two settings files coexisting (`settings.gradle.dcl` plus `settings.gradle.kts`): Gradle selects one, and the other becomes a file a future reader or agent edits believing it is authoritative | medium | mitigate | The `SETTINGS-FORM-EXCLUSIVE-OK` gate fails when both exist and also when neither exists, and the action requires deleting the Kotlin DSL form as part of a successful conversion rather than leaving it behind. |
| T-02-22 | Repudiation | An undocumented version deviation: a dependency pinned below its REQUIREMENTS.md target with no record leaves the project unable to distinguish a deliberate, reasoned pin from an accidental one at audit time | medium | mitigate | Task 2's gate asserts all seven BUILD ids appear in the new STATE.md section, so a silently-skipped requirement outcome fails the gate. The action requires the forcing build error verbatim rather than a paraphrase, and requires marking a cell as not recorded rather than inferring a value from the catalog — the catalog shows what was pinned, never what was intended. |
| T-02-23 | Denial of Service | A whole-file overwrite of `.planning/STATE.md` or `.planning/PROJECT.md` during the docs edit would destroy accumulated project decisions and the Phase 3 concerns that are Phase 3's inputs | medium | mitigate | The action mandates scoped `Edit` replacements and explicitly forbids `Write` for both files. The `DOCS-APPENDED-NOT-OVERWRITTEN-OK` gate asserts pre-existing landmarks survive — the Phase 1 Key Decisions row, STATE.md's frontmatter key and `## Accumulated Context` heading — so an overwrite fails rather than passing as a successful edit. |
| T-02-SC | Tampering | npm/pip/cargo package installs | low | mitigate | Not applicable — this phase introduces no npm/pip/cargo packages (Gradle/Maven ecosystem only), so the automated `package-legitimacy check` gate has no supported ecosystem to run against. This plan adds no dependency at all: it changes a build file's format and edits documentation. The manual audit in `02-RESEARCH.md` § Package Legitimacy Audit covers the phase's one new coordinate family (handled in 02-03), and its one `[SUS]` artifact was gated by a `blocking-human` checkpoint in 02-02. |
</threat_model>

<verification>
- `settings.gradle.dcl` and `settings.gradle.kts` do not both exist, and at least one does.
- `./gradlew projects` reports root `DevFest_Nantes` with exactly `:androidApp` and `:shared`.
- `./gradlew clean :shared:assemble :androidApp:assembleDebug :androidApp:assembleRelease :shared:jvmTest :androidApp:testDebugUnitTest detekt lint` and `:shared:compileKotlinIosSimulatorArm64` both exit 0.
- `.planning/STATE.md` holds a DCL pilot outcome section and a version-deviations table naming all seven BUILD ids, and still holds its frontmatter, `## Accumulated Context` heading and both Phase 3 concerns.
- `.planning/PROJECT.md`'s Key Decisions table holds both the Phase 1 row and a new Phase 2 row.
- `android.yml` reports 4 successful jobs and `ios.yml` reports success on the pilot commit; both are green on the docs commit.
</verification>

<success_criteria>
BUILD-07 is satisfied on either of its two acceptable terms — one build file converted to Gradle Declarative DSL with the project building through it, or every attempt reverted with its specific blocking limitation documented — and the phase's complete version record, per requirement id with reasons for any deviation, is written into the project's durable planning documents, discharging D-03's documentation obligation.
</success_criteria>

<output>
Create `.planning/phases/02-dependency-build-tooling-upgrade/02-05-SUMMARY.md` when done. Record: which DCL target was attempted first and its result, the verbatim DCL error or documented limitation for each target that failed, whether `enableFeaturePreview("TYPESAFE_PROJECT_ACCESSORS")` had a DCL equivalent or was safely dropped, which settings-file form the repository ended on, any table cell marked not-recorded because a prior SUMMARY was silent on it, and both CI run IDs per commit.
</output>
