---
phase: 02-dependency-build-tooling-upgrade
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gradle/libs.versions.toml
autonomous: false
requirements: [BUILD-01]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "The version-target policy (REQUIREMENTS.md-pinned numbers vs. the newer stable releases verified 2026-09-17) is decided once, by the developer, before the first version string is written — and that same policy governs BUILD-01/02/04/05 (D-03)"
    - "`gradle/libs.versions.toml` carries a Kotlin 2.4.x `kotlin` version and a KSP version at or above AGP 9's 2.3.1 floor, with every other dependency group still at its pre-phase value (D-01: one group per commit)"
    - "`./gradlew :shared:assemble :androidApp:assembleDebug :shared:jvmTest :androidApp:testDebugUnitTest` succeeds on the new Kotlin/KSP pair"
    - "Both `.github/workflows/android.yml` (all four jobs) and `.github/workflows/ios.yml` complete successfully on the pushed Kotlin-bump commit (D-02)"
    - "The Android debug app installs, launches, and its Agenda screen renders on a live device/emulator after the Kotlin bump (D-02 local sanity run)"
    - "The Kotlin/KSP bump is a single revertible commit — `git revert` of it restores the pre-phase toolchain with no other change riding along (D-01)"
  artifacts:
    - path: "gradle/libs.versions.toml"
      provides: "Kotlin 2.4.x + AGP-9-floor KSP version pins; single source of truth for every version this phase moves"
      contains: "kotlin = \"2.4"
  key_links:
    - from: "gradle/libs.versions.toml"
      to: "shared/build.gradle.kts"
      via: "alias(libs.plugins.kotlin.multiplatform) / alias(libs.plugins.ksp) resolve the bumped [versions] entries"
      pattern: "alias\\(libs\\.plugins\\.(kotlin\\.multiplatform|ksp)\\)"
    - from: "gradle/libs.versions.toml"
      to: "androidApp/build.gradle.kts"
      via: "alias(libs.plugins.kotlin.android) / alias(libs.plugins.kotlin.compose.compiler) share the same `kotlin` version ref"
      pattern: "alias\\(libs\\.plugins\\.kotlin\\."
  prohibitions:
    - statement: "MUST NOT achieve a green CI signal by weakening the verification surface — no removing or soft-failing a job in android.yml/ios.yml, no deleting the Detekt or lint step, no adding a Detekt baseline to suppress new findings, no narrowing shared/build.gradle.kts's detekt `source.setFrom(...)` source sets"
      status: unverified
    - statement: "MUST NOT land a version pinned below its REQUIREMENTS.md target without recording the deviation and its reason — D-03 grants the escape valve and the documentation obligation together, never the first without the second"
      status: unverified
---

<objective>
Resolve the phase's one open scope question — which version numbers this phase actually targets — and then prove the entire staged-upgrade mechanism end-to-end on the lowest-risk dependency group: Kotlin + KSP.

Purpose: D-01 locks a six-commit sequential staging order and D-02 locks what "green" means between stages (full CI on both workflows + one local sanity build/run). Both are unproven process claims until a real commit travels the whole path. This plan takes the thinnest such path — one `[versions]` edit in the catalog → local assemble + unit tests → commit → push → `android.yml` and `ios.yml` both green → the app launching on a device — so that an architectural dead-end in the staging mechanism surfaces after one commit instead of after five. Stage 1 (Kotlin 2.4.0) is the right tracer because RESEARCH found it carries exactly one forced sibling (KSP, whose version is bound to the Kotlin compiler version) and no source-code change.

Output: one commit bumping `kotlin` and `ksp` in `gradle/libs.versions.toml`, green on both CI workflows and sanity-run on a device; plus a recorded developer decision fixing the version-target policy for BUILD-01/02/04/05.
</objective>

<execution_context>
@/Users/robin/Development/Android/DevFestNantes/.claude/gsd-core/workflows/execute-plan.md
@/Users/robin/Development/Android/DevFestNantes/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-dependency-build-tooling-upgrade/02-CONTEXT.md
@.planning/phases/02-dependency-build-tooling-upgrade/02-RESEARCH.md
@.planning/phases/02-dependency-build-tooling-upgrade/02-PATTERNS.md
@gradle/libs.versions.toml
@.github/actions/android-setup/action.yml
</context>

## Artifacts this phase produces

Symbols and files **created** by Phase 02 (none of these exist in the repo today — exclude them from drift/source-grounding verification):

| Artifact | Created by | Kind |
|---|---|---|
| `settings.gradle.dcl` (or `buildSrc/build.gradle.dcl` if the primary pilot is blocked) | 02-05 | new file |
| `shared/src/commonTest/kotlin/com/gdgnantes/devfest/model/ScheduleSlotDateParsingTest.kt` | 02-04 | new file |
| `appolloCache` — new `[versions]` key in `gradle/libs.versions.toml` | 02-03 | new catalog key |
| `android-kotlin-multiplatform-library` — new `[plugins]` alias in `gradle/libs.versions.toml` | 02-02 | new catalog key |
| `kotlin { android { … } }` — new DSL block in `shared/build.gradle.kts` | 02-02 | new config block |
| `packaging { … }` — new DSL block in `androidApp/build.gradle.kts` (replaces the pre-AGP-9 block) | 02-02 | new config block |
| `## Phase 02 version deviations` — new section in `.planning/STATE.md` | 02-05 | new doc section |

This plan (02-01) creates **no new symbols** — it edits existing `[versions]` values only.

<assumptions>
## Flagged assumptions (spec-less phase — no SPEC.md exists)

Automated edge-coverage classification returned `category: unclassified`, `status: unresolved` for **all seven** of BUILD-01 … BUILD-07. That is expected, not an error: the edge-probe engine classifies user-facing feature/CRUD requirements, and version-bump/build-tooling requirements do not fit its patterns. Per the spec-less fallback protocol, each unresolved row is surfaced here as an explicit assumption rather than silently auto-resolved into `must_haves.truths`.

- **BUILD-01 … BUILD-07 (all 7):** automated edge-coverage classification did not apply. What actually grounds each requirement's acceptance criteria in this plan set's `must_haves` is (a) the D-01 … D-09 locked decisions in `02-CONTEXT.md` and (b) the live-verified version numbers in `02-RESEARCH.md` § Version Verification (re-checked 2026-09-17 against Maven Central / Google Maven `maven-metadata.xml`). No edge-probe-derived predicate backs them.
- **BUILD-01, specific to this plan:** `02-RESEARCH.md` assumption **A3** — the exact KSP release string paired with the final chosen Kotlin version was *not* pinned at research time. Task 2 must look it up live; the acceptance criterion is "KSP changed and the build succeeds", not "KSP equals a specific string".
- **Version staleness:** `02-RESEARCH.md` found 4 of 9 REQUIREMENTS.md-pinned versions already 1–2 releases behind five days after the prior research pass. Every version in this phase is re-verified against its registry `maven-metadata.xml` immediately before the commit that writes it — never taken from recall.
- **`kmp-native-coroutines`:** `kmpNativeCoroutines = "1.0.0-ALPHA-45"` is a Kotlin-compiler-plugin-coupled dependency that `02-RESEARCH.md` did **not** analyse. It is treated in Task 2 as a forced sibling of the Kotlin bump in the same class as KSP: bumped only if the KGP move breaks it, and recorded if so.
</assumptions>

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <decision>Does Phase 02 target the exact version numbers REQUIREMENTS.md pins, or the newer stable releases verified on 2026-09-17?</decision>
  <context>
    REQUIREMENTS.md hard-pins four numbers: Kotlin 2.4.0 (BUILD-01), AGP 9.2.0 (BUILD-02), Compose BOM 2026.08.00 (BUILD-04), Apollo 5.0.1 (BUILD-05). Those numbers were the latest-at-authoring-time snapshot from 2026-09-12. Re-verification on 2026-09-17 (02-RESEARCH.md § Version Verification, direct `maven-metadata.xml` fetches) found newer stable releases for all four: Kotlin 2.4.20, AGP 9.4.0, Compose BOM 2026.09.00, Apollo 5.2.0. All eight numbers are real, published, non-withdrawn stable releases.

    This is a scope/requirements question, not a technical one, which is why it is a checkpoint rather than a discretionary call. D-03 already establishes the philosophy "pin to the newest version that actually builds and document the gap rather than stopping the phase" — but D-03's escape valve is written for blockers hit *during* a bump, not for proactively adopting a version newer than the locked requirement text. BUILD-03 (Gradle 9.7.1) and BUILD-06 ("latest stable", unpinned) are unaffected either way: 9.7.1 is exactly current, and BUILD-06's own text already says "latest stable".

    Whichever option is chosen governs all four pinned requirements consistently for the rest of the phase, and Task 2 writes the first of those numbers.
  </context>
  <options>
    <option id="pinned">
      <name>Target the REQUIREMENTS.md numbers literally: Kotlin 2.4.0, AGP 9.2.0, Compose BOM 2026.08.00, Apollo 5.0.1</name>
      <pros>The requirement text is satisfied verbatim, so BUILD-01/02/04/05 verify without an explanation. AGP 9.2.0 has been GA longer than 9.4.0, so the AGP-9 migration — the riskiest stage — lands on the better-documented, more-reported release. No deviation to document under D-03.</pros>
      <cons>The project ships already two patch releases behind on Kotlin and two minor releases behind on AGP the day the phase completes, so Phase 3 inherits a toolchain it may want to bump again. Detekt's AGP-9 compatibility alphas are being developed against the newest AGP, so an older AGP is marginally less-tested against the alpha this phase must pin.</cons>
    </option>
    <option id="newest-verified">
      <name>Target the newest verified stable releases: Kotlin 2.4.20, AGP 9.4.0, Compose BOM 2026.09.00, Apollo 5.2.0</name>
      <pros>The project lands on the actual current stack, so Phase 3 (multi-module extraction) starts from a toolchain it will not immediately want to move again. Bug fixes in the intervening releases are picked up for free — relevant because AGP 9.x is a young major line.</pros>
      <cons>BUILD-01/02/04/05 are then satisfied by a superset rather than verbatim, which must be recorded as a deliberate deviation in STATE.md/REQUIREMENTS.md under D-03. AGP 9.4.0 is newer and therefore has less community-reported migration experience than 9.2.0 for the forced Detekt/Hilt/Firebase-perf interactions.</cons>
    </option>
  </options>
  <resume-signal>Select: `pinned` or `newest-verified`</resume-signal>
</task>

<task type="tracer">
  <name>Task 1: End-to-end Kotlin + KSP bump — catalog to device, one dependency group only</name>
  <reversibility rating="reversible">A single `[versions]` edit in one file; `git revert` of this one commit restores the pre-phase Kotlin/KSP pair with nothing else riding along (D-01's stated reversal mechanism).</reversibility>
  <precondition>`android info` reports at least one connected device or running emulator — the D-02 local sanity run installs and launches the debug app on a live target. If none is attached, start one with `android emulator start` before proceeding; if that also fails, halt rather than skipping the sanity run.</precondition>
  <files>gradle/libs.versions.toml</files>
  <read_first>
    - `gradle/libs.versions.toml` — the file being edited; note the `[versions]` block's exact current `kotlin` and `ksp` lines and that `kotlin-android`, `kotlin-cocoapods`, `kotlin-compose-compiler`, `kotlin-multiplatform`, `kotlin-serialization`, `kotlin-reflect`, `kotlin-test` and `kotlin-test-junit` all resolve through the single `kotlin` version ref (so one edit moves eight entries)
    - `shared/build.gradle.kts` — consumes `libs.plugins.kotlin.multiplatform`, `libs.plugins.ksp`, `libs.plugins.kmp.native.coroutines`; also sets `jvmTarget` to `JvmTarget.JVM_11` inside `androidTarget { compilerOptions { … } }`, which the Kotlin bump must not disturb
    - `androidApp/build.gradle.kts` — consumes `libs.plugins.kotlin.android`, `libs.plugins.kotlin.compose.compiler`, `libs.plugins.ksp`; `ksp(libs.dagger.hilt.compiler)` / `kspAndroidTest` / `kspTest` are the three KSP consumers that break first if the KSP version is wrong
    - `.planning/phases/02-dependency-build-tooling-upgrade/02-RESEARCH.md` — § Version Verification (the cross-cutting table's KSP row and assumption A3), and the anti-pattern "Deferring the KSP bump to the AGP commit"
    - `.planning/phases/02-dependency-build-tooling-upgrade/02-PATTERNS.md` — § `gradle/libs.versions.toml`, which lists the exact per-stage catalog lines and confirms only `kotlin` and `ksp` belong to Stage 1
    - `.github/actions/android-setup/action.yml` — confirms CI pins JDK 17 (`zulu`) and invokes `gradle/actions/setup-gradle@v4` with `gradle-version: wrapper`, so this commit needs no CI-file edit
  </read_first>
  <action>
    Implements BUILD-01 and D-01 stage 1. One dependency group, one commit.

    Re-verify both target versions live before writing anything — 02-RESEARCH.md found 4 of 9 pinned numbers stale after five days, so registry metadata, not recall, is the source:
    `curl -s https://repo1.maven.org/maven2/org/jetbrains/kotlin/kotlin-gradle-plugin/maven-metadata.xml`
    `curl -s https://repo1.maven.org/maven2/com/google/devtools/ksp/com.google.devtools.ksp.gradle.plugin/maven-metadata.xml`

    In `gradle/libs.versions.toml`'s `[versions]` table, set exactly two keys:
    - `kotlin` — to `2.4.0` if the Task above resolved `pinned`, or `2.4.20` if it resolved `newest-verified`.
    - `ksp` — to the KSP release string KSP publishes for that chosen Kotlin version. AGP 9's documented floor is 2.3.1 and 02-RESEARCH.md verified 2.3.12 as the current plugin-marker latest; the exact paired string is RESEARCH assumption A3 and was not pinned, so look it up on the KSP GitHub releases page (github.com/google/ksp/releases) for the chosen Kotlin version and use that. KSP moves in the Kotlin commit and not the AGP commit because its compatibility is bound to the Kotlin compiler version, not to AGP.

    Change nothing else in the catalog. `agp`, `detekt`, `dagger`, `firebasePerfPlugin`, `composeBom`, `appollo`, `firebaseBom`, `kotlinxCoroutines`, `kotlinxSerialization`, `kotlinxDatetime`, `googleServicesPlugin` and `firebaseCrashlyticsPlugin` all belong to later stages (02-02 … 02-04) and keep their current values in this commit — D-01 rejected combining groups even when convenient.

    One conditional forced sibling: `kmpNativeCoroutines` (`com.rickclephas.kmp.nativecoroutines`, currently `1.0.0-ALPHA-45`) is a Kotlin-compiler-plugin-coupled dependency in the same class as KSP, and 02-RESEARCH.md did not analyse it. If the build fails on `kmp-native-coroutines` plugin resolution or on a compiler-plugin version mismatch after the KGP bump, bump `kmpNativeCoroutines` to the release the plugin publishes for the chosen Kotlin version **in this same commit** (it is part of the Kotlin toolchain group, not a separate stage) and note the version chosen in the SUMMARY. Do not bump it speculatively if the build is green without it.

    If either target version does not build, apply D-03: pin the newest version that does build, and record the deviation plus its reason in the SUMMARY so that 02-05 Task 2 can consolidate it into STATE.md. Do not stop the phase, and do not silently downgrade without the record.

    Commit with `build(02-01): bump Kotlin to <version> and KSP to <version>`, then push the branch so both workflows run on this commit and drive them to completion.
  </action>
  <verify>
    <automated>grep -vE '^[[:space:]]*#' gradle/libs.versions.toml > /tmp/cat-02-01.txt && grep -qE '^kotlin = "2\.4\.' /tmp/cat-02-01.txt && ! grep -qE '^ksp = "2\.2\.20-2\.0\.3"' /tmp/cat-02-01.txt && grep -qE '^agp = "8\.13\.0"' /tmp/cat-02-01.txt && grep -qE '^composeBom = "2025\.09\.01"' /tmp/cat-02-01.txt && grep -qE '^appollo = "4\.3\.3"' /tmp/cat-02-01.txt && grep -qE '^firebaseBom = "33\.16\.0"' /tmp/cat-02-01.txt && echo "STAGE1-CATALOG-OK"</automated>
    <fails_when>non-zero exit, or the `STAGE1-CATALOG-OK` line is absent. A missing `kotlin = "2.4.` or an unchanged `ksp` value means the bump never landed; a failing `agp`/`composeBom`/`appollo`/`firebaseBom` match means a second dependency group leaked into this commit, which is the D-01 violation this gate exists to catch.</fails_when>

    <automated>./gradlew --no-daemon :shared:assemble :androidApp:assembleDebug :shared:jvmTest :androidApp:testDebugUnitTest</automated>
    <fails_when>non-zero exit, or a `FAILURE: Build failed` / `Execution failed for task` line in the output. Watch specifically for `ksp`, `kspTest` or `kspAndroidTest` task failures naming Hilt-generated code — that is the KSP-version-mismatch signal, not a Kotlin problem.</fails_when>

    <automated>SHA=$(git rev-parse HEAD) || exit 1; BR=$(git branch --show-current) || exit 1; test -n "$SHA" -a -n "$BR" || { echo "NO-GIT-CONTEXT"; exit 1; }; for WF in android.yml ios.yml; do RID=$(gh run list --workflow "$WF" --branch "$BR" --limit 20 --json databaseId,headSha --jq "[.[]|select(.headSha==\"$SHA\")][0].databaseId"); test -n "$RID" || { echo "NO-RUN $WF $SHA"; exit 1; }; gh run watch "$RID" --exit-status || { echo "RED $WF $RID"; exit 1; }; done && echo "CI-GREEN-BOTH $SHA"</automated>
    <fails_when>non-zero exit, a `NO-RUN <workflow> <sha>` line (the commit was never pushed, or no run was triggered for it), a `RED <workflow> <run-id>` line, or the absence of the final `CI-GREEN-BOTH <sha>` line. This is D-02's "full CI (Android + iOS) passes on the pushed commit" gate and reuses the `gh run list`/`gh run view` idiom Phase 01 proved.</fails_when>

    <automated>android run && android layout | grep -q 'com.gdgnantes.devfest.mobile.androidapp.dev' && echo "LOCAL-SANITY-RUN-OK"</automated>
    <fails_when>non-zero exit from `android run` (build, install, or launch failed), the package id absent from the `android layout` dump (the app launched but no UI attached — a crash-on-start), or the absence of the `LOCAL-SANITY-RUN-OK` line. This is D-02's "one local sanity build/run per group" — it catches what CI cannot, e.g. a Compose Preview or runtime-init regression.</fails_when>

    <human-check>
      The debug app (`com.gdgnantes.devfest.mobile.androidapp.dev`) was built, installed and launched on the attached device after the Kotlin/KSP bump, and both CI workflows are green on the commit.
      Confirm the app looks and behaves as it did before the bump: the Agenda screen lists sessions (not an empty/error state), and moving between the bottom-navigation destinations works. Screenshots from `android screen capture` are attached in the SUMMARY.
      This is the D-02 sanity confirmation for stage 1, not a full regression pass — D-04's per-screen smoke pass belongs to 02-02.
    </human-check>
  </verify>
  <acceptance_criteria>
    - `gradle/libs.versions.toml` `[versions]` has `kotlin` at `2.4.0` or `2.4.20` (matching the policy the checkpoint resolved) and `ksp` at a value different from its pre-phase string and ≥ 2.3.1.
    - The same `[versions]` table still shows `agp = "8.13.0"`, `composeBom = "2025.09.01"`, `appollo = "4.3.3"` and `firebaseBom = "33.16.0"` — no other group moved in this commit.
    - `./gradlew :shared:assemble :androidApp:assembleDebug :shared:jvmTest :androidApp:testDebugUnitTest` exits 0.
    - `gh run watch` exits 0 for the latest `android.yml` run and the latest `ios.yml` run whose `headSha` equals this commit's SHA.
    - `android run` exits 0 and the subsequent `android layout` dump contains `com.gdgnantes.devfest.mobile.androidapp.dev`.
    - `git log -1 --oneline` shows exactly one commit for this stage, and `git show --stat HEAD` lists `gradle/libs.versions.toml` as its only changed file.
    - If any target version was pinned below its REQUIREMENTS.md/RESEARCH.md target, the SUMMARY names the version used, the version intended, and the build error that forced the change.
  </acceptance_criteria>
  <done>Kotlin 2.4.x and its paired KSP release are pinned in the version catalog as one revertible commit; the project assembles and unit-tests locally, both CI workflows are green on that exact commit, and the debug app runs on a device.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner / dev machine → Maven Central + Google Maven + Gradle Plugin Portal | Untrusted artifact bytes cross here on every dependency resolution; a wrong coordinate is fetched and, for a Gradle plugin, *executed* at configuration time |
| `gradle/libs.versions.toml` → Gradle build graph | The catalog is the only place a plugin id or artifact coordinate is declared; an edit here changes what code runs during the build |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Elevation of Privilege | `[plugins]` / `[versions]` in `gradle/libs.versions.toml` — Gradle resolves and executes plugin code at configuration time with developer/CI privileges | high | mitigate | Only the `kotlin` and `ksp` **version** values change in this plan; no plugin id, group, or artifact name is touched. Both versions are re-verified against `repo1.maven.org` `maven-metadata.xml` by direct fetch before the edit (the action's first step), never from recall. The `STAGE1-CATALOG-OK` gate asserts every other group's value is unchanged, so a coordinate cannot drift in under cover of a version bump. |
| T-02-02 | Tampering | KSP version string looked up from `github.com/google/ksp/releases` (RESEARCH assumption A3 — not pinned at research time) | medium | mitigate | The release string is cross-checked against the `com.google.devtools.ksp.gradle.plugin` plugin-marker `maven-metadata.xml` on Maven Central before use — the GitHub page is used to find the *pairing*, Maven Central to confirm the artifact actually exists under the official group. |
| T-02-03 | Spoofing | Typosquat of `org.jetbrains.kotlin` / `com.google.devtools.ksp` group coordinates | low | accept | No group or artifact name is authored in this plan — the existing `[plugins]`/`[libraries]` entries already carry the official coordinates and are asserted unchanged by the catalog gate. The only genuinely new coordinate family in this phase (`com.apollographql.cache`) is handled in 02-03 with its own gate. |
| T-02-SC | Tampering | npm/pip/cargo package installs | low | mitigate | Not applicable to this phase — it introduces no npm/pip/cargo packages (Gradle/Maven ecosystem only), so the automated `package-legitimacy check` gate has no supported ecosystem to run against. The one new Maven coordinate family is manually audited in `02-RESEARCH.md` § Package Legitimacy Audit, and the one `[SUS]`-verdict artifact (`dev.detekt` 2.0.0-alpha) is gated by a `blocking-human` checkpoint in 02-02 before it is pinned. |
</threat_model>

<verification>
- `grep -vE '^[[:space:]]*#' gradle/libs.versions.toml | grep -E '^(kotlin|ksp|agp|composeBom|appollo|firebaseBom) ='` shows the Kotlin/KSP pair moved and the other four unchanged.
- `./gradlew --no-daemon :shared:assemble :androidApp:assembleDebug :shared:jvmTest :androidApp:testDebugUnitTest` exits 0.
- The latest `android.yml` and `ios.yml` runs for this commit's SHA both report `completed:success`.
- `android run` exits 0 on a live device and the app's UI tree is reachable via `android layout`.
- `git show --stat HEAD` lists `gradle/libs.versions.toml` and nothing else.
</verification>

<success_criteria>
The version-target policy for BUILD-01/02/04/05 is decided and recorded, and BUILD-01 is satisfied by a single revertible commit that is green on both CI workflows and sanity-run on a device — proving D-01's staging order and D-02's green definition work end-to-end before the riskier stages start.
</success_criteria>

<output>
Create `.planning/phases/02-dependency-build-tooling-upgrade/02-01-SUMMARY.md` when done. Record: the version-target policy the developer selected, the exact `kotlin`/`ksp` (and, if bumped, `kmpNativeCoroutines`) strings written, any D-03 deviation with its forcing build error, the two CI run IDs, and the `android screen capture` output from the sanity run.
</output>
